# OUTLOOK — planned next steps (updated at reviewer handoff, post-2.9/3.1)
*(Ordered; each step's precondition is the one before it unless noted.)*

## Status at handoff (July 2026, branch v1-merge-continuation-without-fable)

DONE: 2.9a firewall seal (learned-beats-hand on approach confirmed clean, 94/97/95 vs ceiling).
DONE: autonomous discovery arc 2.9b-2.9g — headline: 2.9e positive existence proof (unguided
internal-surprise spawning grew load-bearing composites; hand-audited as real chess: confinement
via both-interior-diagonals-sealed, safe rook reposition; guided control arm = 0, so not
scaffolded). Bounded by honest negatives: no recurrence at scale (2.9f/g), warm-up topology does
not carry across rungs without a shared graph (3.1), helpful candidates die of inactivity decay
and budget pressure (3.0 fate logs, signature 1/1/2/2/3 three runs running).
PROMOTED CONCLUSION (in BRIEF): conjunctions are born unguided and can be causal; the flat
substrate cannot make them permanent; bottleneck = survival/credit representation + runtime cost.
CAUTION LEARNED: Phase 3.0 was labeled graph-native but ran StemCellTerminals inside the probe
harness (FormalReConEngine: 0 references). "Graph-native" henceforth means: nodes in the engine,
evaluated via request/confirmation, or it does not count.
DONE AFTER HANDOFF: **3.2 real native migration** now passes the graph-native acceptance check:
composites are TERMINAL nodes in the Mate1/Mate2 foundation graph, requested by SUB/SUR through
`FormalReConEngine`, and predicate eval counters increment only inside `_evaluate_terminal`.
Result is a useful negative: 5/5 acceptance pass, but mature population 0/0/0/0/0, no recurrence,
no cross-rung load-bearing survivor, and win rate 0/16 for all seeds. Stop rule fired on mature
population failure. Artifact: `reports/autogrowth/clean_slate_krk/phase3_2_real_native_graph_ecology/summary.json`.
DONE AFTER HANDOFF: **3.3 migrated-flat native host gate** materializes the sealed Stage B atoms
as weighted TERMINAL nodes under a native `stage_b_policy` SCRIPT and proves request-gated atom
evaluation through `FormalReConEngine`. The architecture acceptance passes, but behavioral
equivalence fails against official sealed artifacts: 71/73/71 versus 93/92/92 on the 128 heldout.
The run correctly stops before ecology. Artifact:
`reports/autogrowth/clean_slate_krk/phase3_3_migrated_flat_native_ecology/summary.json`.
DONE AFTER HANDOFF: **3.4 host tiebreak alignment** makes the migrated host match executable flat
replay's deterministic `(score, uci)` tiebreak, legal ordering, repetition guard, and
`_sealed_action_key_scales`. The gate still fails against official sealed artifacts: 71/73/71
versus 93/92/92 (deltas -22/-19/-21), and the residual diagnostic finds 0 first-move differences
between current executable flat replay and migrated host. Ecology correctly does not run. Artifact:
`reports/autogrowth/clean_slate_krk/phase3_4_host_tiebreak_alignment/summary.json`.
DONE AFTER HANDOFF: **3.5 equivalence forensics** resolves the paradox. Official 93/92/92 is a
full 16-white-move rollout artifact, not a single-move label. Current executable flat replay and
migrated native host have 0 full-trace differences and both score 71/73/71; stored artifact sample
traces diverge downstream in continuation/black-reply provenance (e.g. seed11 row640 artifact stops
after 2 white moves, current/migrated continues to 8; seed12/13 show different successor FENs before
the next white choice). Artifact:
`reports/autogrowth/clean_slate_krk/phase3_5_equivalence_forensics/summary.json`.

## Immediate (the decisive step)

1. **Official continuation provenance repair before ecology**: recover or retire the exact
   black-reply/rollout provenance that produced the saved Stage B 93/92/92 artifacts. The migrated
   native host now matches current executable flat replay through full traces, so the remaining gap
   is saved-artifact continuation provenance, not tiebreak, graph routing, or ecology. Do not run
   ecology variants until the host-alone yardstick is reproducible or the yardstick is replaced by
   the current executable 71/73/71 baseline.
2. **Merge to main** — overdue; two long-lived branches now exist. Run the merge-prep checklist.
3. **Growth animation** — blocked until a run produces a mature population worth animating.

## Operating without the reviewer (standing protocol)

- Three-strikes rule: the same instrumented signature in 3 consecutive runs without a targeted
  fix = stop running variants, fix the named cause.
- Acceptance checks before results count: any "graph-native"/architectural claim is verified at
  call-chain level (what executes what), not by class names or file names.
- Measurement sovereignty, quantifier polarity declarations, paired evals with intervals,
  exact-ungated judges, stop rules: unchanged, non-negotiable.
- Independent audit: any NEW pool/stratum/judge gets predicates re-derived from scratch (second
  model or by hand) before its numbers enter the record.
- banQuo keeps the "hey wait" veto; it has the best hit rate of any signal in the project.

## Phase 3+ (unchanged sequencing, after 3.2)

4. **2.9b — autonomous discovery probe, approach rung (primary target).** Spawn-and-credit must
   grow structure on the rung with NO hand procedure visible: surprise/failure-localized spawning
   (grow candidates where current structure fails or predicts wrongly), contrastive credit,
   exact ungated judge. Graded against three yardsticks that now exist: random/fallback floor,
   the dumb hand ceiling (0.59–0.64), and the clean flat-learner baseline (~0.67–0.71). The
   dress-rehearsal methodology is the chase campaign's failure-cluster loop, mechanized:
   cluster failures by percept signature → probe with own imagination (null-move playouts,
   quantifier calls on candidate quiet moves) → spawn gated candidate branches → credit → keep.
   Stretch target: near-corner conversion (where the hand ceiling died at 0.80).
5. **Graph-native learned dispatch migration:** move the flat episode learner's competence into
   graph structure (learned quorum weights / learned branch conditions), so learned and
   hand-built skills share one substrate. Includes M4-successor: promotion by counterfactual
   evidence over composite (quorum) units, not per-key precision.
6. **Interpretability pass on discovered structure:** does grown structure converge with the
   ceilings' decision rules? Does anything load on the L-shape/knight-distance atoms organically?
   (The gold-standard "discovered opposition/chase" claim lives here.)

## Phase 3+ — judgment and imagination

7. **Learned positional feel (appraisal bus):** regression on plies-to-next-basin from existing
   game logs; internal terminals sensing recognizer confirmations → a potential over positions.
   Roles: fallback guidance (subsumes repetition guard), tie-breaking, and PLAN INTERRUPTION
   (inhibitory internal terminal on plan RET paths: hold plan while felt value non-decreasing).
   Doctrine: feel orders/interrupts, never confirms — tempo moves are invisible to any
   state-value; quantifiers cross the plateaus.
8. **Imagination as planning:** null-move own-move chains (plan proposal), surprise terminal
   (predicted-vs-actual delta) feeding attention and spawn-localization. Machinery exists
   (virtual frames); this makes it autonomous rather than quantifier-internal.
9. **Purify the dispatcher:** replace the priority fan with top-down goal request (win →
   nearest-achievable-subgoal chain) once any skill lattice branches.

## Transfer and scale (after discovery has a result either way)

10. **Level 2 replay:** one command, empty graph → full KRK system via the curriculum
    (demo + the topology-growth timelapse animation; trace data already logged).
11. **KQK transfer:** generalize piece-parameterized feature schemas (dist(X,Y), attacked(X))
    with lazy binding; three arms (scratch / frozen-KRK-parents / KRK-init-plastic); measure
    head-start, pre-register ceiling effects.
12. **KPK:** the real opposition/tempo test — side-to-move atoms become load-bearing; draw basins;
    the discovered-opposition claim gets its decisive venue.
13. **Interactive game-playback animation** (nodes/edges lighting per tick, request cascade to
    the bound actuator) — renderer over existing trace JSONL; article/demo asset.

## Standing questions to keep honest

- Does the affordance/veto ratio stay flipped under discovery (not just weight-training)?
- Can grown quorums beat the M4-compression problem as predicted?
- Where exactly does flat weighting end and composition begin? (Stage A/B boundary — refine it.)
- What does the discovery loop cost in compute vs hand-mining? (Article-relevant economics.)
