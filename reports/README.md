# Active Reports

This directory is reset for `KRK Autogrowth v0`.

Historical reports were moved to:

```text
archive/pre_autogrowth_2026_06_10/reports/
```

Do not recreate the old report/control-plane tree as the active workflow. New reports should be minimal outputs of actual learning/evaluation runs.

Expected active outputs:

- `reports/autogrowth/*.json` for committed run summaries.
- `reports/autogrowth/*.md` for short human summaries when useful.
- `reports/autogrowth/runs/` for large or intermediate run artifacts.

Reports are observers, not teachers. Learners must not consume old report row IDs, Stage7/Stage8 labels, selector labels, or hand-authored curriculum labels as causal features.
