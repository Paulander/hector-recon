# Native deferred specialization engineering closure

Status: **PASS — development canary only; no scientific claim**

Starting point: `59903b744078150a3b65c7166a0de8cfe45edf40`

Canary artifact SHA-256: `9e455a32c913a581d75dfeef77686fbe248db6f2ad6a0fe8a538b43136346b04`

## Engineering transition

Mixed-outcome genome nominations and deferred specialization children now have
distinct dormant origins. A single pure matcher preserves MATURE behavior and
allows non-mature structural unfolding only for an exact, one-level,
immutable-hypothesis-bound specialization lineage. V2 graph confirmation owns
revocation, request and eligibility; the contradiction transaction appends an
ordered request but creates no child. Sealed requests are consumed once at a
predetermined successor structural frontier, and children begin dormant with a
fresh V3 escrow and zero authority.

An adjacent synthetic historical child had lineage ordinals but no immutable
specialization binding. It is deliberately retained as unbound historical
ledger provenance and cannot unfold through a PROBATION parent; inventing a
binding would misstate its reads. The focused pure matcher test separately
proves that genuinely bound PROBATION lineage matches.

## Focused validation

- matcher/origin/capacity/envelope: `10 passed in 3.34s`;
- immediately adjacent V2 authority paths: eight passed across the bounded
  selections (historical compatibility, VIRTUAL isolation, mixed shadow,
  historical isolation, atomic receipt guard, maturity/disconnect, revocation,
  and serialization); the final five-test selection was
  `5 passed in 1558.58s`;
- development canary: PASS, 32 already-viewed references, four disjoint
  regions, predetermined structural frontier 69;
- no complete repository suite, fresh data, scientific runner, cohort, R1, or
  retired-successor evaluation was opened.

The 192-request boundary accepts through slot 192 and rejects the 193rd
atomically. Both specialization arms consumed one request and exactly one
genome call; disconnected emitted zero dummy requests. Confirmed and rejected
candidate-terminal states survive serialization.

## Canary result

| arm | TP | FP | abstention | safe positive coverage |
| --- | ---: | ---: | ---: | ---: |
| local contrast | 1 | 3 | 0 | 0.0 |
| disconnected | 0 | 0 | 4 | 0.0 |
| counterexample blind | 1 | 1 | 2 | 0.0 |

Every arm records the triggering parent false prediction. Parent influence is
zero after contradiction, including the intervening VIRTUAL query; child
influence is zero before certification. The unsafe specialization arms are
reported without reinterpretation: this canary establishes the engineered
transition and evidence isolation, not safe competence.

At both child births, all seven escrow categories are present in exact order,
all categorized reads are subsets of complete `V_birth`, discovery exclusion
equals complete `V_birth` (69 records), and nomination/birth/certification
frontiers are 68. Each child starts with zero support, successes,
contradictions, certification receipts and decision influence. Local category
counts are `55/64/40/4/45/1/0`; blind counts are
`43/64/40/4/45/1/0` in the documented category order.

Serialization parity holds at the atomic revocation-plus-queue append, queue
seal, request consumption, child birth and child certification boundaries.
Child ablation removes only child decision influence while parent structural
commitment remains available. Semantic reference parity checks preserved
actuation, ordered signals, typed terminals and terminal sources. R0 topology,
weights, credit and lifecycle digests are identical across source and all arms.

Remaining external-requirement mismatch: **none known**. The historical
unbound-child isolation above is an explicit fail-closed compatibility boundary,
not a relaxation of the frozen-lineage requirement.
