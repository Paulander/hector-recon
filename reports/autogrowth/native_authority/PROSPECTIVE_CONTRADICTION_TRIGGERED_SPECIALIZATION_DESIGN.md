# Prospective contradiction-triggered specialization of shadow-origin hypotheses

Contract version: **v3 — blocking engineering corrections integrated**
Status: **ENGINEERING RECLOSED; CORRECTED DEVELOPMENT CANARY PASS; NO SCIENTIFIC EXECUTION**

## Frozen question and boundary

Hypothesis: a prospectively certified shadow-origin hypothesis can be safely
specialized only when its contradictory REAL outcome graph-confirms revocation
and an unevaluated request, child birth is deferred to a predetermined later
structural epoch, and the child earns authority from still-later REAL evidence.
The strongest null is no safe child engagement advantage over revocation-only
or counterexample-blind controls.

The learner may read accepted REAL traces, opaque typed-signal identities,
graph state, frozen lineage, and immutable escrows. The laboratory may schedule
four disjoint development regions and score the final sealed region after the
fact. No scorer, confidence weighting, chess-specific feature, active evidence
seeking, wider conjunction, negative child, recursive specialization, fresh
data, 32-seed runner, retired successor, or R1 mechanism is in scope.

The changed factor is the temporal composition of existing V2 revocation with
existing one-level specialization. Kill the canary on same-event child work,
evidence/frontier leakage, unmatched specialization budgets, serialization or
generation drift, or any R0/action/semantic-trace change. Compute is limited to
focused adjacent tests and one four-region development canary. Its sealed,
read-only evaluation region is the frozen transfer check and makes no scientific
claim.

## Dormant provenance and exclusion

Dormant cells carry one explicit immutable origin:

- `MIXED_OUTCOME_SHADOW`: an actual genome nomination finalized from mixed
  discovery outcomes.
- `DEFERRED_SPECIALIZATION_CHILD`: a one-level child born in a successor
  structural epoch.

A deferred child is never identified by `prune_reason == mixed_outcomes`.
Origin is present in manifests, continuation identity, serialization, and
capacity accounting. Mixed shadows use ordinary trial capacity only at genome
nomination; dormant specialization children use their separate bounded storage.

Every dormant cell is excluded from ordinary mature-context discovery,
ordinary proposal grammar, decision roots, correction legs unless separately
prospectively certified, reward/value, child-response, and action selection.

## Canonical lineage-only structural matching

One pure canonical matcher is shared by envelope `_cell_matches()` and V2
`_structural_pattern_matches()`. Existing MATURE behavior remains unchanged.
Direct base members always match structurally. A dormant context may be
recursively unfolded only for the child being tested and only when all of these
hold:

1. the context names the child's exact frozen lineage parent;
2. the child escrow operation is `specialization`;
3. child depth equals parent depth plus one and is exactly one;
4. the child contains exactly one `context:<parent>` member;
5. the child binds the parent's immutable hypothesis digest;
6. the parent is DORMANT or PROBATION, never PRUNED or replaced;
7. recursion reads only the parent's frozen member pattern.

Parent prospective certification and decision state are irrelevant to this
structural recursion. An unrelated dormant cell is never structurally usable.

## V2 graph request and atomic REAL event

V2 receipt consumption must not call the old immediate specialization
materializer. Its graph adds `SPECIALIZATION_REQUEST` and
`SPECIALIZATION_ELIGIBILITY` nodes. For a depth-zero
`MIXED_OUTCOME_SHADOW` parent, request confirmation in one REAL transaction
requires:

- the parent pattern matched before outcome mutation;
- the parent was prospectively certified in the pre-mutation snapshot;
- the grounded result contradicted fixed polarity;
- graph-produced revocation confirmed;
- no prior lifetime request exists;
- mode is not `DISCONNECTED`.

For exact graph-emitted parent requests only, Python may attach candidate
terminals for every role-permitted opaque identity in the request-bound support
vocabulary. Python may not outcome-filter or rank identities. Persist graph
revocation IDs, graph request IDs, eligible IDs per request, and every candidate
terminal's identity, role permission, recursive implication status, exact
distinct parent-supporting REAL receipt and stable-physical-interaction IDs,
support count, contradiction presence, specialization mode, confirmation, and
graph node state. A terminal confirms exactly when it is role-permitted, is not
recursively implied, has at least `MIN_SUPPORT` parent-polarity supports, and is
either counterexample-blind or absent from the triggering contradiction.
LOCAL_CONTRAST and COUNTEREXAMPLE_BLIND receive the same anonymous terminal
population before that final contradiction-absence predicate. Only
graph-emitted request IDs may append the organism-owned queue.

Atomic order is fixed: pre-outcome prediction commits; REAL action occurs;
grounded result arrives; graph emits contradiction, revocation, request, and
eligibility; decision state clears and the request appends atomically; event
closes with no child creation, matching, capacity allocation, or evaluation.
The emission persists three independent ledgers:
`prequential_false_authority_ids`, `graph_revocation_ids`, and
`graph_specialization_request_ids`. The first means cells whose committed
pre-outcome decision was contradicted; it is derived from immutable pre-outcome
classification, matching certified cells, truthful REAL outcome, and fixed
polarity, never from mode or request creation. Requests are a subset of
revocations; revocation may occur without a request; later state changes never
erase the prequential error.
Parent influence after consumption, including intervening VIRTUAL queries, is
zero. A required revocation without its request, or a request without graph
revocation, is an implementation failure. A focused fail-fast test forbids V2
from invoking immediate `_materialize_specialization()` during consumption.

## Immutable accepted-REAL references and child escrow

Every accepted REAL interaction appends one immutable task-generic reference:
original receipt ID, ordinal, stable physical-interaction ID, semantic trace
digest, typed-signal digest, observed outcome, and source generation. Original
receipt identities are preserved; V2 never recreates an outcome as historical
discovery evidence.

For parent `p`, contradiction `c`, and selected opaque identity `b`, retain:

- `D_all`: parent discovery plus transitive ancestor records;
- `D_sup`: parent-supporting discovery records;
- `S_real`: later parent-supporting REAL records before `c`;
- `C`: the contradiction record;
- `E_b`: exact records inspected by the eligibility terminal for `b`;
- `M_b`: visible records matching frozen parent pattern plus `b`;
- `V_birth`: every accepted REAL record visible at child birth.

The child's fresh immutable escrow categorizes direct child matches, parent
discovery reads, parent discovery support, parent prospective support,
eligibility reads, contradiction trigger, and transitive ancestor reads.
Every categorized read is contained in `V_birth`. Discovery exclusion is the
complete `V_birth`; nomination read frontier is the maximum categorized-read
ordinal; birth and certification frontiers are the maximum `V_birth` ordinal.

The child inherits fixed parent polarity but starts DORMANT with zero support,
successes, contradictions, certification records, and decision influence.
Only distinct REAL receipts beyond its frontier may change that state. VIRTUAL
frames remain measurement-only.

## Append-only successor generations

Lifecycle sequence is append-only:

`STRUCTURAL_OPEN(g) -> PROSPECTIVE_OPEN(g) -> PROSPECTIVE_SEALED(g) -> STRUCTURAL_OPEN(g+1) -> PROSPECTIVE_OPEN(g+1)`.

Generation `g` plus its sealed request queue produces immutable structural
successor `g+1`. The successor binds the complete continuation digest and
accepted-REAL-ledger digest of `g`, frozen request-queue digest, predetermined
structural-epoch schedule, new candidate-manifest digest, and all prior parent
decision states and transition history. Validate and replay `g` independently,
then validate child births; replay only post-birth events through `g+1`.
Transition occurs at a predetermined event frontier, never in response to a
favorable queue.

## Bounded queue and child storage

Both capacities equal the existing proposal-attempt cap: request queue `192`;
dormant specialization-child storage `192`. The append-only queue is ordered by
`(contradiction ordinal, parent cell ID)`.

The specialization genome seed is derived before outcomes from the organism's
frozen learning configuration and bound into experimental, continuation,
generation-boundary, consumption, and child-birth identities. The sole
production consumer is `consume_next_structural_request()`: it selects the
first unconsumed sealed request, takes the attempt ordinal from that request's
fixed sealed index, constructs one `CompetenceContextGrowthGenome` internally,
and makes exactly one call. Caller-selected request IDs, seeds, and genome
objects are unavailable. Existing hypotheses, pending births, every prior
attempted tuple (including rejected tuples), and materialized children form one
without-replacement reservation set.

- one lifetime request per depth-zero parent;
- no depth-one recursive specialization;
- one genome call and at most one child per consumed request;
- no eviction, replacement, retry, or runner-supplied pattern;
- empty eligibility, duplicate pattern, or full child storage permanently
  consumes the attempt;
- dormant children count against child storage;
- LOCAL_CONTRAST and COUNTEREXAMPLE_BLIND consume the same request slot and
  make exactly one genome call; only eligibility semantics differ;
- DISCONNECTED produces no dummy request.

Focused tests consume every sealed request exactly once, including permanent
empty, duplicate, and capacity rejections; slot 192 remains permanently
consumed and request append 193 fails atomically.

## Four-region development canary

Use already-viewed or synthetic development data with disjoint receipt and
stable physical-interaction IDs:

1. parent discovery;
2. parent prospective support/contradiction and child nomination;
3. still-later child certification;
4. sealed read-only evaluation after child certification.

The evaluation region is pre-outcome on frozen child state and cannot certify,
revoke, nominate, append requests, or mutate any ledger. Compare paired
LOCAL_CONTRAST, DISCONNECTED revocation-only, and COUNTEREXAMPLE_BLIND arms.
Parent state is identical through contradiction; specialization arms have exact
request/genome-budget parity.

Report every arm, including unsafe arms: raw TP, FP, abstention; safe deployable
coverage (positive coverage only when FP is zero, otherwise zero); triggering
parent false prediction; zero post-contradiction parent influence; zero
pre-certification child influence; complete certification/revocation/request/
specialization transitions; and later child evaluation behavior.

## Focused validation and stops

Require matcher parity for MATURE, PROBATION, both DORMANT origins, PRUNED,
direct lineage, and unrelated shadows. Require exact semantic serialization at
revocation, queue append, queue seal, request consumption, child birth, and
child certification, including rejected and accepted candidate-terminal states.
After child certification, child ablation removes only child decision influence
while dormant-parent structural matching remains available.

Require exact R0 topology, weights, credit, lifecycle, action, GraphActuation,
ordered signal identities, typed terminal signals, and terminal source
identities at every generation and serialization boundary.

Stop before interpretation on any contract violation. If all focused tests and
the canary pass, preserve only this engineering closure and stop. No scientific
cohort or follow-on mechanism is authorized.

The corrected one-seed development canary passed with 119 identical anonymous
candidate terminals before the intended predicate difference. Local confirmed
37 and selected `tg26s_shared_atom_a3c023db787c08da42e7` from 32 distinct
supports; blind confirmed 117 and selected
`tg26s_shared_atom_462644543c09da5451c1` from 37. Both children certified, but
each evaluation produced 1 TP, 2 FP, and 1 abstention, hence zero safe coverage.
This is engineering evidence for memory, engagement, correction, ownership,
and isolation only—not safe competence or a local-versus-blind comparison.
