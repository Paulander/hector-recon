# Native deferred-specialization engineering correction closure

Status: **PASS — corrected development canary only; no scientific claim**

Starting point: `ce8cb6e3a00b3421b0bd2ea6be6d5f57b11d8e62`

Corrected canary artifact SHA-256:
`1324010fef3a50a0b20bb1697984bb644f0abde8bb0489487599c17fc5391211`

The prior canary remains byte-identical at SHA-256
`9e455a32c913a581d75dfeef77686fbe248db6f2ad6a0fe8a538b43136346b04`.

## Engineering corrections

V2 eligibility terminals now retain the full graph-evaluated generic rule:
role permission, recursive parent implication, exact distinct parent-supporting
REAL receipt and stable physical-interaction identities, occurrence count,
triggering-contradiction presence, mode, confirmation, and node state. Python
attaches the complete role-permitted request vocabulary without filtering on
support, implication, or contradiction. Graph confirmation requires
role-permitted, non-implied, at least `MIN_SUPPORT`, plus only the intended
local contradiction-absence predicate.

The organism now freezes its existing learning-config genome seed before
prospective outcomes and binds it into experimental, continuation, generation,
consumption, and child identities. `consume_next_structural_request()` is the
only production consumer. It owns canonical order, fixed sealed-index ordinal,
internal genome construction, one call, permanent consumption, and
without-replacement reservation across existing, pending, rejected, and
materialized tuples. No caller request ID, seed, or genome object remains.

Certification emissions independently persist cells whose committed
pre-outcome decision was contradicted, graph revocations, and graph
specialization requests. False-prediction identity is derived from the frozen
pre-outcome classification, matching certified cell, truthful REAL outcome,
and fixed polarity. It is mode-independent and survives later revocation and
serialization. Requests remain a subset of revocations; disconnected
revocation legitimately has no request.

## Focused validation

- corrected eligibility, serialization, ownership, duplicate/retry, and
  192/193 boundaries: `16 passed in 3.96s`;
- bounded adjacent V2/shadow selection: `8 passed, 24 deselected in 1989.63s`;
- corrected one-seed development canary: PASS;
- complete repository suite: deliberately not run before a future source
  freeze;
- no fresh data, scientific cohort, runner, R1, handover, or retired successor
  was opened.

The ownership tests prove the first sealed request is unavoidable, caller
selection APIs are absent, seed and ordinal survive serialization, two pending
requests cannot reserve the same tuple, rejected requests cannot retry, every
sealed request is consumed once, slot 192 is permanent, and append 193 fails
atomically.

## Corrected development canary

The local and blind arms had the same 119 anonymous terminal candidates before
the contradiction-absence predicate (`e95a7425...c794c3120`). Local confirmed
37 and selected `tg26s_shared_atom_a3c023db787c08da42e7`, which was not implied
and had 32 distinct supporting receipts/interactions. Blind confirmed 117 and
selected `tg26s_shared_atom_462644543c09da5451c1`, which was not implied and
had 37 distinct supporting receipts/interactions. Both used frozen seed
`7875574914420937836`, one canonical request, one queue slot, and one genome
call.

| arm | prequential false IDs | revocation IDs | request IDs | TP | FP | abstention | safe coverage |
| --- | --- | --- | --- | ---: | ---: | ---: | ---: |
| local contrast | parent | parent | parent | 1 | 2 | 1 | 0.0 |
| disconnected | parent | parent | none | 0 | 0 | 4 | 0.0 |
| counterexample blind | parent | parent | parent | 1 | 2 | 1 | 0.0 |

Here `parent` is the immutable `canary_mixed_shadow_parent` ID. Parent influence
after contradiction and child influence before certification were zero in all
arms. Both specialization children later certified, but both produced false
positives. This canary demonstrates memory, engagement, correction, evidence
separation, and organism-owned composition; it is explicitly not evidence of
safe competence or of local-versus-blind performance.

All four evidence regions were disjoint by receipt and stable physical
interaction. Serialization parity held after revocation/queue append, queue
seal, request consumption, child birth, and child certification. Semantic
trace parity retained GraphActuation, ordered signals, typed terminals, and
terminal sources. R0 topology, weights, credit, and lifecycle digests matched
the source in every arm.

Remaining external-requirement difference: **none known**. Recursive
specialization, negative children, wider conjunctions, active evidence seeking,
confidence weighting, request prioritization, scientific cohort construction,
R1, and handover remain explicitly postponed.
