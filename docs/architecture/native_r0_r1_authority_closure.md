# Native R0→R1 causal-authority closure

Status: engineering/development package only. All positions are previously used or retired. This document does not make a fresh KRK claim.

Implementation result: authority gates passed, but the package closed negative at `actual_child_availability`; see `../autogrowth/NATIVE_R0_R1_AUTHORITY_HANDOVER_DEVELOPMENT_RESULT_20260716.md`.

## Binding organism boundary

Inference consists of a serialized learned `NativeReConKRKGraph`, its generic outcome-grounding/value state, the generic ReCoN genome/embodiment (including anonymous CHOICE), and a chess environment adapter. No curriculum runner, trainer object, mate-distance label, forced-move provider, tablebase, or host score selector is serialized or required.

The virtual-frame firewall is capability protection plus explicit deep state isolation. It is not a universal Python sandbox. Dream execution may mutate only isolated copies. It cannot grant reward, maturity, grounding, lifecycle changes, reservoir changes, topology changes, or real actuation.

## Causal-authority table

| Concern | Before this package | Experimental path after closure | Owner after closure |
|---|---|---|---|
| Observation | Python supplied board terminals | Adapter supplies board and generic terminal measurements | environment |
| Legal action enumeration | Python enumerated moves | Adapter enumerates legal actuator identities and successor transitions; it does not rank them | environment |
| Action choice | `_confirmed_action_candidates` sorted Python scores; `_choose_with_child_priority` could override | Formal graph confirms option legs; anonymous CHOICE waits, compares graph-owned activations, and emits exactly one actuator identity | learned graph + generic genome |
| Child request | Host availability/provider helpers and priority routing | Each hypothetical successor formally requests the frozen R0 graph in a deep-isolated frame | generic genome + learned graph |
| R0 child value | Host-side gate/provider path | Serialized mature, outcome-grounded R0 competence emits only its own consolidated value/confidence/provenance | learned graph |
| All-reply composition | Host loop decided whether a first move qualified | An anonymous `and` SCRIPT confirms only when every reply-local CHILD_RESPONSE confirms | generic genome |
| Spawning | Trainer scheduled/constructed action branches | The selected graph actuator causes the existing native trial/StemCell triplet substrate to materialize an observed branch; the laboratory performs the mechanical mutation | generic genome (decision) + laboratory (materialization) |
| Proposal winner | Host-side structural ranks existed | No proposal competition is opened in this package | not applicable |
| Promotion/pruning | Trainer lifecycle code | Frozen R0 maturity is serialized; R1 promotion/pruning is not run or inferred in this bounded package | learned graph for R0; not exercised for R1 |
| Credit | Trainer computed reward/child routing | Environment reports only real terminal facts/time; generic intrinsic credit consumes actual mature-child emission or observed outcome | environment + generic genome |
| Consolidation | Trainer retained state | Frozen R0 consolidation is serialized; no dream or development evaluation consolidates itself | learned graph |
| Real actuation | Python selected and pushed a move | FormalReConEngine emits one identity; adapter may only execute that identity | learned graph + environment |

## Fail-hard exclusions

The native experimental path installs tripwires around `NativeReConKRKGraph.choose`, `audit_choice`, `_r0_available`, `_r0_available_with_dispatch_cache`, and `_choose_with_child_priority`. Any call is an integrity failure, not a fallback.

Anonymous CHOICE is the single new generic primitive. It has no chess, KRK, stage, target, candidate, or mate semantics. It waits for all declared option measurements, chooses the greatest graph-owned activation with a stable identity tie-break, and exposes exactly one actuator identity. Zero or multiple emissions fail hard.

## Actual-child handover

For each R1 action leg the environment produces a deep-isolated successor for every legal Black reply. The actual frozen R0 graph is formally requested on each White-to-move frame. CHILD_RESPONSE contains only formal confirmation, serialized consolidated value, R0 maturity, observed-outcome grounding provenance, and R0-owned uncertainty. The parent all-replies SCRIPT is a generic quantifier. It never checks mate, forced mate, target identity, or correctness.

PREDICTION_RESIDUAL is diagnostic only. It is not surprise, attention, reward, maturity, or grounding, and cannot certify a dream.
