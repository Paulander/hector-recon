# Hector/ReCoN Brief

Mission: merge the Feb baseline learner, native ReCoN runtime, and TG46+ evaluation rigor into one
learner whose frozen graph beats its baseline on heldout KRK behavior with causal ablation evidence.
Canonical dieted parent: reports/autogrowth/clean_slate_krk/dieted_foundation_v1/krk_tg46c_real_mate2_repair_seed_20260702_rev1.json
Canonical sha256: ae382d0463e35eff09e9515a715648b5d49b1e0891d127a660e036e378452eb6
Adopted 2.5 gate: balanced seed 20261211 threshold=0.854756; exact mate-in-2 remains verifier.

Standing spec: move loops state polarity explicitly. Learned gates/priors are dispatch-layer only;
labels, strata, success judgments, reported ground truth use exact ungated audits; all learner-visible key builders pass the firewall at construction.
Reclassified call sites: phase2_edge_mate_v1 pool labels, 2.8e true-middle heldout construction, 2.8e policy success judge, and 2.8d-fix chase rung judge.
Provenance law from the persistent-ladder checkpoint: all future run artifacts must store row-level outcomes plus complete runner config, including black policy, seed schedule, judge version, fence-check timing, and tick/tick-equivalent budget.
Split law from stable plasticity: every rung has train chunks, a validation buffer freshly split from the train pool, and a gate heldout; consolidation may consult only validation plus previously-passed-rung replay rows, never gate rows. If a gate row influences an update, the gate is void.

Phase 2.8b-fix: `enter_mate2_skill` repaired from helpful-opponent existential reply semantics to
universal all-reply semantics. It is exact mate-in-3 restricted to edge/fence distance-1 closure;
the bright line is fixed here: no mate-4 exactification.

Phase 2.8c/2.8d were confounded by mixed strata. Re-stratification found the old waypoint heldout
was 26 mate1-now / 8 mate2-now / 2 distance-1 / 64 true-middle; universal-d1 heldout was
16 mate1-now / 24 distance-1 / 0 true-middle. Full table: reports/autogrowth/clean_slate_krk/phase2_edge_mate_v1/phase2_8e_pool_strata_summary.json.

Phase 2.8e clean true-middle heldout: 100 starts, all from existing true-middle heldout rows; no
fresh rows needed. Evaluation horizon 10, black fixed-seed uniform legal, success = ungated exact
mate2 OR actual mate, hard failures = confinement crossed / rook lost / stalemate / repetition.

True-middle conversion: dispatcher 30/100=0.300 Wilson [0.219, 0.396]; fallback 30/100=0.300
[0.219, 0.396]; best 2.8c learned seed 20270212 25/100=0.250 [0.175, 0.343]; fixed chase
31/100=0.310 [0.228, 0.406]. Chase is not interval-separated from dispatcher.

Paired on true-middle: chase vs dispatcher = 17 win/win, 14 win/loss, 13 loss/win, 56 loss/loss.
Learned vs dispatcher = 23 win/win, 2 win/loss, 7 loss/win, 68 loss/loss. Fallback matched
dispatcher outcomes on this heldout: 30 win/win, 70 loss/loss.

Chase rook-loss audit: all four known losses were black replies capturing the rook after a
chase-bound move, so they are skill bugs, not follow-on fallback behavior. Strata: three
true-middle, one mate1-now. Example FENs: `6k1/4KR2/8/8/8/8/8/8 w - - 0 1` and
`8/8/8/8/8/8/1K3R2/3k4 w - - 0 1`.
Integration no-adopt: Phase 2.8i paired 200 games with-chase 137/200 vs without 134/200 (+0.015), paired 128 win/win, 9 loss/win, 6 win/loss, 57 loss/loss; repetitions 61 vs 56; skill violations 0.
Phase 2.9a sealed firewall: leaks removed (`black_reply_mobility_after`, `is_stalemate_after`, terminal-only aliases, micro mobility/risk); sealed Stage A/B retrains 94/97/95 and 93/92/92, leaked terminal count 0. Stage B learned is ~0.72 (93/128) vs dispatcher ~0.65 (83/128); boundary finding: the deep conditional rung defeats both flat weighting and the current hand dispatcher - open frontier, not "flat learner wins."
Phase 2.9b autonomous discovery: 56/69/72 of 128 vs fallback 20, random 6, flat official 94/97/95, ceiling 77; ablation 10/10/6, leaks 0, quorum layer mixed.
Phase 2.9c discovery-operator refinement: failure-localized spawning plus heldout M4-successor promotion gives 54/70/66 of 128, all above fallback 20 but below hand ceiling 77 and clean flat replay 86/90/89; spawned quorum health 0 load-bearing, 32/32/28 inert, 16/16/20 harmful; M4 promotes 0 affordances and 0 vetoes. Next: repair promotion/spawn signal before graph-native plasticity migration.
Phase 2.9d advisory trial routing: spawned composites enter scoring with capped low initial weight before ablation. Trial layer changes behavior but not individual composite credit: trial full 51/61/59, final promoted 54/70/66, nonzero composite ablations 0/0/0, load-bearing/inert/harmful 0/48/0 for all seeds. Stop rule fired: load-bearing stayed zero, so do not advance to step 5 full plasticity migration.
Phase 2.9e pure ecological Stage B probe: first autonomous-discovery positive result. Sealed two-arm run on clean 128 heldout, 32-row train prefix for runtime, exact judge post-hoc only. Arm1 unguided internal triggers produced load-bearing counts 1/0/1 and wins 80/68/82; birth/death stayed bounded at 149/145/4, 156/152/4, 140/136/4 births/pruned/survivors, leaks 0. Hand-audited positive-ablation survivors were chess-meaningful: seed31 `rook_attacked_after=0 AND to_file_edge_distance=2` delta +1; seed33 `bk_neighbor_ne_available zero AND bk_neighbor_se_available zero` delta +1. Arm2 guided-residual control was quarantined and produced 0/0/0 load-bearing. Interpretation: weak existence proof, not a characterized phenomenon.
Phase 2.9f full-train characterization: same sealed ecological probe scaled to all 600 Stage B train rows and 5 seeds. Arm1 load-bearing 0/1/0/0/0, wins 79/69/81/87/77 versus dispatcher 81 and official flat 92; no recurring load-bearing conjunctions across seeds; Arm2 stayed 0/0/0/0/0. Populations stayed bounded at 4 survivors with heavy pruning (700-781 births, 696-777 pruned). The only full-train load-bearing survivor was seed32 `file_delta_magnitude=0 AND white_king_to_rook_after=2` delta +1. Enrichment remained weak/seed-specific. Outcome: 2.9e was a real existence proof but not robust at scale; next step is graph-native plasticity/economy to stabilize useful structure rather than claiming recurrence.
Phase 2.9g habitat-local ecology: kept the 2.9f sealed boundary and 4-live heldout-ablation budget, but changed survival from global decay/cap to habitat-local decay plus per-habitat/total nutrition pruning. Full 600 train / 128 heldout / 5 seeds: Arm1 load-bearing 1/1/0/0/0, wins 80/71/80/85/76 versus dispatcher 81 and official flat 92; Arm2 guided control stayed 0 load-bearing. Positive composites were seed31 `king_support_manhattan_distance negative AND white_king_to_black_king_distance zero` delta +1 and seed32 `king_pair_knight_distance_like positive AND rook_black_king_opposite_sides_of_white_king_on_primary_axis positive` delta +2. No recurring load-bearing conjunctions; no mature population formed; ecology churned 707-772 births down to 4 survivors via total-cap pruning. Outcome: habitat-locality gives a second-scale positive but not stable recurrence. Next: graph-native plasticity/economy with persistent local resource accounting, not more spawn targeting.
Promoted conclusion for step 5: flat-substrate autogrowth produces occasional causal composites (2.9e hand-audited confinement/safe-reposition; 2.9f/g small positive ablations) but not stable recurring topology. Bottleneck is survival/credit representation and runtime cost, not raw expressivity or spawn targeting.
Phase 3.0 graph-native ecological memory: implemented Stage B `StemCellTerminal` TRIAL/MATURE/PRUNED composites with parent-local resource accounting, parent-local routing/activation, fate logs, sibling ranks, and a bounded pruned add-back screen. Full 600-row run was attempted repeatedly but did not complete seed31 in-turn; bounded 64 train / 32 clean heldout / 5 seeds completed. Arm1 mature 0/0/0/0/0, load-bearing mature 0/0/0/0/0, wins 19/21/19/23/19, no recurring mature composites. Pruned add-back screen found helpful pruned candidates 1/1/2/2/3 from 4 audited per seed, with prune reasons dominated by local inactive decay and parent-local budget. Stop rule fired: mature population fails to form/stabilize. Next: inspect fate logs for why helpful candidates die, then adjust local credit/maturity thresholds before another full-scale attempt.
Phase 3.1 same-population carryover diagnostic: added Stage A approach warm-up -> Stage B chase using the same ecological composite/stem-cell population (schedule labels not learner-visible; not a full Mate_In_1/Mate_In_2-to-chase curriculum claim). Bounded 5-seed run, Stage A 128 train + Stage B 128 train + Stage B 64 heldout: mature 0/0/0/0/0, load-bearing 0/0/0/0/0, wins 38/36/39/46/39; no recurring mature/load-bearing composites. Stage A spawned 326-358 candidates per seed, but final survivors were all born on Stage B (22/27/29/36/31), so warm-up topology did not carry across this rung. Helpful-pruned screen again found 1/1/2/2/3, reinforcing that survival/thresholds still kill useful candidates. Existing native single-graph foundation sanity at max_ticks=80 passes Mate1 18/18 and Mate2 23/23 with same-graph second moves 23/23; max_ticks=18 fails, so foundation runtime needs adequate tick budget. Missing piece remains full curriculum migration: connect the ecological memory economy to the Mate1/Mate2 foundation graph instead of treating Stage A/B sealed flat weights as separate warm-up/chase scorers.
Phase 3.2 native-foundation ecology wiring: trained a coarse native Mate1/Mate2 graph and used its confirmed candidates as the Stage A/B base scorer with ecological composites layered on top (same native graph, same composite population; no Stage labels visible; random legal fallback only when native graph has no candidate). Bounded coverage diagnostic, 3 seeds, Stage A 16 train + Stage B 16 train + Stage B 16 heldout: native foundation still passes Mate1/Mate2, but Stage coverage is too low (Stage A train 0/16 scored, Stage B train 2/16, heldout 1/16) and native base wins 0/16. Arm1 mature 0/0/0, load-bearing 0/0/0, wins 0/0/0. Conclusion: the immediate blocker is not ecology survival but foundation-to-frontier coverage/retrieval; exact mode has zero Stage A/B coverage and coarse mode is still too sparse/slow. Next test should improve graph-native retrieval/continuation coverage before scaling ecology again.
Phase 3.2 real native graph migration: acceptance-gated implementation now spawns composites as `StemCellTerminal`-tracked TERMINAL nodes inside the same `FormalReConEngine` foundation graph, under habitat SCRIPT parents with SUB/SUR pairs. Grep/call-chain proof: `run_phase32_real_native_graph_ecology_probe` -> `_GraphNativeCompositeRuntime.evaluate_composite` -> `FormalReConEngine.request(ROOT_ID)` -> `FormalReConEngine.run(active_nodes={ROOT_ID,parent_script,composite_terminal})` -> `FormalReConEngine._evaluate_terminal` -> `_real_native_composite_predicate`; dynamic proof passed 5/5 seeds with SUB request observed, predicate eval count incremented, terminal CONFIRMED, parent TRUE, 8 ticks. Five-seed persistent foundation->Stage B run, 24 foundation rows + 24 Stage B train + 16 heldout: mature 0/0/0/0/0, wins 0/0/0/0/0, helpful-pruned 0/0/0/0/0, recurring mature composites 0, cross-rung load-bearing survivors 0. Population curve grew TRIAL-only 1/9/17/24 on foundation then 25/33/41/48 on chase; no mature population formed, so stop rule fired for every seed. Artifact: `reports/autogrowth/clean_slate_krk/phase3_2_real_native_graph_ecology/summary.json`. Conclusion: request-gated native wiring is real, but local parent-credit/maturity still does not stabilize topology; inspect fate logs/credit thresholds before another run variant.
Phase 3.3 migrated-flat native host gate: implemented first-class `stage_b_policy` SCRIPT plus weighted atom TERMINAL nodes in the native `FormalReConEngine` graph; dynamic call-chain proof passes (ROOT request -> `stage_b_policy` SUB -> atom terminal -> `_phase33_stage_b_atom_predicate`, eval counter increments). The required behavioral equivalence gate fails against the official sealed Stage B artifacts: migrated in-graph host scores 71/73/71 of 128 versus official 93/92/92 (deltas -22/-19/-21), so the prompt stop rule fired and no ecology seeds were run. Artifact: `reports/autogrowth/clean_slate_krk/phase3_3_migrated_flat_native_ecology/summary.json`. Interpretation: native request-gating and atom materialization work, but the official sealed scorer/evaluator semantics are not yet reproduced by the current executable replay; recover that host equivalence before testing survival/maturity again.
Phase 3.4 host tiebreak alignment: migrated host now uses the same deterministic `(score, uci)` tiebreak, legal-move ordering, third-repetition guard, and `_sealed_action_key_scales` as executable flat replay. Behavioral equivalence still fails against the official sealed artifacts: migrated host 71/73/71 versus official 93/92/92 (deltas -22/-19/-21), so ecology again did not run. Residual diagnostic found 0 first-move differences between the current executable flat replay and migrated host on heldout; therefore the remaining gap is not native graph routing or tiebreaking, but unrecovered official artifact/evaluator semantics. Artifact: `reports/autogrowth/clean_slate_krk/phase3_4_host_tiebreak_alignment/summary.json`.
Phase 3.5 equivalence forensics: official 93/92/92 is a full 16-white-move rollout artifact (`heldout_eval.success_count`, success endpoint `ungated_exact_mate3_or_better_confirmed`), not a single argmax label. The current migrated gate uses `_evaluate_policy`/`_rollout_policy` with the same ungated Stage B enter-mate2 success kind, but current executable replay and migrated host both score 71/73/71 and have 0 full-trace differences against each other. Stored artifact sample traces diverge downstream: seed11 row640 artifact stops `fence_broken` after 2 white moves while current/migrated continues to 8; seed12 row627 and seed13 row610 have different black-reply successor FENs before the next white choice. Conclusion: host equivalence is blocked by unrecovered official continuation/black-reply provenance, not host scoring or ecology. Artifact: `reports/autogrowth/clean_slate_krk/phase3_5_equivalence_forensics/summary.json`.
Phase 3.6 yardstick sovereignty repair: repository/git-history search did not recover a callable producer for historical Stage B 93/92/92, and the saved `phase2_9a_stage_b_sealed_seed.v0` artifacts lack full row traces, row-level success vectors, trace digests, black-reply policy, evaluator contract, and exact runner entrypoint. Therefore 93/92/92 is classified as `non_replayable_count_only_yardstick` and is background evidence only, not an ecology gate. New canonical executable host baseline is current flat replay with full trace manifest: 71/73/71 on 128 heldout; migrated native host is full-trace identical to it with 0 trace differences for all three flat seeds. Artifact: `reports/autogrowth/clean_slate_krk/phase3_6_yardstick_sovereignty/summary.json`. Next: run native ecology against this executable 71/73/71 host baseline; report host+matured vs host-alone, mature stability, helpful-pruned, and recurrence.
Phase 3.7 recent-curriculum black-resistance audit: uses only branch-local recent curriculum artifacts (`phase2_9_overnight` Stage A approach and Stage B true-middle) plus sealed Stage A/B weights; no old `krk_curriculum.py` ladder import, no learner-visible stage labels, no runtime tablebase/DTM move source, and no White oracle move provider. Exact-adversarial black is an opponent/evaluator policy only: choose the legal black reply that minimizes the same post-hoc success predicate, preferring fence break/rook loss/stalemate. Canonical fixed black gives Stage A 81/79/82 and Stage B 71/73/71; exact-adversarial black drops to Stage A 57/59/58 and Stage B 45/43/42, almost entirely extra `fence_broken`. Artifact: `reports/autogrowth/clean_slate_krk/phase3_7_recent_curriculum_black_resistance/summary.json`. Conclusion: before ecology, train one persistent native graph through recent Stage A -> Stage B under exact-adversarial black with regression gates; random-black competence is too soft.
Phase 3.7 persistent staged ladder (artifact path `phase3_8_persistent_staged_ladder` to avoid the prior 3.7 path collision): provenance law implemented in design/summary/seed artifacts with row outcomes and runner configs. Re-baseline correction: 2.9e Arm1 survivor populations replayed under the same exact-adversarial runner score 48/43/44 versus executable flat 45/43/42 (+3/0/+2), so 2.9e remains a causal-composite existence proof but not an 80/68/82 robustness claim under the harder opponent. One persistent native graph per seed: foundation gate passes, but Stage A plasticity fails the adversarial regression gate at 52/51/50 versus executable flat 57/59/58; Stage B and ecology were not run. Failure taxonomy is extra `fence_broken` plus small `rook_lost` drift. Next: repair Stage A plasticity/regression protection before any ecology.
Phase 3.8 stable plasticity (artifact path `phase3_9_stable_plasticity`): persistent-ladder/consolidation runner split into `persistent_staged_ladder.py`; M3/M4 restored as fast chunk updates plus behavioral consolidation into slow weights only when validation and prior replay do not regress. Split law recorded; gate heldout rows are not consulted by update decisions. Three-seed result: foundation passes all; chunks consolidate Stage A 5/1/3 of 6, but gates are 50/60/55 versus flat 57/59/58, so only seed32 reaches Stage B. Seed32 then consolidates 2/6 Stage B chunks and passes 49 versus flat 43 with Stage A still 60 versus 59. Standing ladder is false because seeds31/33 fail Stage A. Endpoint deltas: accepted chunks on failed Stage A seeds are validation-neutral, while heldout regresses via extra `fence_broken`/`rook_lost`; accepted Stage B chunks in seed32 reduce validation `fence_broken` by 3 and add +2 wins. Conclusion: consolidation can work, but a single small validation buffer is not yet a reliable acceptance oracle; next repair is stronger replay/acceptance coverage, not more ungated training.
Phase 3.9 stratified acceptance repair (artifact path `phase3_10_stratified_acceptance`): only acceptance coverage changed. Validation rows are stratified from the train pool by initial exact-adversarial endpoint family, and consolidation now requires both wins non-regression and hard-endpoint non-regression (`fence_broken`, `rook_lost`, `horizon`, repetition, stalemate, illegal). Three-seed result: Stage A gates improve to 51/63/56 versus flat 57/59/58; seed32 remains a full ladder pass with Stage B 49 versus flat 43 and Stage A regression still 63 versus 59, but seeds31/33 still fail Stage A. Accepted Stage A validation deltas are now endpoint-clean or fence-improving, yet heldout still misses by 6/2 wins. Conclusion: endpoint-family validation is better but still too coarse; the remaining acceptance gap is geometry/position-cluster coverage, not lack of plasticity or endpoint accounting.
Phase 3.9 credit precision + honest paired gates (artifact path `phase3_11_credit_precision_paired_gates`): recognizer-localized negative credit targets the fence/rook flip ply plus two discounted prior plies; features and contrast rule unchanged, chunk budget 12, validation folds resampled fresh per chunk. Flip-ply identification was 100% on hard-fail episodes: Stage A 173/176/171 of 173/176/171, Stage B 202/198/196 of 202/198/196. Gate rule is row-paired: pass if discordant pairs favor learner or learner is non-inferior within the predeclared 3/128 margin. Standing ladder is now true for all seeds under exact-adversarial black: seed31 Stage A 55 vs flat 57 (paired non-inferior), Stage B 48 vs 45; seed32 63 vs 59 and 44 vs 43; seed33 60 vs 58 and 45 vs 42. Recomputed `253c8a7` gates under paired stats: seed31 was a real Stage A regression (-6, outside margin), seed33 was noise-level parity (-2, inside margin). Next: run ecology on this standing 3-seed persistent ladder; no new topology claim until mature/load-bearing composites survive there.
Phase 3.12 standing-ladder ecology (artifact path `phase3_12_standing_ladder_ecology`): stem-cell composites ran through the 3.9 persistent ladder with request-gated native evaluation, critical-period optimism/credit, positive achievement-flip nutrition, activation-conditioned decay, and TRIAL grace. Stop rule fired on seed31 after Stage A: live population exploded from 21 after foundation to 302 after Stage A (301 TRIAL, 1 MATURE, 57 PRUNED), so Stage B, load-bearing ablation, cross-rung continuity, and recurrence were not tested. The mature Stage A cell was `white_king_to_rook_after=2 AND king_support_manhattan_distance=zero`; host/mature Stage A stayed 55 vs flat 57 and all-live 56 vs 57, so this was not gate regression. Helpful-pruned=0; acceptance margins had teeth (Stage A accepted chunk deltas all 0, rejected deltas all -1). Conclusion: fair conditions finally expose the next bottleneck as birth/death throttling, not host competence or cell expressivity.
Phase 3.13 discriminative cell economy (artifact path `phase3_13_discriminative_cell_economy`): froze ladder/gates/ratchet/spawn triggers and changed only cell attribution. Per ply, host-alone argmax is compared with host+advisory choice; same-choice plies give zero cell credit/debt, changed-choice responsible cells get later flip nutrition/debt, maturity requires 3 choice-changing positive events, dormancy rent is small nonzero, and per-parent habitat capacity blocks births. Stop rule fired on seed31 after Stage A: choice-changing signal was real, not near-zero (foundation 5/21=23.8%, Stage A 9/345=2.61%; Stage A changed outcomes 2 achievement flips / 2 failure flips / 5 neutral), but population still exploded/failed stability from 21 TRIAL after foundation to 302 TRIAL after Stage A, with 0 MATURE, 1 PRUNED, and 69 capacity-blocked births. No gate regression; Stage B, ablation, continuity, and recurrence were not run. Conclusion: discriminative attribution creates an honest signal, but admission/birth control still leaks across too many habitats; next repair should require resource-funded or exposure-proven habitat births before more credit tuning.
Phase 3.14 audition economy (artifact path `phase3_14_audition_cell_economy`): froze the 3.13 ladder/spawn/capacity boundary and added training-side imagination only. TRIAL cells whose own active-proposal argmax disagreed with host-alone got paired virtual rollouts (K=12 per cell, cap=2 per ply, H=8) against exact-adversarial black; verdicts came from internal recognizer flips, not exact judges. Stop rule fired on seed31 after Stage A: verdict signal was strong enough to mature cells but too sparse over the population. Foundation auditions/cell mean 0.67, verdicts 0 better / 8 worse / 6 tie; Stage A mean 0.38, verdicts 22 better / 55 worse / 26 tie, 402 audition frames, 4 cap-skipped, 0 budget-skipped. Population still failed stability at 292 TRIAL / 2 MATURE / 13 PRUNED after Stage A (alive 294 > band 160), with 63 capacity-blocked births and no gate regression; Stage B/ablation/continuity did not run. Mature Stage A cells: `bk_neighbor_s_available=zero AND bk_neighbor_w_available=zero` and `rook_attacked_after=0 AND to_rank_edge_distance=2`. Conclusion: auditions can create mature topology, but audition coverage is starved because births still spread across too many low-evidence habitats.
Cross-experiment recurrence banked in the 3.15 artifact: two chess concepts recur across substrate/economy/opponent changes. Safe rook reposition: 2.9e `rook_attacked_after=0 AND to_file_edge_distance=2` corresponds to 3.14/3.15 `rook_attacked_after=0 AND to_rank_edge_distance=2`. Confinement: 2.9e `bk_neighbor_ne_available=zero AND bk_neighbor_se_available=zero` corresponds to 3.14/3.15 `bk_neighbor_s_available=zero AND bk_neighbor_w_available=zero`.
Phase 3.15 scheduled auditions (artifact path `phase3_15_scheduled_audition_economy`): froze 3.14 audition mechanics and added a replay buffer so each TRIAL cell samples its own firing positions toward K=10; all-sampled agreement prunes as `scheduled_redundancy`, distinct from debt pruning. Stop rule fired on seed31 after Stage A: scheduled verdicts visibly drained the population but not enough to pass. Stage A verdict mix: 35 better / 92 worse / 54 tie / 136 redundancy-prune, 709 audition frames, 18 capacity-blocked births. Population ended 170 TRIAL / 10 MATURE / 165 PRUNED; mature cells include recurring concepts `rook_attacked_after=0 AND to_rank_edge_distance=2` and `bk_neighbor_s_available=zero AND bk_neighbor_w_available=zero`. Drain improved versus 3.14 (PRUNED 13 -> 165, MATURE 2 -> 10), but 158/170 surviving TRIAL cells remained under K=10 (92.9% > 25% stop) and alive 180 still exceeded the stability band 160. No gate regression; Stage B/ablation/cross-rung continuity did not run.
Phase 3.16 homeostatic audition economy (artifact path `phase3_16_homeostatic_audition_economy`): froze 3.15 mechanics and added complete end-stage flush plus birth gating when the under-K TRIAL backlog is >=0.30. 3.15 forensics: under-K was not the live per-ply cap (0 cap skips); it was replay-buffer starvation (`agreement_only_underfilled` 1597, `without_firing_samples` 331), and 3.15 did not store birth timing. Five-seed 3.16 run fixed the backlog completely: pre-flush under-K fraction 1.0 in each seed -> post-flush 0.0, scheduled_unjudged stop false. But the gate overcorrected: all seeds stopped after Stage A with population collapse, 0 TRIAL / 0 MATURE / 5-6 PRUNED, no Stage B, no ablation, no cross-rung survivors, no recurring mature families. Births deferred by backlog were 332/375/343/345/387 (1782 total); verdict mix 13 redundancy-prune / 10 tie / 3 cell-worse; only 5 final-flush paired auditions cost 40 frames, while active scans covered 5522 white positions and added only 2 new samples. Conclusion: court backlog is solved, but admission is now too conservative and K/redundancy pruning kills lone cells before maturity. Next repair should require evidence escrow/firing-buffer readiness before birth while allowing multiple concurrent cells per habitat once backlog is serviceable, not a hard global backlog gate.
Phase 3.17 supply-side audition economy (artifact path `phase3_17_supply_side_audition_economy`): replaced the 3.16 global backlog gate with pool-scan audition supply and count homeostasis (TRIAL band [20,60], court throughput 60). Five seeds ran through Stage B with no stops, no gate regression, and no count-homeostasis birth deferrals. Supply was repaired: 3902 TRIAL cells scanned, 3860 fired somewhere, only 42 VACUOUS prunes, and 36693 pool samples; verdict mix was 792 cell-better / 1678 cell-worse / 762 tie / 2739 redundancy-prune / 513 budget-offered-prune / 42 vacuous. Populations stabilized at final T/M/P 5/28/751, 8/24/791, 11/27/727, 3/31/738, 9/44/759 with max alive 57/58/61/56/74. Mature recurrence appeared: five families recurred in >=3/5 seeds, including `FAMILY-SAFEROOK` in 4/5; `FAMILY-CONFINE` reached 2/5. But heldout ablation is a hard negative: load-bearing/inert/harmful mature was 0/0/28, 0/0/24, 0/0/27, 0/0/31, 0/0/44; helpful-pruned=0; cross-rung load-bearing survivors=0; host+mature vs host was -4/+1/+1/0/0. Conclusion: supply and homeostasis are no longer the blocker; short audition better-verdicts are false positives for heldout causal utility. Next repair should add counterfactual probation before MATURE routing: audition success nominates, but parent/rung validation must prove non-harm or positive paired effect before a cell becomes routed mature.
No-go: new TG names, new report documents, training logic changes, `docs/autogrowth/ACTIVE_BRIEF.md`, `reports/autogrowth/pools/`, and `archive/`.
