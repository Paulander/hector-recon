# Native prospective evidence V2 scientific discriminator — preregistration

Date: 2026-07-24

## Status and immutable boundary

This document is frozen before canonical learner execution. The experiment is a synthetic mechanism test conditional on one frozen chess-backed ecology. It is not evidence about KRK generalization, R1, or arbitrary environments.

The protected V2.1 engine is byte-identical to `cd0c739f69558030c3bc81daf292af068c6f54d6`. Required identities are:

- package manifest `c0116b15982511d446dee7a926c4d31e3066e59e3cc3faf9bb161b4c24b50a58`;
- authority source `25945864fd998caf22ae12cbcb9bcb4779447337c0079f705640c63d2356f029`;
- registry source `f3aee5cccf761af1cb6a5de94b886d5e758c0a07cb0f6d77b8898f662ca73b58`.

Any protected-source mismatch stops as `engineering_boundary_requires_reclosure`.

## Scientific question

Does receipt-disjoint, post-frontier prospective certification reduce prequential false authority from prefix-perfect adaptively discovered patterns while preserving authority for a recurring competence pattern?

## Frozen ecology and authority

Route A uses legal chess predecessor/action/successor transitions, exactly one graph-emitted action, and truthful successor checkmate state. Classifier-visible external terminals are opaque hashes. Only the laboratory manifest knows their planted/nuisance roles. The learner receives graph traces and signed grounded receipts; it receives no mate predicate, correctness Boolean, planted/spurious label, host-selected conjunction, or enumerated candidate.

The prefix has 32 positive rows on both predeclared target pairs and 32 negative rows that make each target singleton impure. The suffix has eight spurious-family rows truthfully false in A/B and true in C, followed by eight planted-family rows truthfully true in A/B and false in C. C changes the physical latent transition, never a label or receipt. Marginals are preserved. Prefix, suffix, opaque identities, transition pairs, and C permutation are frozen before canonical discovery.

## Seeds and discovery

Exactly 32 SHA-256 commit-derived genome seeds are retained. The seed is the inferential unit. No replacement, rerun, selection, or qualified-only inference is allowed.

Each prefix uses the actual graph request/error terminal, frozen content-blind genome, StemCell lifecycle, and nomination escrow with authority disconnected. No host conjunction enumeration or insertion is permitted. The exact planted pair digest and finite spurious-family pair digests are frozen in the ecology manifest.

After prefix close, a target is eligible only if it has the exact predeclared member digest, fixed positive/AVAILABLE polarity, MATURE structure, at least four prefix physical supports, and zero contradictions. Spurious selection uses the lexicographically smallest SHA-256 canonical hypothesis manifest, then cell ID. Missing targets remain `ABSENT` and the seed remains in all analyses.

Prefix serialized organisms and complete target/candidate manifests must be committed and pushed before suffix exposure. They are restored, never regenerated.

## Arms

For each seed, the exact frozen candidate population is cloned into:

- A: prospective, post-frontier certification;
- B: lawful legacy same-ledger authority;
- C: prospective certification under the precommitted truthful latent permutation.

Pattern, digest, polarity, lineage, escrow, frontiers, structural state, topology, executed topology, and source state must match. A/C begin with zero prospective authority. B authority equals historical same-ledger maturity. Nomination, specialization, births, host authority, and topology changes are forbidden after cloning; native grounded contradiction/revocation remains enabled.

## Pre-outcome parity and exposure admission

For every seed/row, A/B/C must exactly match on ordered and typed classifier-visible signals, selected opaque inputs, actuator and option identities, actuation, graph matching commitment, complete candidate activation map, target activation indicators, and logical opportunity identity. C may differ only in its physical predecessor/successor/outcome association. Any mismatch stops as `control_exposure_parity_failure`.

Each arm is separately frozen in a V2.1 laboratory registry with the complete outer-manifest hash. Registry-owned adjudication must receive exactly one wrapper for all 32 organisms. The outer target adjudicator requires both exact targets and at least four distinct physical post-frontier activations for each target in every arm. At least 24/32 seeds must qualify. Otherwise stop before outcomes as `targeted_prospective_evidence_starvation`.

## Event law and endpoints

Every suffix event is:

`open REAL event → graph pre-outcome authority/commitment → graph action → real legal transition → truthful signed receipt → consume → precommitted-cell maturity/revocation`.

Primary values are recorded before receipt consumption.

For each seed/arm:

- `false_deployment_authority`: count of rows where the production graph classification is formally deployable AVAILABLE and the subsequently observed truthful result is false;
- `planted_authority_coverage`: planted-family activation rows where the exact frozen planted cell is in the graph-consumed AVAILABLE emission, without conditioning on the current outcome.

Selected-spurious-attributable false authority is secondary. Final classifications and pooled event totals are descriptive.

## Engagement

Require A/C zero immediate prospective authority; B exactly lawful legacy authority; A planted target at least four later supports, zero contradictions, and graph certification in at least 24/32; A selected spurious target at least one grounded contradiction in at least 24/32; lawful receipt-grounded revocation; and the frozen C association break. If targeted exposure passes but planted or spurious engagement is below 24/32, close as `mechanism_contrast_starvation` and do not causally interpret superiority tests.

## Primary inference

Across all 32 seeds:

- `D_safe = false_deployment_authority[B] - false_deployment_authority[A]`;
- `D_signal = planted_authority_coverage[A] - planted_authority_coverage[C]`.

Positive favors A. Publish all values, wins/losses/ties/effective n, one-sided exact paired sign tests, and Holm adjustment of exactly these two p-values. Each contrast requires adjusted `p <= 0.05` and positive difference in at least 17/32 fixed seeds. Both must pass. All-32 paired mean/median and deterministic 20,000-replicate bootstrap intervals are descriptive only.

If engagement and integrity pass but either primary contrast fails, preserve `valid_negative_prospective_separation_not_supported`. If both pass, the bounded claim is `prospective_evidence_separation_supported_in_frozen_ecology`.

## Runtime, integrity, and stopping

The outer manifest binds every tracked runtime file under `src/` and `libs/recon-lite/src/`, all experiment code/tests/docs, config/dependency files, Python/package/import paths, deterministic environment, process policy, seeds, ecology, transition pairing, permutation, and retired toy parity. It is reverified before every phase.

The two-process policy is permitted only after permanently retired (maximum four seed) serial/two-process byte parity. The pre-freeze two-seed canary failed byte parity for both seeds, so the preregistered fallback is now binding: canonical discovery and suffix execution are serial in ordinal order, with A→B→C sequential inside each seed. No attempt will relax equality or repair parallelism in-package. No threads, GPU, shared RNG, xdist, assertion disabling, or mutable worker state.

Run new focused/adversarial tests, a critical V2.1 subset, and the exact adjacent 54-test suite once. Carry the 1,013-test V2.1 certificate only while all protected hashes remain exact.

Do not access KRK historical regression, fresh KRK, retired-65, R1, validation/regression, or held-out pools. Do not repair, tune, rerun, replace, transfer, or open another pool in this package.
