# V2 fresh discriminator — source-freeze engineering result

Date: 2026-07-26

## Scope

This package implements and tests the replacement chess-backed synthetic V2
runner, then stops before canonical fresh discovery or any canonical fresh
grounded receipt. The protected V2.1 authority organism, laboratory registry,
and atomic snapshot harness remain byte-identical. The retired V2 experiment,
integrity abort, and atomic reclosure artifacts also remain unchanged.

The scientific question, mechanisms, arms, logical ecology, doses, targets,
admission gate, estimands, tests, and verdicts are unchanged. The only future
scientific change is the preregistered new physical ecology and new experiment,
seed, terminal, and artifact namespace.

## Implemented authority boundary

The new experiment module provides:

- deterministic legal mate/nonmate pair enumeration and physical denylisting;
- an import-stable opaque-terminal chess graph with exactly one graph-owned
  actuator;
- prefix-only native nomination and fixed-polarity target selection;
- exact A/B/C construction using `V2Mode` enum values and explicit complete
  identity contracts;
- persist-once 96-snapshot integration and exact global-preflight binding;
- registry-owned outcome-blind exposure and all-32 admission;
- a mint-only truthful outcome capability with durable access records;
- an actual `NativeProspectiveAuthorityV2` tri-arm adapter;
- an experiment-local scientific carrier that binds complete row records, seed
  results, final snapshots, and post-event semantic identities into COMMITTED;
- all-32 inference using the original `D_safe` and `D_signal`, exact sign tests,
  Holm family two, engagement, and original stop/verdict branches; and
- separate future commands for discovery, snapshot persistence, exposure, and
  science. The design-freeze command invokes none of them.

Post-event carrier identity deliberately differs from pre-outcome snapshot
identity: the protected codec's lawful-initial-authority validator is used only
for frozen pre-outcome snapshots. Dynamic certification/revocation is recorded
by the full continuation digest, immutable suffix-topology digest, complete
scientific rows, and final serialized snapshots.

## Permanently retired actual-V2 canary

The canary uses a retired physical ecology and the actual native V2 wrapper,
not the generic integer adapter. Its canonical artifacts are:

- `reports/autogrowth/native_authority/v2_fresh_discriminator/retired_actual_v2_canary.json`
  SHA-256 `85d9a62cecd0b1c3e8c3a9361037e564397e74fb579afb587d37312e4cf78aa2`;
- `reports/autogrowth/native_authority/v2_fresh_discriminator/retired_actual_v2_canary_ecology.json`
  SHA-256 `6e5df4c53ee706a49381ceb730edef969c4eb37331cf7467670eb3fe245b855e`.

The canary passed:

- exact 96-artifact global preflight with zero preflight outcome access;
- transport corruption at the first and last snapshot;
- mode, source, candidate, and topology swap rejection;
- wrong and minimally fabricated exact-preflight receipt rejection;
- row-parity rejection before any outcome capability exists;
- direct truthful-environment access rejection without the mint capability;
- exposure immutability and fresh-process import-stable graph restoration;
- all A/B/C open, mint, consume, validation, and arm-commit faults;
- durable tri-arm row-commit and genuine `durable_commit:COMMITTED` faults;
- byte-identical live state and nonresumable dangling PREPARED for every fault;
- one successful actual-V2 row with exactly three guarded accesses;
- COMMITTED-next-seed restart; and
- byte-identical journal-only reconstruction without replay.

The machine canary digest is
`4b2b124ecd375b24c4af3c25e30a2088fe61ca636ec9270979a0015b720ce967`.

## Validation and stop

The focused module passed 10/10 tests. Python compilation and `git diff --check`
passed. The optional local ruff executable could not be spawned because of an
environment permission error; it was not used as evidence. The inherited
1,013-test, 42-focused, five-critical, and 54-adjacent certificates remain
applicable because every source they bind is unchanged. No redundant inherited
suite was rerun.

No canonical fresh discovery request, fresh grounded receipt, canonical A/B/C
snapshot, suffix outcome, KRK pool, retired-65 row, R1 row, or held-out row was
opened.

## Frozen predata package

The exact source freeze was pushed as
`8c268b2411240a16295a5e70bd8063e39e56e6e0`. The design-freeze command then
derived all 32 unique genome seeds from that commit and wrote the deterministic
physical/ecology/environment/source manifests without invoking discovery,
snapshot persistence, exposure, or science.

The outer manifest records:

- transport SHA-256
  `e922fbea08e6f0f0cfb456c19609fa149e5a61c0e4c840eb450af9d78d05556d`;
- canonical manifest digest
  `70612bfe5bc558b3f02d1962c5850fc8d265f5b58d393afc7052a8d3e2ace765`;
- source-runtime digest
  `1dae9caa164e7006f80001ce6df397210033383319fd7591d1a2850c862c3ee4`;
- 160 distinct predecessor positions, 160 distinct transition digests, and 160
  distinct V2.1 physical fingerprints;
- zero predecessor, transition, or physical-fingerprint overlap with the
  denylist;
- `fresh_discovery_prefix_executed: false`; and
- `fresh_grounded_receipts_minted: 0`.

The individual design-file transport hashes are:

- seed manifest:
  `6b472b10ea1840dcb5d055b2e522119ae781a4b07d00868d95e0fbacb52a191a`;
- physical denylist:
  `230fd0bf09f68009f26e244d225fafc6e967a9a8e22f947647f9ee3e75f5dbc1`;
- ecology manifest:
  `3ac38e4d88483b25db5835da2901c2f604d0800c0435054c7492ad8a3024ff74`;
- C-permutation manifest:
  `c58be58650b1785c40ddb04edee0070da97f0ea487e2daeea4931c9061f91bf9`;
- environment manifest:
  `320fb750c0bdaeee214dc5ac5b43bf16af1b944c7a357dc7b6ea71f4ae993ea3`;
- source-runtime manifest:
  `3808a54aaf0dcc521fdda373cc4b4495805a83cfb003e2afcd196f5a1a6686d9`.

All predeclared discovery, snapshot, exposure, journal, carrier, and canonical
result paths remain absent. The package is stopped here for external
implementation audit.
