# Deferred shadow specialization: first fresh discriminator

Status: **PREREGISTERED — FROZEN NOT EXECUTED**

Starting source: `26242968cca9327f6239a4bb22b7d80866c2297d` on
`codex/native-v2-systemd-schema-reclosure-20260802`.

## Question and exclusions

The single question is whether contradiction-aware local specialization yields
safer useful competence than (a) revocation without specialization and (b)
counterexample-blind specialization. This is a deferred-specialization
selectivity test. It is not a Mate-in-2, R1 handover, 29/32 mastery, or full-KRK
test. No mechanism, rescue, threshold change, replacement seed, stream top-up,
or child-specific row selection is permitted after this freeze.

The cohort contains 32 new, pre-outcome genome seeds. Every seed has three
paired arms: `local_contrast`, `disconnected`, and `counterexample_blind`.
They are cloned from one genome-nominated mixed-outcome shadow-parent state.
They receive the same rows, frames, R0 actions, semantic traces, physical
interactions, and truthful environment transitions through the parent's
contradiction. Local and blind bind the same organism-owned specialization
seed, request ordinal, request slot, structural frontier, queue/storage laws,
candidate-terminal population, and exactly one genome call. Their sole intended
difference is the existing local absence-from-triggering-contradiction
eligibility predicate. Disconnected uses the same revoked parent and emits no
specialization request.

## Frozen regions and data firewall

The manifest freezes four ordered, physically disjoint regions:

1. parent discovery (64 rows);
2. parent prospective support and contradiction (64-row fixed maximum);
3. child prospective certification (256 rows);
4. sealed read-only evaluation (256 rows).

All FENs, planned physical IDs, row IDs, and complete canonical D4 orbit keys
are globally disjoint across the four new regions. Evaluation D4 orbits are
additionally excluded from the exact frozen historical terminal-trace
regression recorded in the source manifest. That regression is the already
viewed reference pool used by the immediate engineering lineage. The exclusion
is practical, not an
exhaustive claim over multi-gigabyte binary results. Receipt
IDs and realized stable physical-interaction IDs must also be disjoint across
regions at execution.

The child-certification and evaluation regions form one shared post-parent
stream frozen before any child exists. It is independent of arm, selected child
identity, activation, outcome, or evidence shortage. Every arm receives every
row in the same order. Arm-specific rows, activation filtering, top-ups,
physical repeats, replacement seeds, and post-hoc extension are forbidden.
The learner receives no mate label, correct move, tablebase value, or
child-specific selection signal.

## Parent, request, birth, and exposure admission

For each seed, ordinary graph-native growth on the discovery region must create
at least one actual `mixed_outcomes` shadow. The canonical earliest such parent
is fixed without consulting later rows. The parent must earn prospective
certification from later distinct REAL evidence, make a committed pre-outcome
false prediction, and be revoked by the existing graph path. The same outcome
may graph-emit the local/blind request, but no child may be proposed, matched,
created, or evaluated in that open outcome event.

In the next structural generation, the organism-owned consumer takes sealed
slot zero, constructs its frozen genome internally, and makes one call. Each
specialization child receives a new immutable escrow, fixed parent polarity,
complete birth exclusion, and equal birth/certification frontier. Parent
discovery, certification, the triggering contradiction, birth reads, and
VIRTUAL frames cannot certify it. The child begins dormant and can earn
authority only from still-later distinct REAL outcomes.

Before any post-birth outcome is read, a mutation-free scan of the fixed
certification region records distinct physical activation opportunities for
both children. Admission requires both children to have at least four in at
least 24 of 32 paired seeds. All 32 remain in every report and paired analysis;
the gate never selects a 24-seed subset. Failure stops as
`paired_child_evidence_starvation`, without replacement, top-up, repetition,
threshold change, or child-specific extension.

## Sequential evidence and irrevocable error accounting

For each certification row, all arms classify first. The program commits the
action, matching cells, and decision state; executes the real environment
transition; consumes the truthful grounded result; then records maturity,
revocation, request, and influence transitions. Persist separately:

- the triggering parent false prediction;
- every child prequential false prediction before revocation;
- child graph revocations;
- post-revocation influence;
- child support/certification transitions.

A later revocation never erases an earlier false prediction. Discovery, birth,
or VIRTUAL evidence certifying a child is an `instrument_stop`. Later child
contradictions use the unchanged graph-produced revocation path. Revoked child
influence must be zero on every subsequent row.

## Sealed evaluation and outcomes

After certification closes, every organism is serialized/restored exactly and
then sealed. All arms receive the identical evaluation stream read-only.
Evaluation may not add evidence, certify/revoke, append requests, create
structure, or alter weights, credit, lifecycle, R0 action, or semantic trace.

For every seed and arm report AVAILABLE-decision raw true positives, raw false
positives, abstentions, and safe positive coverage. Safe positive coverage is
the raw true-positive count when raw false positives equal zero, otherwise
zero. Unsafe organisms are never omitted or suppressed.

## Inference and conclusions

The seed is the inferential unit. Two preregistered one-sided exact paired sign
tests compare local safe positive coverage with disconnected and with
counterexample-blind. Persist all 32 treatment/control values and differences,
wins, losses, ties, effective non-tied sample size, exact unadjusted
probability, and Holm-adjusted probability across the two-test family. Pooled
position decisions are descriptive only. Alpha is 0.05 after Holm correction.

`deferred_local_specialization_supported` requires both adjusted tests to pass
and every engagement/validity gate to pass. If engagement and validity pass but
either comparison fails, conclude `deferred_specialization_not_superior`. A
failed 24/32 exposure gate concludes `paired_child_evidence_starvation`. Any
identity, row-order, evidence/frontier, serialization, graph-state, semantic
trace, R0, budget, or mutation violation concludes `instrument_stop`.

Validity additionally requires exact parent contradiction/revocation,
local/blind graph requests, candidate population parity before the intended
predicate, matched request/genome/capacity budgets, successor-generation-only
birth, exact escrows/frontiers, zero discovery/birth/VIRTUAL certification,
irrevocable prequential errors, zero post-revocation influence, exact
serialization, mutation-free evaluation, and exact R0 topology, weights,
credit, lifecycle, action, and semantic traces. No in-package rescue is
permitted.

## Frozen stopping point

This package freezes only the preregistration, program, 32 seeds, complete row
stream, source/dependency hashes, data-free tests, validation record, and an
unexecuted result placeholder. Preparation must stop before parent discovery
outcomes, parent prospective outcomes, child exposure scanning, child
certification outcomes, sealed evaluation, or any scientific result. Execution
requires separate review and one explicit invocation of the frozen command.
