# Active Brief: KRK Autogrowth v0

Status: learning-core reset branch.

The previous branch mode over-optimized for not approving bad mechanisms. This branch should optimize for allowing a minimal learner to act in sandbox, receive credit, promote or delete candidate topology, and prove whether the loop is alive.

## Current Direction

Build and evaluate one causal trace-derived topology-growth loop. Triplets are the current narrow proof pattern, not the full candidate space:

```text
rollout -> trace -> triplet candidate -> sandbox activation -> credit ->
M3 update -> promotion/deletion -> M4 consolidation -> held-out evaluation
```

The experiment is not "finish Stage7" or "prepare Stage8". It is:

> Can ReCoN grow one useful topology addition from traces that improves held-out KRK conversion without learner-visible stage labels?

## What To Keep Active

- Core ReCoN graph/request-confirmation engine.
- Triplet representation.
- Stand-alone or shared-performance sensor TERMINAL candidates, when they are equivalent to per-script terminal instantiations in saved/real ReCoN topology.
- Primitive local sensor-composition candidates: AND, OR, XOR, and LAG/temporal terminals that compare current sensor values against prior ticks to expose change, low-pass behavior, and derivative-like signals.
- Baseline-to-ReCoN compiler.
- KRK evaluation harness, refactored toward competence metrics.
- M3 fast plasticity and M4 consolidation as active experimental variables.
- A small promotion-boundary safety gate.

## What Is Historical Only

Archived under `archive/pre_autogrowth_2026_06_10/`:

- Old report/control-plane packets.
- Stage7/Stage8 completion/quarantine artifacts.
- Selector behavior/review artifacts.
- Previous external audit pack.
- Report-gate tests.

Do not rebuild those as the mainline workflow.

## Current Topological-Growth Checkpoint State

The old `M` labels in this file are subcheckpoints inside one larger milestone: **TG: topological growth works**. They should not be read as separate research milestones.

- M0-M3 are implemented as the active baseline: cleaned branch guidance, feature firewall, locked KRK generation, and baseline/sham evaluation.
- M4 is implemented as non-behavior-changing evidence preparation: train traces plus mechanical triplet candidate mining under `reports/autogrowth/`.
- M5/M6 now run one mined candidate in sandbox-only ReCoN topology, record M3 fast-credit updates, and automatically quarantine on failure.
- Current selected candidate activates on heldout but is rejected: 0/200 mates, 18 rook-loss regressions at h40/h80, M3 updates nonzero, M4 consolidation zero.
- M7 full three-arm artifact is generated: baseline, sham-growth, and autogrowth sandbox are compared in `reports/autogrowth/krk_autogrowth_v0_experiment.json`.
- Current v0 result is a useful fail, not a promotion: candidate is quarantined after 0/200 h40/h80 mates, 18 blunder regressions, M3 updates nonzero, and M4 consolidation zero.
- M8 multi-candidate lifecycle training is implemented: multiple mined candidates can gain experience, receive M3 fast credit, mature, or be quarantined.
- Current M8 result is also a useful fail: 8/8 default candidates and 12/12 broader candidates quarantine under negative credit/rook-loss evidence, leaving no heldout candidate.
- External review clarified the next step: do not add a second candidate architecture. Normalize the existing `StemCellTerminal` / TRIAL / MATURE lifecycle into a stricter local candidate contract with separate relevance and outcome-credit stats.
- M9-M11 local suppressor checkpoint is implemented. `StemCellTerminal` now carries separate candidate-local relevance, credit, and survival stats; XP alone cannot mature a candidate.
- Current M11 result is a safety improvement, not KRK competence: `reports/autogrowth/krk_autogrowth_m11_local_suppressor.json` reduces heldout rook losses from 18 to 3 for the bad M4 sibling action, with 77 local suppressions, zero illegal moves, zero stalemates, and no conversion gain.
- M12 local ACTION arbitration is implemented. Move choice may occur through first-class local ACTION sibling nodes, but the harness cannot install an external move selector or direct override.
- Current M12 result is a safe fail: `reports/autogrowth/krk_autogrowth_m12_local_arbitration.json` rejects every trained M4 ACTION sibling because each receives negative causal intervention evidence. Arbitration falls back to baseline, giving 0 rook losses but still 0/200 mates.
- M13 risk-aware candidate generation is implemented. It enumerates legal training actions offline, rejects projected negative continuations, emits ReCoN-compatible ACTION candidates, and then evaluates them through M12 local arbitration.
- Current M13 result is still a safe fail: `reports/autogrowth/krk_autogrowth_m13_risk_aware_candidates.json` generates 12 candidates from 3,841 legal actions, but arbitration selects 0 heldout actions after local negative-evidence gating; result is 0 rook losses and 0/200 mates.
- M14 context-specialized candidate generation is implemented. It uses 18 generic before-context features with exact-match activation to test whether one-step ACTION candidates were failing from overly broad context.
- Current M14 result is another safe fail: `reports/autogrowth/krk_autogrowth_m14_context_specialized_candidates.json` generates 12 context-specific candidates, but arbitration selects 0 heldout actions; result is 0 rook losses and 0/200 mates.
- M15 local multi-step SCRIPT candidates are implemented. Candidates contain one SCRIPT with two sequential ACTION children and local TRIAL/survival stats.
- Current M15 result is a safe activation failure: `reports/autogrowth/krk_autogrowth_m15_local_scripts.json` generates 12 SCRIPT candidates, but heldout starts/steps/completions are all 0; result is 0 rook losses and 0/200 mates.
- M16 reusable SCRIPT fragments are implemented. M15 exact starts are generalized through local TERMINAL subcondition fragments over generic KRK features, still under SCRIPT/ACTION/stem-cell structure.
- Current M16 result opens only a narrow partial-curriculum runway: `reports/autogrowth/krk_autogrowth_m16_script_fragments.json` gives 11 train starts, 10 heldout starts, 12 heldout steps, 2 completions, zero heldout rook losses, and 0/200 mates. `partial_curriculum_ready=true`, `broad_curriculum_ready=false`.
- TG17 triplet-chain runway is implemented. `reports/autogrowth/krk_autogrowth_tg17_triplet_chain_runway.json` inventories four ready/formally validated legacy predefined-topology KRK control runs, exposes current candidates as before-terminal -> ACTION delta -> after-terminal triplets, and finds 42 after-to-before chain edges.
- Current TG17 result allows only a bounded partial curriculum: 11 train starts, 5 train mates, 10 heldout starts, 42 chain edges, zero heldout rook losses/illegal moves/stalemates, and 0/200 heldout mates. `bounded_partial_curriculum_allowed=true`, `broad_curriculum_allowed=false`.
- TG18 bounded fragment-chain curriculum is implemented. `reports/autogrowth/krk_autogrowth_tg18_fragment_chain_curriculum.json` runs protected baseline, sham fragment-chain, and real fragment-chain arms over the locked TG17/M16 runway.
- Current TG18 result is a clean failure with rollback/quarantine: h40 real chain remains 0/200 mates and reduces repetition events from 2,600 to 2,574, but causes 2 rook-loss regressions. Training M3 updates: 16; heldout M3 updates: 10; heldout chain starts: 8; completions: 1; M4 consolidation: 0.
- TG19 isolated LAG terminal checkpoint is implemented. `reports/autogrowth/krk_autogrowth_tg19_lag_terminals.json` adds a local temporal TERMINAL over generic feature deltas (`rook_attacked_by_black`, `rook_present`, repetition seen) that can inhibit a candidate transition but cannot choose a replacement move.
- Current TG19 result is partial-continue, not competence: h40 LAG keeps 0/200 mates, removes the 2 no-LAG rook losses, triggers 2 local suppressions, and keeps safety clean, but chain completions drop from 1 to 0 and repetition only improves by 1 event vs baseline. Training M3 updates: 16; heldout M3 updates: 9; M4 consolidation: 0.
- TG20 local continuation retry is implemented. `reports/autogrowth/krk_autogrowth_tg20_continuation_retry.json` lets a LAG-suppressed active SCRIPT completion locally retry another same-parent SCRIPT sibling if that sibling passes the same local TERMINAL checks.
- Current TG20 result is the first small conversion signal on this autogrowth runway, but still only partial-continue: h40/h80 retry reaches 1/200 mates, keeps 0 rook losses/illegal moves/stalemates, restores 1 chain completion vs TG19 LAG-only, reduces h40 repetition by 16 vs baseline, and records 2 heldout retry successes. Training M3 updates: 22; heldout M3 updates: 11; M4 consolidation: 0.
- TG21 local retry-edge reinforcement is implemented. `reports/autogrowth/krk_autogrowth_tg21_retry_edges.json` mines train-only active-SCRIPT -> sibling-SCRIPT retry edges and applies their bonus only inside the same local LAG-suppressed retry context.
- Current TG21 result is a clean no-scale failure: 4 retry edges were mined, 2 edge bonuses fired on heldout, and safety stayed clean, but h40/h80 remained identical to TG20 retry at 1/200 mates, 1 chain completion, 0 rook losses, and the same repetition counts. M4 consolidation: 0.
- TG22 retry diagnostics is implemented. `reports/autogrowth/krk_autogrowth_tg22_retry_diagnostics.json` traces TG20/TG21 retry events without changing behavior.
- Current TG22 diagnosis: retry edges are redundant. The artifact records 43 retry events and 18 heldout no-edge/edge comparisons; edge bonuses fired in 4 comparisons but changed the chosen sibling 0 times. Most retry requests had no local sibling available: 34 no-sibling events vs 4 completion/mate-linked events.
- TG23 retry-candidate expansion is implemented. `reports/autogrowth/krk_autogrowth_tg23_retry_candidate_expansion.json` mines additional train-only SCRIPT siblings from retry contexts where TG22 saw no local sibling.
- Current TG23 result is a clean precision failure: 8 expansion candidates were generated from 6 train retry contexts, but heldout h40/h80 dropped from TG20's 1/200 mates to 0/200, completions dropped from 1 to 0, retry successes dropped from 2 to 1, repetition returned to baseline, and safety stayed clean. The new candidates created more starts/steps and negative credit, not better continuation.
- TG24 curriculum reward recovery is implemented. `reports/autogrowth/krk_autogrowth_tg24_curriculum_reward_recovery.json` recovers the old `src/recon_lite_chess/training/krk_curriculum.py` reward/stage diagnostics as evaluation and credit instrumentation only.
- Current TG24 result: TG18-TG23 did not use `krk_reward`, `KRK_STAGES`, `box_min_side`, or `did_box_grow` in their runtime loops. TG24 now records old curriculum reward components, excess moves where exact curriculum positions apply, box/confinement trajectories, generic progress trajectories, repetition/fivefold, repeated white actions, rook attacked/missing, paired candidate-on/off deltas, and a yoked random SCRIPT-fragment control. Heldout h40 candidate-on remains TG20-like at 1/200 mates with no rook-loss/stalemate regressions and +0.97 graded-credit delta; yoked random also reaches 1/200 mates but has -0.46 graded-credit delta, worse non-terminal progress, and more confinement regressions. Trace-mined therefore beats yoked random on graded credit, but still exposes 3 confinement regressions and only a tiny mate signal.
- TG25 local precision gate is implemented but superseded as the active path. `reports/autogrowth/krk_autogrowth_tg25_local_precision_gate.json` audits confinement sign semantics first: positive `box_min_side_delta` means looser/worse confinement because `did_box_grow` is `box_min_side_after > box_min_side_before`.
- TG25 precision-gate result is a clean no-M4 failure: train evidence had 11 changed candidate rollouts, 5 negative-or-confinement-regression changes, and 2 confinement-regression changes. The local TERMINAL/RET gate suppressed 21 heldout candidate actions and reduced total confinement-worsened events from 1546 to 1540, but h40 mate signal dropped from ungated 1/200 to gated 0/200, graded credit fell by -2.56 versus ungated and -1.59 versus baseline, and one paired confinement regression remained. M3 was frozen for confirmation; M4 consolidation stays 0.
- Active TG25 direction is now foundation curriculum re-entry: use the old KRK curriculum as the actual experience distribution and training/evaluation runway, not merely diagnostics. Start with dense generated Mate_In_1 positions, then verified forced Mate_In_2 positions, before returning to edge-trap/fence/cut/box stages.
- Curriculum scheduling is allowed and is not a purity violation. Stage/curriculum labels may choose the training distribution and appear in diagnostics, but learner-visible features remain generic board/graph/action/outcome features. Runtime tablebase/DTM move provision, direct provider override, stage-label features, and hand-authored tactical names remain forbidden.
- Current TG25 foundation implementation trains local ACTION TRIAL nodes with M3 credit over legal generated Mate_In_1 moves, uses checkmate only as the reward label, and promotes via M4 only when heldout accuracy clears threshold. Mate_In_2 is generated by mechanical forced-mate verification, not by trusting old stage templates.
- Current TG25 foundation result: `reports/autogrowth/krk_autogrowth_tg25_foundation_curriculum.json` passes. Mate_In_1 improves from pre-training 28/300 to 300/300 heldout and 120/120 mirrored generalization; wrong-action suppression is 5179/5253, M3 updates are 591,698, and Mate_In_1 M4 event count is 1. Verified Mate_In_2 reaches 83/100 first-move success and 83/100 3-ply conversion with M4 event count 1. Labels remain schedule/diagnostic only; learner-visible records use generic ACTION feature nodes.
- TG26 edge/fence curriculum is implemented as a bounded continuation of TG25. `reports/autogrowth/krk_autogrowth_tg26_edge_fence_curriculum.json` uses curriculum as the training distribution, reuses the Mate_In_1/Mate_In_2 foundation, and switches harder-stage credit to graded reward: mate dominates; slow mate is still better than non-mate; reaching earlier solved regions is good; confinement/fence/king approach are small shaping terms; repetition/no progress, confinement regression, rook loss, stalemate, and illegal moves are penalized.
- Current TG26 result is a useful fail, not an advancement: bounded edge-trap heldout is 1/8 conversion with 1/8 earlier-region handoff, 2 rook-loss events, 2 confinement regressions, M3 updates 8,717, and M4 0. Fence-hold heldout is 0/8 conversion, 0/8 handoff, 3 rook-loss events, 4 confinement regressions, M3 updates 8,970, and M4 0. Foundation regression passes in the bounded run, but stage advancement/safety fail.
- TG26 also exposes a throughput issue: 500/100 edge/fence chunks with all-legal-action, all-black-reply handoff scoring were not practical in this run. Before long edge/fence training, optimize/cap scoring or cache reusable foundation/handoff evaluations. Do not return to broad random KRK or ecological spawning until edge/fence small slices pass safely.
- TG26b failure repair is implemented. `reports/autogrowth/krk_autogrowth_tg26b_edge_fence_failure_repair.json` adds detailed failure-slice audit, cheap generic safety/progress scoring for all legal actions, deep foundation handoff scoring only for top-K actions, cached cheap/deep scores, and a strict local safety gate for rook-loss, one-reply rook-loss risk, stalemate, and confinement regression. This is local board/action/outcome safety credit, not a global strategy provider.
- Current TG26b result is a safe partial continue, not M4: larger bounded slice uses 160 train samples, 48 heldout positions, and 2 chunks per edge/fence stage. Edge-trap improves to 10/48 conversion and 10/48 earlier-region handoff, with 0 rook losses, 0 confinement regressions, M3 updates 142,876, M4 0. Fence-hold improves to 7/48 conversion and 7/48 handoff, with 0 rook losses, 0 confinement regressions, M3 updates 143,152, M4 0. Foundation regression still passes: Mate_In_1 1.0 heldout, Mate_In_2 0.8125.
- TG26b throughput improved: edge-trap cheap-scored 3,708 actions, deep-scored 1,589, pruned 4,397, and safety-rejected 608; fence cheap-scored 3,740, deep-scored 1,585, pruned 4,497, and safety-rejected 924. Continue conditions pass, but stage advancement thresholds do not, so M4 correctly remains 0.
- TG26c handoff-filtered curriculum is implemented. It keeps TG26b safety/graded credit but optionally requires the position generator to find at least one deep-scored candidate action that can hand off to the Mate_In_1/Mate_In_2 foundation. This filter is trainer-side curriculum scheduling only: it is not a learner-visible feature, not a provider override, and not a runtime DTM/tablebase move source.
- Current TG26c result is a sharper partial continue: `reports/autogrowth/krk_autogrowth_tg26c_edge_fence_curriculum_handoff.json` uses edge handoff-filtered generation over a 160-train/48-heldout, 2-chunk bounded slice. Edge-trap improves to 34/48 conversion and 34/48 earlier-region handoff, with 0 rook losses, 0 confinement regressions, 0 stalemates, M3 updates 138,920, and M4 0. Fence-hold remains at 7/48 conversion/handoff with clean safety because fence handoff filtering was disabled in the larger run.
- `reports/autogrowth/krk_autogrowth_tg26c_edge_fence_handoff_generation_smoke.json` proves combined edge+fence handoff-filtered generation at smoke scale: edge 7/12 and fence 7/12 conversion/handoff, both with 0 rook losses, 0 confinement regressions, 0 stalemates, M3 nonzero, and M4 0. Larger combined fence-filtered runs were too slow at 96/32 and 160/48, so the next bottleneck is throughput, not another retry candidate.
- TG26d handoff-filter validation is implemented. `reports/autogrowth/krk_autogrowth_tg26d_handoff_filter_validation.json` first verifies the canonical TG26c main artifact as full parseable JSON: 3,343,251 bytes, schema `krk_autogrowth_tg26c_edge_fence_handoff_curriculum.v0`, sha256 `50e6cb6eecb52f9ddddff6a61b8ac665f162dedd1333ce640b2a342720844d6a`. Smoke is no longer the only evidence.
- TG26d conservative interpretation: handoff-filtered heldout measures curriculum runway usefulness, not stage competence. The bounded main TG26d run uses 40 filtered train-pool positions, 80 train samples per chunk, 12 heldout positions per slice, and 2 chunks. Edge-trap gets filtered 4/12, unfiltered 6/12, boundary-near-miss 1/12 conversion/handoff with 0 rook loss/stalemate/illegal/confinement regression, M3 68,356, M4 0. Fence-hold gets filtered 7/12 but unfiltered 0/12 and boundary 0/12 with clean safety, M3 69,184, M4 0. This is partial edge generalization evidence and fence runway-only evidence; it is not an edge/fence competence claim.
- TG26d generator stats show the selection pressure directly. Edge filtered train-pool acceptance is 40/172 attempts (23.3%) with 87 no-handoff rejections. Fence filtered train-pool acceptance is 40/630 attempts (6.35%) with 397 invalid generations and 191 no-handoff rejections. On-demand 80/24 and 160/48 TG26d validation runs were stopped after several minutes, so the remaining blocker is persistent/indexed handoff-eligible pool generation, especially for fence.
- TG26e persisted pool validation is implemented. `reports/autogrowth/krk_autogrowth_tg26e_persisted_pool_validation.json` stores six indexed pools (`edge_filtered`, `edge_unfiltered`, `edge_boundary_near_miss`, `fence_filtered`, `fence_unfiltered`, `fence_boundary_near_miss`) with FEN, generator/slice type, acceptance reason, cheap/deep candidate scores, best candidate action, handoff type, safety flags, black reply when available, and cached feature/action keys.
- Current TG26e result uses 64 filtered train-pool positions per stage, 32 heldout positions per slice, 96 train samples per chunk, and 2 chunks. Edge-trap survives larger validation: filtered 21/32, unfiltered 12/32, boundary 4/32 conversion/handoff, safety clean, M3 82,869, M4 0. Fence-hold improves but remains weak: filtered 19/32, unfiltered 1/32, boundary 0/32, safety clean, M3 84,962, M4 0. Fence is not competent; the single unfiltered hit is not enough to proceed broad.
- TG26e pool statistics expose remaining selection pressure: edge filtered accepts 96/441 attempts with 245 no-handoff rejections; fence filtered accepts 96/1703 attempts with 543 no-handoff rejections. Foundation regression passes, direct provider override remains false, runtime tablebase/DTM move source remains false, and stage/curriculum labels remain non-learner-visible.
- TG26f fence boundary rehearsal is implemented. `reports/autogrowth/krk_autogrowth_tg26f_fence_boundary_rehearsal.json` keeps edge as a regression guard and trains fence with additional persisted unfiltered and boundary/near-miss rehearsal pools. This is still local ACTION-weight training over generic board/action/outcome credit; no provider override, runtime tablebase/DTM, learner-visible stage labels, broad KRK, or ecological spawning.
- Current TG26f result is an informative no-boundary-transfer result, not a success. Edge stability is preserved: filtered 21/32, unfiltered 12/32, boundary 4/32, safety clean, M3 110,814, M4 0. Fence filtered improves to 21/32 and unfiltered nudges from TG26e's 1/32 to 2/32, but boundary remains 0/32 with clean safety, M3 115,253, M4 0. Boundary rehearsal alone does not solve fence transfer.
- TG26g fence boundary local signal is implemented. `reports/autogrowth/krk_autogrowth_tg26g_fence_boundary_signal.json` adds fence-only local ACTION feature nodes for generic post-move deltas: confinement delta sign, black edge-distance delta sign, black mobility delta sign, white-king distance delta sign, and rook-safe state. This signal does not choose moves directly and does not use learner-visible stage labels, provider override, or runtime tablebase/DTM.
- Current TG26g result is another informative failure. Edge guard remains stable: filtered 21/32, unfiltered 12/32, boundary 4/32, safety clean, M3 110,814, M4 0. Fence filtered drops to 18/32, unfiltered nudges to 3/32, but boundary remains 0/32 with clean safety, M3 150,600, M4 0. The local delta signal is insufficient for fence boundary transfer.
- TG26h FeatureHub / terminal substrate revival is implemented. `reports/autogrowth/krk_autogrowth_tg26h_terminal_substrate_revival.json` audits behavior-changing `ActionRanker.choose()` paths, compares current autogrowth features against `FeatureHub` and older KRK features, then reproduces the foundation curriculum through first-class `StemCellTerminal` feature-vector TERMINALs with local M3 weights. `ActionRanker` remains only a diagnostic baseline/scaffold in the artifact.
- Current TG26h result passes the foundation checkpoint: terminal-native Mate_In_1 is 100/100 heldout and 40/40 mirrored, and terminal-native Mate_In_2 is 90/100 heldout conversion with M4 event count 1 after heldout confirmation. On the same heldout, the diagnostic ActionRanker baseline gets Mate_In_2 83/100. The run still uses synchronous Python legal-move enumeration as the environment interface; behavior-changing weights are terminal/node-local, not a provider override or tablebase/DTM move source.
- TG26i terminal-native edge/fence validation is implemented. `reports/autogrowth/krk_autogrowth_tg26i_terminal_edge_fence_validation.json` uses the TG26h terminal foundation and terminal stage learners, with ActionRanker-family TG26g results kept as reference scaffolding only. The tractable bounded run uses 32 filtered train-pool positions, 16 heldout positions per slice, 16 fence rehearsal positions per rehearsal slice, top-K 3 deep scoring, and 2 train chunks; a larger 64/32/top-K 6 run was stopped after ~16 minutes because terminal-native handoff scoring needs persisted/progress-aware pools before scaling.
- Current TG26i result is a useful failure, not a stage advance. Foundation regression passes (Mate_In_1 1.0, Mate_In_2 0.90), safety is clean, and edge remains stable: filtered 13/16, unfiltered 8/16, boundary 4/16. Fence remains the bottleneck: filtered 8/16, unfiltered 1/16, boundary 0/16, with M4 0. Compared with TG26g, fence unfiltered is -2 and fence boundary is unchanged at 0. Terminal substrate restoration alone did not solve fence boundary transfer.
- TG26j curated terminal curriculum reuse is implemented. `reports/autogrowth/krk_autogrowth_tg26j_curated_terminal_curriculum.json` treats `src/recon_lite_chess/training/krk_curriculum.py::KRK_STAGES` as the active near-term schedule source again, not legacy diagnostics. The artifact inventories curated stages, validates which FENs are mate-in-one / strict forced-mate-in-two / later graded-rollout / invalid under python-chess, and trains terminal-native Mate_In_1/Mate_In_2 on curated foundation buckets. Curriculum labels and descriptions remain schedule/diagnostic only; learner-visible features stay generic terminal/action/outcome features.
- Current TG26j/TG26k result: the curated Stage 1 `Mate_In_2` bank is repaired and now has five legal strict mate-in-two originals; with symmetries, `Mate_In_2` contributes 25 strict entries. The Stockfish artifact now uses Stockfish 16 mate-search proof plus the exact legal-reply verifier; they agree on 30/150 strict mate-in-two claim entries overall, with 60 invalid/terminal and 60 mate-longer entries belonging to later/broader `optimal_moves == 2` stages. The simple all-at-once TG26j terminal run exposes interference: original Mate_In_2 converts 4/5 and symmetry-expanded converts 18/23.
- TG26k cumulative curated replay is implemented. `reports/autogrowth/krk_autogrowth_tg26k_curated_replay_curriculum.json` trains Mate_In_1, then repaired Mate_In_2 buckets sequentially with prior-bucket replay and regression checks. Final replay recovers Mate_In_1 18/18 and Mate_In_2 23/23, with 536 Mate_In_2 first-move terminals and nonzero M3 updates, but M4 remains 0 because 3 intermediate bucket-admission windows are unstable. This is a clear self-growth partial: terminal spawning and M3 weighting are active, but flat shared terminal weights still have bucket interference.
- TG26l context-gated local terminal subgraphs are implemented. `reports/autogrowth/krk_autogrowth_tg26l_context_gated_curriculum.json` adds generic before-position terminal gates and local Mate_In_2 first-move terminal learners per context prototype. This stabilizes repaired Mate_In_2 admission: 23/23 converted, no fallback gates, M3 updates nonzero, and M4 Mate_In_2 consolidation event count 1. The runtime cause is still generic context-gate activation plus terminal subgraph weights; stage labels remain schedule/diagnostic only. This is the current best local self-growth substrate for early KRK.
- TG26m context-gated edge/fence validation is implemented. `reports/autogrowth/krk_autogrowth_tg26m_context_gated_edge_fence_validation.json` uses TG26l as the Mate_In_2 handoff substrate inside the existing persisted edge/fence graded rollout. Bounded 16-train/8-eval slices keep foundation regression clean (`Mate_In_1=1.0`, `Mate_In_2=1.0`, no unconfirmed gates) and safety clean, but do not advance edge/fence: edge filtered 3/8, unfiltered 2/8, boundary 0/8; fence filtered 3/8, unfiltered 0/8, boundary 0/8; M3 updates are nonzero and M4 remains 0. This says TG26l repaired early handoff structure, but it is not the missing edge/fence boundary mechanism.
- TG26n single-graph curriculum is implemented. `reports/autogrowth/krk_autogrowth_tg26n_single_graph_curriculum.json` corrects the curriculum invariant: one persistent graph is trained through Mate_In_1 then Mate_In_2; Mate_In_1 terminals/triplets are matured before Mate_In_2; Mate_In_2 spawns explicit before-terminal -> action-delta -> after-terminal triplets; and the same graph chooser is used on every white move. There is no separate Mate_In_1 network and no hardcoded Mate_In_1 handoff. The initial normalized run got Mate_In_1 18/18 and Mate_In_2 13/23.
- TG26n 50-repetition audit found the missing stability rule. Flat context-free `action_pattern:*` terminals and unbounded repeated M3 credit make the single graph collapse under long replay: the old flat-action 50/50 artifact and the no-flat-but-unbounded 50/50 artifact both get Mate_In_2 0/23. The corrected bounded artifact `reports/autogrowth/krk_autogrowth_tg26n_single_graph_curriculum_bounded_reps50.json` sets `score_context_free_action_terminals=false` and `max_abs_local_weight=1.0`, keeps action patterns available inside triplets, and gets Mate_In_1 18/18 plus Mate_In_2 23/23 with the same graph, 15,681,600 M3 updates, and a Mate_In_2 M4 event count of 1.
- Historical curriculum evidence supports high-throughput staged training but must be separated from autonomy claims. `reports/autogrowth/krk_autogrowth_tg26h_terminal_substrate_revival.md` records Mate_In_1 100/100 heldout and Mate_In_2 90/100 heldout after 300-train/100-heldout generated curricula. Older snapshots such as `snapshots/krk_triplet_pipeline/adaptive_krk_stage2a_fixed/run_manifest.json` used 40 stage0 cycles, 20 stage1 cycles, 150 samples/cycle, and 100-sample evals. Those runs are useful curriculum/plasticity evidence, but later April/May edge/fence/drive snapshots also include loaded learners and handoff/composition scaffolds, so they are not clean single-graph autonomy proof.
- TG26o native ReCoN migration is implemented as a partial checkpoint, not a foundation success claim. Correction: Article.md defines ReCoN nodes as SCRIPT plus TERMINAL; actuator/action affordances must therefore be `NodeType.TERMINAL` leaves with actuator metadata, not a third core node type. `reports/autogrowth/krk_autogrowth_tg26o_native_recon_partial.json` materializes feature terminals and actuator affordance terminals as actual `NodeType.TERMINAL` nodes, materializes triplets as SCRIPT/TERMINAL subgraphs with actuator terminal leaves, uses real SUB/SUR/POR/RET pairs, stores trial/mature state and M3 weights on graph nodes/edges, and uses `FormalReConEngine` for runtime choice. Native Mate_In_1 reaches 5/5 on the no-symmetry originals after 5 repetitions with 18,523 nodes, 37,436 edges, and 98 triplets. Mate_In_2 native parity is not proven: the no-symmetry 5/2 run is blocked by throughput because the generic formal engine ticks the fully materialized graph too slowly.
- TG26p native scheduler foundation is the current first-two-step checkpoint. `reports/autogrowth/krk_autogrowth_tg26p_native_scheduler_foundation.json` uses one persistent native graph through Mate_In_1 then Mate_In_2, with actuator affordances as TERMINAL leaves, real SUB/SUR/POR/RET edges, M3/M4 state on graph nodes/edges, and runtime choice through `FormalReConEngine`. The indexed scheduler ticks only candidate triplet structural nodes and grouped terminals by default; individual feature terminals remain real graph nodes with weights/maturity but are not all ticked for every runtime choice. This is a scheduler optimization, not an external move provider. The symmetry-expanded run passes Mate_In_1 18/18 and Mate_In_2 23/23 with 23 same-graph second moves, 336,799 nodes, 680,724 edges, and 1,782 triplets.
- TG26q native scheduler replay/audit is implemented and fails for the right reason. `reports/autogrowth/krk_autogrowth_tg26q_native_scheduler_replay_audit.json` is a completed 50x no-symmetry replay audit (`include_symmetries=false`): curated replay remains stable at Mate_In_1 5/5 and Mate_In_2 5/5 through repetition 50, same-graph second moves remain 5, M3 reaches 4,087,400 updates, and scheduler equivalence on the tiny full-feature-terminal subset has 0 mismatches. Ablations are causal: masking Mate_In_2 first-move triplets collapses first moves, and masking mate-terminal structures collapses second moves. But generated heldout is 0/12 Mate_In_1 and 0/6 Mate_In_2, so the current exact-triplet native substrate does not generalize beyond seen/generated-equivalent patterns. The attempted full symmetry-expanded 50x audit was stopped after more than an hour without intermediate artifact; TG26q needs progress/checkpoint logging before rerunning full symmetry. Do not return to edge/fence.
- TG26r native foundation generalization repair is implemented as a bounded diagnostic failure, not a pass. `reports/autogrowth/krk_autogrowth_tg26r_native_foundation_generalization.json` trains the same native graph on generated Mate_In_1 then Mate_In_2 and compares exact triplets, prototype terminals, and canonical/relative features. Exact remains 0/6 Mate_In_1 and 0/3 Mate_In_2 with 9 nulls. Prototype terminals improve Mate_In_1 to 2/6 and reduce nulls to 7, while canonical gets 1/6 and 8 nulls; all arms remain 0/3 Mate_In_2. Scheduler equivalence stays clean at 0 mismatches, M4 true promotion remains 0, and only the root is mature/materialized. Larger 120/40/60/20 and 30/12/15/6 generated runs were stopped after long CPU-bound execution without artifact, so the next repair must improve native generated-foundation throughput and generalization; do not return to edge/fence.
- TG26s shared feature atom substrate is implemented as a Mate_In_1-only diagnostic checkpoint. `reports/autogrowth/krk_autogrowth_tg26s_shared_feature_atoms.json` replaces triplet-local generic feature leaves with reusable shared `NodeType.TERMINAL` atoms in the shared arms, disables grouped cache terminals there, lets shared atoms causally tick/confirm through `FormalReConEngine`, retrieves candidate triplets from active atom overlap, adds small generic projection atoms, and records pruning/compression diagnostics. The architecture invariant is repaired but the learning result fails: baseline prototype is 2/6 generated Mate_In_1 heldout, while shared atom, shared projection, and post-prune arms are all 0/6 with 6 nulls. Shared atoms are genuinely reused (453 atoms, 449 reused; 0 triplet-local feature terminals and 0 grouped cache terminals in the pruned shared arm), scheduler equivalence has 0 mismatches, and pruning removed 83 low-utility shared atoms without train replay collapse. Do not proceed to Mate_In_2; next repair is shared-atom retrieval/credit so reused terminals improve heldout instead of only replay.
- TG26t shared atom utility voting is implemented and produces the first strong shared-terminal Mate_In_1 signal, with an important caveat. `reports/autogrowth/krk_autogrowth_tg26t_shared_atom_utility_voting.json` keeps the 12-train/6-heldout TG26s scale and compares baseline prototype, hard-overlap shared atoms, weighted shared-atom voting, action-pattern scoring, contrastive credit, and soft quorum. Baseline prototype remains 2/6 with 4 nulls; hard-overlap, weighted, action-atom, and contrastive arms remain 0/6 with 6 nulls because old full-triplet confirmation still fails. The soft-quorum shared-terminal evidence arm reaches 6/6 with 0 nulls, action/check atoms included, and scheduler equivalence 0 mismatches. Caveat: `strict_native_quorum_materialized=false` and all 6 soft-quorum selections are marked `soft_quorum_selected_without_full_triplet_confirmation`; this is a shared-terminal evidence breakthrough, not yet a fully materialized ReCoN SCRIPT/quorum runtime. Next step is to turn the soft quorum into first-class ReCoN graph structure before Mate_In_2.
- TG26u native quorum materialization is implemented. `reports/autogrowth/krk_autogrowth_tg26u_native_quorum_materialization.json` keeps TG26t's 12-train/6-heldout Mate_In_1 scale, preserves TG26t soft quorum as a diagnostic baseline, and materializes the quorum as first-class native ReCoN structure: quorum SCRIPT nodes, evidence TERMINAL leaves, shared feature/action atoms as `NodeType.TERMINAL`, actuator affordance TERMINALs, real SUB/SUR edges, and selection only from quorum SCRIPTs confirmed by `FormalReConEngine`.
- Current TG26u result passes the native Mate_In_1 materialization checkpoint: baseline prototype is 2/6, soft quorum diagnostic is 6/6, materialized native quorum is 6/6 with 0 nulls, scheduler equivalence has 0 mismatches, and the canonical selected arm records 10 FormalReConEngine-confirmed quorum candidates across the 6 heldout positions. Action/check atom ablation collapses to 0/6, actuator terminal ablation collapses to 0/6, and removing materialized quorum while keeping shared atoms gives 0/6. Top-positive-atom ablation remains 6/6, so the causal dependence in this small slice is specifically on action/check atoms plus actuator legality, not every high-weight positive atom.
- TG26u is not a Mate_In_2 or edge/fence claim. Candidate construction still uses trainer-side shared-atom retrieval to decide what quorum nodes to materialize, but final behavior is mediated through materialized SCRIPT/TERMINAL graph confirmation rather than the TG26t soft-quorum bypass. Next checkpoint is generated Mate_In_2 with the same native materialized quorum structure and no hardcoded Mate_In_1 handoff.
- TG26v native quorum Mate_In_2 chaining is implemented. `reports/autogrowth/krk_autogrowth_tg26v_native_quorum_mate2_chaining.json` reuses the same TG26u native graph, trains generated Mate_In_2 first-move evidence only from same-graph materialized Mate_In_1 continuation success, and materializes first moves as native quorum SCRIPT/evidence TERMINAL/chain TERMINAL/actuator TERMINAL structure. The chain terminal is positive only when the same graph's TG26u Mate_In_1 quorum solves the reply states; no hardcoded Mate_In_1 provider, ActionRanker runtime, tablebase/DTM runtime source, or learner-visible stage label is used.
- Current TG26v result passes the tiny generated Mate_In_2 checkpoint: Mate_In_1 materialized quorum remains 6/6 with 0 nulls; Mate_In_2 heldout is 3/3 first-move success and 3/3 conversion; same-graph second-move count is 3; materialized Mate_In_2 quorum confirmations are 3; soft-chain diagnostic is 3/3; scheduler equivalence has 0 mismatches. Ablations collapse as required: Mate_In_2 first-move quorum 0/3, Mate_In_1 quorum 0/3, actuator terminals 0/3. This is a small native chaining proof, not a broad KRK or edge/fence competence claim.
- TG26v includes a throughput guard: all legal first moves are enumerated for credit/materialization, but expensive black-reply continuation checks are skipped for validator-known non-forced first moves and recorded as negative chain evidence. This keeps curriculum/evaluation labels trainer-side; they are not learner-visible graph features. Next checkpoint should scale generated Mate_In_2 and audit whether this forced-skip optimization remains acceptable before returning to edge/fence.
- TG26w internal handoff affordance / guard audit is implemented. `reports/autogrowth/krk_autogrowth_tg26w_internal_handoff_affordance_guard_audit.json` measures TG26v's validator skip guard and adds a materialized internal continuation-attention gate: handoff quorum SCRIPTs, handoff evidence TERMINALs, shared atom probes, and actuator TERMINALs. Validator/teacher labels are used for training the handoff affordance, but the internal-handoff evaluation arm does not use validator skip during runtime choice or evaluation.
- Current TG26w result passes the minimal internal-handoff criterion but exposes precision debt. Guarded baseline remains 3/3, the one-position fully guardless probe checks 20 first-move candidates and 69 reply continuations in 68.50 seconds with 0 skipped candidates and 1/1 conversion, and the internal-handoff arm reaches 3/3 conversion with 3 same-graph second moves and 3 materialized Mate_In_2 quorum confirmations. The internal gate approves 15 candidates and rejects 47, with 12 false positives and 0 false negatives. Ablations remain causal: masking internal handoff, Mate_In_1 quorum, or actuator terminals collapses conversion. The next step is not edge/fence; it is improving internal gate precision and/or scaling Mate_In_2 with progress artifacts.
- TG26x terminal-kind lifecycle and modest native foundation scale is implemented. `reports/autogrowth/krk_autogrowth_tg26x_terminal_lifecycle_modest_scale.json` adds terminal-kind policies for environment feature, action-delta, projection/conjunction, internal attention, handoff gate, chain-confidence, veto/safety, and actuator TERMINALs. The policy reports expected activation, decay, exposure/precision thresholds, utility formula, M4 promotion requirements, and prune reasons by terminal kind.
- Current TG26x result is a structural pass with a learning caveat. Tiny TG26w calibration remains clean: conversion 1.0, internal-gate false positives 12, false negatives 0. The modest generated run keeps Mate_In_1 heldout at 12/12 with 0 nulls, but Mate_In_2 is only 2/6 conversion and 2/6 first-move success. The internal gate approves 35 candidates, rejects 89, records 33 false positives and 4 false negatives, runs 95 deep reply checks, records 132,964 M3 updates, keeps scheduler equivalence at 0 mismatches, and applies lifecycle/pruning diagnostics without claiming broad competence. Do not return to edge/fence; next step is improving internal handoff precision/replay and scaling generated Mate_In_2 cautiously with progress artifacts.
- TG26y continuous handoff attention is implemented and fails usefully. `reports/autogrowth/krk_autogrowth_tg26y_continuous_handoff_attention.json` treats internal handoff as request-strength modulation rather than a hard branch killer, compares binary, high-recall threshold, top-K, top-K plus epsilon, softmax, and two-stage attention, and keeps runtime/evaluation free of validator skip, ActionRanker runtime, tablebase/DTM move provision, and learner-visible stage labels.
- Current TG26y result: Mate_In_1 remains clean at 12/12 with 0 nulls, but every attention mode remains at 1/6 Mate_In_2 conversion. High-recall threshold is selected because it drops false negatives to 0, but it spends 314 deep reply checks and creates 101 false positives; binary baseline spends 46 checks with 6 false negatives and 16 false positives. Repetition-2 comparison also remains 1/6 and reintroduces 2 false negatives. Scheduler equivalence stays at 0 mismatches; causal ablations collapse behavior; M4 promotion remains 0 because checkpoint-level heldout confirmation failed. Bottleneck is no longer only binary rejection. Next repair should inspect same-graph continuation/quorum evidence for checked forced moves and why chain confirmation selects only one heldout first move.
- TG26z forced-chain decomposition and continuation repair is implemented. `reports/autogrowth/krk_autogrowth_tg26z_forced_chain_decomposition.json` audits forced Mate_In_2 candidates layer by layer: internal attention, same-graph Mate_In_1 continuation over all black replies, chain terminal, Mate_In_2 first-move quorum, and final selection. Validator-known forced first moves are used only as audit labels and curriculum scheduling; final runtime/evaluation still uses native materialized graph confirmation without validator skip, ActionRanker runtime, tablebase/DTM move provision, or learner-visible stage labels.
- Current TG26z result is the first strong generated native Mate_In_2 repair: ordinary Mate_In_1 remains 12/12 with 0 nulls, continuation Mate_In_1 is already 1.0 with 0 nulls, forced-first chain success is 1.0 with 10 same-graph second moves, and final Mate_In_2 improves from TG26y's 1/6 to 6/6. The repair is narrow: when same-graph continuation confirms through the chain terminal, the materialized Mate_In_2 quorum can be satisfied by that chain terminal instead of requiring extra weak local positive atoms. Causal ablations collapse behavior: masking Mate_In_1 quorum, Mate_In_2 quorum/chain confidence, internal handoff, actuator terminals, or deep continuation checks all gives 0 conversion. Scheduler equivalence remains 0 mismatches. M4 promotes only a small set of non-handoff terminal kinds after heldout confirmation; handoff/chain-confidence terminal M4 remains 0.
- TG27a native foundation scale/replay is implemented. `reports/autogrowth/krk_autogrowth_tg27a_native_foundation_scale_replay.json` scales the TG26z chain-quorum repair to 32 generated Mate_In_1 train, 16 Mate_In_1 heldout, 16 generated Mate_In_2 train, and 8 Mate_In_2 heldout positions on one persistent native graph. Frozen evaluation passes the bounded scale criterion: Mate_In_1 heldout is 16/16 with 0 nulls, Mate_In_2 heldout is 7/8 conversion and 7/8 first-move success, same-graph second moves are 7, continuation Mate_In_1 is 1.0 with 0 nulls, and no M3/M4 changes occur during frozen evaluation. The 10x no-learning replay is full graph replay, not cached replay: all 10 replay conversion rates remain 0.875 and M3 updates stay fixed. Causal ablations still collapse conversion to 0 when Mate_In_1 quorum, Mate_In_2 quorum/chain confidence, internal handoff, actuator terminals, or deep continuation checks are masked. Scheduler equivalence remains 0 mismatches. M4 promotion remains conservative and promotes only the same small non-handoff terminal kinds.
- TG27b single-miss repair is implemented. `reports/autogrowth/krk_autogrowth_tg27b_single_miss_repair.json` explains the lone TG27a Mate_In_2 miss as an internal-attention false negative: FEN `8/7k/5K2/8/8/8/4R3/8 w - - 0 1`, correct first move `e2e8`, request strength `0.018708`, original high-recall threshold `0.02`, and failure bucket `rejected_by_internal_attention`. The repair only lowers the high-recall threshold to `0.018`; it does not add a provider, selector, tablebase/DTM source, or learner-visible stage label. Repaired Mate_In_2 is 8/8 conversion and 8/8 first-move success with 8 same-graph second moves; false negatives drop 1 -> 0, false positives rise 129 -> 131, and deep reply checks rise 377 -> 386. The 10x no-learning full graph replay remains 1.0 throughout with no M3 drift. Causal ablations still collapse conversion to 0, scheduler equivalence remains 0 mismatches, and M4 promotion remains conservative/non-handoff.
- TG28a frozen-foundation edge/fence re-entry is implemented. `reports/autogrowth/krk_autogrowth_tg28a_frozen_foundation_edge_fence_reentry.json` uses the TG27b Mate_In_1/Mate_In_2 native foundation as a frozen continuation substrate while training only a new native edge/fence layer. Foundation freeze holds: M3/M4 deltas during edge training and evaluation are all 0; foundation sanity subset remains Mate_In_1 1.0, Mate_In_2 1.0, replay stable, and foundation ablations still collapse. Edge/fence heldout at 16/8 produces safe native graph-mediated confinement progress: edge/fence success 0.875, confinement-area improvement 0.875, black-king mobility reduction 0.625, selected moves 8/8, nulls 0, rook blunders 0, stalemate avoidance 1.0, scheduler mismatches 0, and edge-only M3 updates 2,745. Causal ablations now behave after correcting the graph wiring: masking edge/fence, action-delta, internal-attention, safety/veto, or actuator terminals collapses selected moves on the ablation slice. Caveat: this is not yet edge/fence-to-foundation chaining; selected heldout moves reached 0 foundation handoffs, with failure buckets mostly `confinement_improved_but_foundation_not_closer`.
- TG28b frozen-foundation bridge-pressure audit is implemented as a bounded failure. `reports/autogrowth/krk_autogrowth_tg28b_frozen_foundation_bridge_pressure.json` keeps the TG27b foundation frozen and records a side-to-move-aware bridge audit. The foundation remains sane on the bounded probe: Mate_In_1 1.0, Mate_In_2 forced-chain sanity 1.0, and foundation M3/M4 deltas during bridge training/eval are 0/0. Trainer-side frontier generation finds bridge-like edge/fence positions, but native graph-mediated bridge response does not appear: bridge heldout 2/2 null selections, reply-envelope foundation reachable count 0, bounded bridge reachable count 0, and failure bucket `no_bridge_candidate_generated`. Edge/bridge M3 credit is nonzero (36 edge updates, 28 bridge updates), but no behavior-changing candidate confirms. A wider top-8 deep probe timed out, so this is a throughput and foundation-basin mismatch diagnosis, not an edge/fence advancement.
- TG28c frozen-foundation response cache + bridge retrieval is implemented. `reports/autogrowth/krk_autogrowth_tg28c_frozen_foundation_response_cache_bridge_retrieval.json` builds a memoized frozen-native foundation response cache, audits cache/live equivalence, samples the learned foundation basin, and uses cache-derived foundation response evidence to retrieve/materialize bridge candidates. The cache is not a provider: final behavior remains mediated through bridge/foundation-response/safety/actuator terminals and `FormalReConEngine`. Result is a bounded diagnostic pass, not advancement: foundation remains frozen and sane (Mate_In_1 1.0, Mate_In_2 1.0, M3/M4 deltas 0), cache/live mismatches are 0 across 226 cached states, basin sampling finds 9 foundation-positive states out of 34, but bridge heldout still produces 0/36 cache-scored candidates with frozen-foundation response, 0 selected moves, 4 nulls, and failure bucket `safe_candidates_exist_but_no_foundation_response`. This confirms TG28b was not just side-to-move bookkeeping or cache mechanics; the frozen TG27b basin is too narrow for current edge/fence bridge successors.
- TG28d foundation-backed bridge frontier is implemented. `reports/autogrowth/krk_autogrowth_tg28d_foundation_backed_bridge_frontier.json` generates bridge/frontier positions outward from frozen TG27b foundation-positive states instead of hoping generic edge/fence successors enter the narrow foundation basin. Result is a tiny bounded pass, not edge/fence competence: foundation remains frozen and sane on the compact probe (Mate_In_1 1.0, Mate_In_2 1.0, cache/live mismatches 0, M3/M4 foundation deltas 0), bridge-frontier generation produces 4 all-reply bridge positions, heldout selection chooses 1/2 graph-mediated bridge moves, that selected move has reply-envelope foundation reachability and same-graph continuation, and safety stays clean with 0 rook blunders and stalemate avoidance 1.0. One-position ablations collapse selection when bridge-pressure, foundation-response, edge/fence, action-delta, actuator, safety/veto, Mate_In_2 foundation quorum, or internal-attention terminals are masked. Important caveat: the canonical artifact uses a small 4/2/1/1 foundation and 2/2 bridge frontier because larger 16/8/8/4 runs were too slow in frontier generation/evaluation. This proves the foundation-backed frontier mechanism can work, not that generic edge/fence generalizes.
- TG28e persisted foundation-backed frontier pool is implemented. `reports/autogrowth/krk_autogrowth_tg28e_persisted_foundation_backed_frontier_pool.json` writes a resumable JSONL pool plus index under `reports/autogrowth/pools/`, deduped by canonical position, candidate move, and foundation config hash, then trains/evaluates the existing graph-mediated bridge layer from persisted pool splits. Result is an infrastructure diagnostic pass, not full advancement: compact fallback is used, cache/live mismatches are 0, foundation sanity is Mate_In_1 1.0 and Mate_In_2 1.0, pool split is 4 train / 2 heldout / 2 regression, heldout selection is 2/2 with 2 reply-envelope foundation-reachable handoffs, same-graph continuations 2, rook blunders 0, scheduler mismatches 0, edge-only M3 updates 108, and bridge-terminal M3 updates 84. Full TG27b generation was probed first and reached clean foundation sanity plus 6 persisted train entries, but hit the 15-minute cap before completing the requested 8/4/4 pool. Throughput is the blocker: compact pool generation took 272.76 seconds for 8 entries, averaging 34.09 seconds per accepted entry.
- TG28f full foundation frontier-pool resume is implemented. `reports/autogrowth/krk_autogrowth_tg28f_full_foundation_frontier_pool_resume.json` fixes the TG28e compact-fallback limitation by running the full TG27b foundation config (32/16 Mate_In_1, 16/8 Mate_In_2), building an anchor index from TG27b and TG28e positives, and persisting a complete 8 train / 4 heldout / 4 regression frontier pool under `reports/autogrowth/pools/tg28f_full_foundation_backed_frontier_pool.jsonl`. Result is a full-config bridge-frontier advancement, not broad edge/fence competence: checkpoint pass is true, target pool completed with 16 accepted entries in 103 attempts, cache/live mismatches are 0, foundation sanity is Mate_In_1 1.0 and Mate_In_2 1.0, foundation M3/M4 deltas remain 0 during pool generation/training/eval, heldout bridge selection is 4/4 with 4 reply-envelope foundation-reachable handoffs and 4 same-graph continuations, rook blunders are 0, stalemate avoidance is 1.0, scheduler mismatches are 0, edge-only M3 updates are 432, and bridge-terminal M3 updates are 336. Most causal ablations collapse selection; caveat: disabling reply-envelope checks still leaves 1/4 selected, so the next audit should inspect whether that residual is acceptable graph evidence or leakage-like overreach.
- TG28g full frontier validation and near-miss audit is implemented. `reports/autogrowth/krk_autogrowth_tg28g_full_frontier_validation_near_miss.json` validates TG28f without broadening KRK or unfreezing the TG27b foundation. It also repairs an instrumentation gap: the cache-backed `disable_reply_envelope_foundation_checks` ablation now actually disables reply-envelope queries instead of leaving them active. Result is a validation pass: pool integrity is 16/16 foundation hash matches and 16/16 cache hash matches, train/heldout anchor overlap is 0, mutation-lineage overlap is 0, exact canonical overlap is 0, geometry-bucket overlap is 0, foundation sanity is Mate_In_1 1.0 and Mate_In_2 1.0, cache/live mismatches are 0, foundation M3/M4 deltas during training/eval are 0/0, frontier heldout remains 4/4 selected with 4 reply-envelope foundation-reachable handoffs and 4 same-graph continuations, residual no-reply-envelope selections are now 0, near-miss false positives are 0/8, and generic edge/fence safety is clean with 0 rook blunders and stalemate avoidance 1.0. Generic edge/fence selected only 1/8 and success is 0.125, so this remains a safety/regression slice, not broad edge/fence competence.
- TG28h controlled mixed frontier + generic edge/fence curriculum is implemented. `reports/autogrowth/krk_autogrowth_tg28h_controlled_mixed_frontier_edge_curriculum.json` compares frontier-only, generic-edge-only, and four mixed schedules on one frozen TG27b foundation graph, keeping frontier, near-miss, and generic edge/fence metrics separate. Result is a bounded mixed-curriculum pass, not broad KRK competence: selected schedule is `mixed_balanced`, foundation sanity remains Mate_In_1 1.0 and Mate_In_2 1.0, cache/live mismatches are 0, foundation M3/M4 deltas during training/eval are 0/0, frontier heldout remains 4/4 selected with 4 reply-envelope foundation-reachable handoffs and 4 same-graph continuations, near-miss false positives remain 0/8, and generic edge/fence improves from TG28g's 1/8 selected / 0.125 success to 8/8 selected / 1.0 success with 1.0 confinement improvement, 0.875 black-king mobility reduction, 0 rook blunders, and stalemate avoidance 1.0. Bounded ablations show frontier depends on foundation-response/bridge-pressure/reply-envelope/Mate_In_2/actuator terminals, while generic edge/fence depends on edge/fence/action-delta/internal-attention/safety/actuator terminals.
- TG28i short staged edge/bridge/foundation rollout is implemented. `reports/autogrowth/krk_autogrowth_tg28i_staged_edge_bridge_foundation_rollout.json` runs a bounded sequential test on the same frozen TG27b foundation graph: generic edge/fence move -> black reply -> bridge/frontier move -> black reply -> frozen foundation continuation. Result is a bounded pass, not broad KRK: selected schedule is `tg28h_mixed_balanced_baseline`, foundation sanity remains Mate_In_1 1.0 and Mate_In_2 1.0 with foundation M3/M4 deltas 0/0, frontier slice is 1/1 selected with handoff, generic slice is 2/2 selected with success 1.0 and clean safety, and staged heldout is 1/1 all-reply success with 2 S1 bridge selections, 2 foundation-reachable responses, and 2 same-graph continuations. The staged generator accepted 2/8 candidates, but near-miss and ablations were disabled in this bounded artifact for runtime. The important interpretation is sequential compatibility; staged-training arms did not beat the TG28h baseline. Next step should be a persisted staged-predecessor pool before scaling TG28i/TG28j.
- TG28j persisted staged-predecessor pool is implemented and extended. `reports/autogrowth/krk_autogrowth_tg28j_persisted_staged_predecessor_pool.json` writes a reusable JSONL pool and index at `reports/autogrowth/pools/tg28j_staged_predecessor_pool.jsonl` and `reports/autogrowth/pools/tg28j_staged_predecessor_pool_index.json`, then evaluates from persisted entries. Current result is an infrastructure/staged pass, not full staged scale: the pool has 8 entries split 4 train / 2 heldout / 2 regression / 0 near-miss, all all-reply staged entries bootstrapped by `accepted_entry_mutation` from the validated TG28i artifact plus legal symmetry mutations. Foundation remains frozen with Mate_In_1 1.0, Mate_In_2 1.0, M3/M4 deltas 0/0; staged heldout gets any-reply success 2 with 2 S1 bridge selections and 2 foundation-reachable responses; frontier selected 1; generic edge/fence success is 1.0. Near-miss and ablations remain disabled in this bounded artifact. Forward staged generation is still unresolved; next step should add persisted staged near-misses and restore ablations before claiming scale.
- TG28k staged near-miss + ablation restoration is implemented. `reports/autogrowth/krk_autogrowth_tg28k_staged_near_miss_ablation_restoration.json` extends the persisted staged pool to 10 entries: 4 train / 2 heldout / 2 regression / 2 staged near-miss. The checkpoint passes conservatively: foundation remains Mate_In_1 1.0 and Mate_In_2 1.0 with M3/M4 deltas 0/0, scheduler mismatches 0, near-miss false positives 0, selected schedule remains `tg28h_mixed_balanced_baseline`, staged heldout keeps 2 any-reply successes with 2 S1 bridge selections and 2 foundation-reachable responses, and generic edge/fence success remains 1.0. All 10 ablations are restored. Actuator/action-delta/edge-fence/internal-attention/safety masks collapse staged first selection to 0; disabling reply-envelope, bridge-pressure, foundation-response, or Mate_In_2 foundation quorum leaves the first edge move selectable on the one-position ablation slice but kills staged success. Integrity caveat: all 10 entries match the foundation hash, but the 8 positive TG28j entries retain the older cache-config hash while the 2 new near-misses match the current cache hash; this is recorded in the artifact as mixed cache hash metadata. This is still a bounded staged-runway result, not broad KRK competence.
- TG28l staged pool integrity + modest scale is implemented. `reports/autogrowth/krk_autogrowth_tg28l_staged_pool_integrity_modest_scale.json` rebuilds a new homogeneous staged pool at `reports/autogrowth/pools/tg28l_staged_predecessor_pool.jsonl` and index path `reports/autogrowth/pools/tg28l_staged_predecessor_pool_index.json`. Result: checkpoint pass is true, interpretation `staged_pool_integrity_and_modest_scale_pass`, pool size 24 with split 8 train / 4 heldout / 4 regression / 8 near-miss, single foundation hash true, single cache hash true, mixed cache hash false, generation methods `accepted_entry_mutation: 16` and `forward_filter: 8`, unique start/S1/foundation-query counts 17/24/20, near-miss false positives 0, foundation Mate_In_1 1.0 and Mate_In_2 1.0, foundation M3/M4 deltas during training/eval 0/0, scheduler mismatches 0, and all 10 ablations restored with layered dependency signal true. Staged heldout remains bounded: 2 any-reply successes, 2 S1 bridge selections, 2 foundation-reachable responses, and 2 same-graph continuations. Generic edge/fence stays safe and functional at success 1.0, rook blunders 0, stalemate avoidance 1.0. Important interpretation: staged training still does not improve over TG28h baseline. Baseline gets staged success 2 and frontier selected 1; every staged-training arm gets staged success 1 and frontier selected 0. Treat the staged pool as validation/regression substrate until a stronger training signal is found.
- TG29a tiny online KRK episode runner is implemented. `reports/autogrowth/krk_autogrowth_tg29a_tiny_online_krk_episode_runner.json` runs a first online integration loop over the existing frozen TG27b foundation plus mixed edge/fence and bridge components. White choices are still graph-mediated through the existing foundation/bridge/edge native evaluators; black replies are harness-simulated with deterministic worst-foundation legal replies. Result: checkpoint pass is true, interpretation `tiny_online_components_compose_safely`, selected schedule `mixed_balanced_plus_staged`, 4 episodes, 2 successes via foundation handoff, 2 max-move terminations, 0 checkmates, 0 rook blunders, 0 illegal moves, 0 stalemates, 0 unsafe moves, average 3.25 white moves, phase counts edge/fence 2 and bridge 11, edge->bridge transitions 1, same-graph foundation continuation count 17, foundation M3/M4 deltas during episode eval 0/0, cache/live mismatch count 0 across 8 audited samples, scheduler mismatches 0, and frontier/staged/near-miss/generic regression slices all pass. Caveat: this is not broad KRK competence; no episode reached explicit checkmate, and half the tiny set still hit max move depth.
- TG29b online failure decomposition is implemented. `reports/autogrowth/krk_autogrowth_tg29b_online_failure_decomposition.json` reruns the TG29a tiny episode set with trace-level failure buckets, explicit terminal-transition semantics, reply-policy comparison, and causal ablations. It is a diagnostic pass, not a repair: 2/4 episodes still succeed by foundation handoff, 2/4 still hit max move depth, safety remains clean, foundation stays frozen with M3/M4 deltas 0/0, cache/live mismatch count is 0, scheduler mismatches are 0, and all regression slices pass. The two failures are now classified as `bridge_loop_without_foundation_progress`; both stay in bridge mode for four white moves and never enter a foundation-recognized state after black's deterministic worst-foundation replies. The TG29a `bridge->foundation=0` was a counting-semantics artifact: TG29b counts terminal handoff transitions after black reply and finds `bridge_to_foundation_transition_count=2`.
- TG29b reply-policy comparison sharpens the next blocker. Under deterministic worst-foundation replies, success is 2/4 with 2 bridge-loop failures. Under mobility-maximizing replies, success is 4/4 with 4 foundation handoffs. Therefore the next checkpoint should repair reply-robust bridge-to-foundation progress pressure, not broaden KRK, unfreeze the foundation, or add a new learner mechanism. TG29b also records that deep offline candidate-row recomputation is too expensive inside the trace pass; the canonical artifact uses lightweight runtime evidence for every turn and leaves deep recomputation opt-in via `max_deep_offline_audit_turns`.
- TG29c reply-robust bridge pressure is implemented. `reports/autogrowth/krk_autogrowth_tg29c_reply_robust_bridge_pressure.json` keeps the TG27b foundation frozen and tests a bounded local repair that widens bridge reply-envelope evidence from one reply to two replies and changes only bridge terminal evidence weights for the combined reply-robust arm. Result: checkpoint pass is true as a local repair, not competence. Worst-foundation success remains 2/4 and max-move terminations remain 2/4, but the diagnosed `bridge_loop_without_foundation_progress` failures drop from 2 to 0. The remaining failures are `selected_moves_safe_but_low_progress`. Safety remains clean, foundation M3/M4 eval deltas are 0/0, cache/live mismatches are 0, scheduler mismatches are 0, and frontier/staged/near-miss/generic regressions pass. Ablations are causal for the selected arm: masking reply-robust, bridge-pressure, or foundation-response evidence reduces the repaired behavior. Important caveat: mobility-maximizing success also drops to 2/4 for the selected repair arm, so TG29c traded away some favorable-reply success while removing worst-reply bridge loops.
- TG29c throughput caveat: the full separate-arm matrix (`reply_robust_bridge_pressure`, `bridge_stagnation_veto_only`, `all_reply_worst_reply_confidence_only`, `combined_reply_robust`) and full 12-mask ablation set were too slow. The canonical artifact records a bounded matrix: baseline vs combined repair, worst-foundation plus mobility policies, five causal ablations, and skipped-arm metadata. Next work should not broaden KRK; it should either improve throughput/indexing for reply-envelope bridge evaluation or refine the combined repair so it restores mobility-policy success while keeping bridge-loop failures at 0.
- TG29d online low-progress repair is implemented. `reports/autogrowth/krk_autogrowth_tg29d_online_low_progress_repair.json` tests progress-delta variants on the same frozen-foundation online runway. It is a diagnostic pass, not a repair: no bounded local progress arm improves over TG29c. Selected arm remains `combined_reply_robust`; worst-foundation success is still 2/4, mobility-max success is still 2/4, max-move failures remain 2/4, `selected_moves_safe_but_low_progress` remains 2, and `bridge_loop_without_foundation_progress` remains 0. Safety remains clean, foundation stays frozen, cache/live mismatches are 0, scheduler mismatches are 0, and regressions remain clean. The attempted balanced progress arms have the same tiny-slice outcome as TG29c: 2 successes, 2 low-progress failures, 0 bridge loops.
- TG29d interpretation: simple progress-delta weighting over existing bridge feature keys is not enough. The low-progress failures are now explained as safe but insufficiently goal-directed moves even when some edge/bridge progress counters fire (`meaningful_edge_progress_count=6`, `meaningful_bridge_progress_count=7`, `meaningful_foundation_progress_count=4`). This suggests the missing piece is not another scalar bonus on the same immediate deltas; likely next work needs either better candidate/audit throughput, a reply-robust/progress-positive staged episode pool, or a richer local progress terminal that distinguishes useful progress from harmless-looking shuffling.
- TG29e reply-robust progress-positive pool is implemented. `reports/autogrowth/krk_autogrowth_tg29e_reply_robust_progress_positive_pool.json` writes a persisted candidate pool at `reports/autogrowth/pools/tg29e_reply_robust_progress_positive_pool.jsonl` plus an index. Result is a diagnostic pass, not a repair: 12 low-progress-turn candidates were audited, all 12 were progress-positive by the current reply-envelope/progress classifier, and better progress candidates existed but lost selection in 2 cases. The bounded pool has 16 entries (0 train, 4 heldout, 8 near-miss, 4 regression), so it is not yet a balanced training pool.
- TG29e bounded A-E replay did not repair selection. On the completed 2-episode / 2-white-move diagnostic slice, selected arm remains `combined_reply_robust_baseline`, repair_applied is false, episode success is 0/2, low-progress failures are 2, bridge loops remain 0, and safety is clean. Foundation remains frozen, M3/M4 eval deltas are 0/0, cache/live mismatches and scheduler mismatches are 0, and frontier/staged/near-miss/generic regressions pass. Important throughput caveat: the full 4x4 online arm matrix and post-arm mobility/ablation checks were stopped after CPU-bound runs; TG29e records skipped/empty post checks rather than hiding the cost. Next step should be profiling/indexing and/or a more direct graph-local selection/evidence repair, not more scalar replay tuning.
- TG29f progress-candidate selection repair is implemented. `reports/autogrowth/krk_autogrowth_tg29f_progress_candidate_selection_repair.json` adds generic progress-magnitude feature atoms to edge/fence and bridge terminal evidence (`*_gain_bucket`, `combined_progress_gain_bucket`) and compares baseline, pairwise contrast, dominance-only, low-progress veto, spurious-bridge veto, and combined repair arms. Result is a partial repair signal, not a checkpoint pass: the selected `pairwise_contrastive_progress_evidence` arm flips both TG29e lost turns to the higher-progress candidates (`f6c6` and `e7b7`), with safety clean, foundation frozen, M3/M4 eval deltas 0/0, cache/live mismatches 0, scheduler mismatches 0, and all regression slices passing. But bounded online episodes still get 0/2 success, one low-progress failure remains, one bridge-loop failure appears, and repair_applied remains false.
- TG29f throughput caveat is severe: the bounded 2-turn / 2-white-move / 6-arm run took 6,511 seconds total, with 5,187 seconds in arm evaluation. The current native selection audit repeatedly materializes edge/bridge candidates and reply-envelope checks. Do not scale broad KRK from here; first add per-arm caching/progress logging and make the selection repair translate into episode-level progress or cleanly fail.
- TG29g trajectory-positive prefix audit is implemented. `reports/autogrowth/krk_autogrowth_tg29g_trajectory_positive_prefix_audit.json` audits the two TG29f failed starts by applying bounded safe first-move candidates, simulating deterministic worst-foundation and mobility-maximizing black replies, and then letting the current graph choose the next white move. It also writes `reports/autogrowth/pools/tg29g_trajectory_positive_prefix_pool.jsonl` and its index. The full context/all-safe-candidate audit was stopped as too slow; the canonical artifact is explicitly marked `quick_context` with 6 safe candidates per start, 12 audited candidates, 24 trajectory rollouts, 639 cache/live foundation queries, and 254.39 seconds per audited candidate.
- Current TG29g result is a diagnostic pass, not a repair or competence claim. One trajectory-positive candidate exists and loses selection: on `8/8/5R2/8/1k6/8/4K3/8 w - - 0 1`, current graph selects `f6d6`, TG29f pairwise selects `f6c6`, but the best trajectory-positive move in the bounded audit is `e2d3`. On the second TG29f failed start, no trajectory-positive candidate was found within the bounded six-candidate cap. Foundation remains frozen, Mate_In_1 and Mate_In_2 sanity are 1.0/1.0, M3/M4 eval deltas are 0/0, cache/live mismatches are 0, scheduler mismatches are 0, and purity boundaries remain clean. Optional repair was intentionally skipped in this artifact.
- TG29h cached trajectory selection repair is implemented. `reports/autogrowth/krk_autogrowth_tg29h_cached_trajectory_selection_repair.json` adds a persistent trajectory rollout cache at `reports/autogrowth/pools/tg29h_trajectory_rollout_cache.jsonl` plus index, seeds from TG29g, writes progress after accepted cache entries, and compares small trajectory-vs-local repair arms using cached/materialized candidate evidence. Result is a diagnostic/cache pass, not an applied repair: 60 cache entries, 16 cache hits, 16 live policy rollouts, 16 audited candidates, 2 trajectory-positive candidates, 2 lost trajectory selections, 2 contrast rows, and candidate cap still blocks 28 safe candidates. Average cost drops from TG29g's 254.39 seconds per candidate to 104.05 seconds per audited candidate on this run, but cache key instability still causes misses across context rebuilds.
- Current TG29h correction: the second TG29g failed start is no longer classified as no-candidate within the wider cap. On `8/4R3/8/k7/8/3K4/8/8 w - - 0 1`, the wider audit finds trajectory-positive `d3c3`, while current graph still selects `d3c4`. The first start still prefers trajectory-positive `e2d3` over current `f6d6`. Materialized arm comparison says `combined_trajectory_selection_repair`, `trajectory_positive_dominance_terminal`, and `trajectory_vs_local_contrastive_evidence` can select 1 lost trajectory candidate, but runtime repair episodes were skipped after an earlier repair-evaluation attempt stayed CPU-bound for ~18 minutes with no progress marker. Do not mark repair as applied. Foundation remains frozen, Mate_In_1/Mate_In_2 sanity are 1.0/1.0, cache/live mismatch is 0, scheduler mismatch is 0, and no provider/tablebase/stage-label shortcut is introduced.
- TG29i stable trajectory cache + selection microprobe is implemented. `reports/autogrowth/krk_autogrowth_tg29i_stable_trajectory_cache_selection_microprobe.json` writes a stable semantic cache at `reports/autogrowth/pools/tg29i_stable_trajectory_rollout_cache.jsonl`, using canonical start FEN, first candidate move, black reply policy, frozen foundation/cache/evidence config names, reply cap, audit cap, and audit version rather than run-local graph/context hashes. Result is an infrastructure/diagnostic pass: 32 stable cache entries, stable key mismatches 0, first-pass and second-pass hit rates 1.0, first/second live rollout counts 0/0, cache/live mismatch 0, scheduler mismatch 0, foundation frozen, and compact regressions pass.
- Current TG29i microprobe result: both known trajectory-positive moves are legal, safety-filtered, runtime-present, and actuator-present under the cap-8 microprobe; retrieval is not the blocker for `e2d3` or `d3c3`. Baseline selects neither. Retrieval-only diagnostic inclusion would select both, but this is not a runtime repair. Graph-evidence repair selects `e2d3` on the first failed start, but `d3c3` still loses on the second failed start; failure buckets are `none: 1` and `trajectory_candidate_evidence_materialized_but_too_weak: 1`. Repair remains unapplied because TG29i is a microprobe over materialized graph evidence, not a runtime episode repair.
- TG29j d3c3 trajectory evidence repair is implemented. `reports/autogrowth/krk_autogrowth_tg29j_d3c3_trajectory_evidence_repair.json` performs a selection-margin audit for the second failed start and compares it to the `e2d3` positive control. Diagnosis: `d3c3` loses because trajectory evidence is not materialized in the current graph candidate row (`current_graph_evidence_score: null`, empty bridge feature keys), while selected `d3c4` has strong reply-envelope/bridge evidence. The pre-repair selected-vs-d3c3 margin is +16.63396 in favor of `d3c4`.
- Current TG29j microprobe result is a repair pass at the materialized graph-evidence level, not an episode competence claim. The combined minimal repair materializes generic trajectory-positive/foundation-progress/partial-positive evidence for `d3c3`, adds trajectory-over-local dominance, and applies a small local-progress-only veto to non-trajectory-positive partials. It selects both known trajectory-positive candidates: `e2d3` and `d3c3`; d3c3 margin flips to -2.18604, e2d3 does not regress, ablation proxy is causal, compact regressions pass, cache hit rate remains 1.0, live rollout count remains 0, foundation remains frozen, and no provider/tablebase/stage-label shortcut is introduced. Bounded episode success is still 0/2 in the artifact, so the repair has not yet proven online conversion.
- TG29k runtime trajectory repair integration is implemented as a bounded artifact-only smoke checkpoint. `reports/autogrowth/krk_autogrowth_tg29k_runtime_trajectory_repair_integration.json` wires the TG29j evidence pattern into a runtime materialized-row selection arm using generic trajectory-positive/foundation-progress evidence, trajectory-over-local dominance, local-progress-only veto, and legal actuator terminals. The rule is not a literal `e2d3`/`d3c3` move override; it materializes evidence from cached frozen graph response rows and selects through graph-evidence scoring. Result: before repair the known trajectory prefixes are selected 0/2; after repair `e2d3` and `d3c3` are selected 2/2, safety is clean, causal masks collapse selection, cache hit rate remains 1.0, live rollout count remains 0, and no ActionRanker/runtime tablebase/direct provider/stage-label shortcut is introduced.
- TG29k is not yet a full online-context pass. The canonical artifact has `artifact_only_smoke=true`; a real `--quick-context` run was attempted but did not reach the `context_built` progress marker in a reasonable window and was killed by command pattern. Bounded episode success in the smoke artifact is 0/1 because it is a one-white-move smoke with a fake foundation cache, not a conversion test. Next checkpoint should either make TG29a context construction resumable/cacheable enough to run TG29k full-context validation, or build a narrow real-context harness that reuses the existing TG29j/TG29i frozen artifacts without rebuilding the full schedule stack.
- TG29l real-context runtime trajectory validation is implemented. `reports/autogrowth/krk_autogrowth_tg29l_real_context_runtime_trajectory_validation.json` builds a minimal real context instead of the full TG29a schedule stack: frozen TG27b foundation, frozen foundation response cache, TG29h/TG29i/TG29j/TG29k artifacts, legal actuator/safety checks, and the two known trajectory-positive starts. Context build succeeds in 14.80 seconds at quick scale, with foundation build 10.57 seconds and compact foundation sanity/cache setup 4.23 seconds. Reused artifact count is 10, rebuilt artifact count is 0, frontier/staged/trajectory pool counts are 16/24/32, duplicate cache entries are 0, foundation/cache hash checks are internally consistent, cache/live mismatch is 0, and the full TG29a schedule stack is explicitly skipped as the prior throughput blocker.
- Current TG29l result is a real-context validation pass, not broad KRK competence. In real context, before repair the known trajectory prefixes are selected 0/2 and after repair `e2d3` plus `d3c3` are selected 2/2. Minimal ablations are causal, foundation remains frozen during eval, Mate_In_1 and Mate_In_2 foundation sanity are 1.0/1.0, no provider/tablebase/stage-label shortcut is introduced, and safety is clean. The tiny two-start episode check now gets 1/2 success with 1 max-move failure, 0 rook blunders, 0 illegal moves, and 0 stalemates. Compact frontier/staged/near-miss/generic regressions are skipped by config after the microprobe; next work should inspect the remaining max-move episode after the repaired first move rather than add learning or broaden KRK.
- TG29m post-trajectory second-move handoff audit is implemented. `reports/autogrowth/krk_autogrowth_tg29m_post_trajectory_second_move_handoff_audit.json` diagnoses the remaining TG29l max-move episode: start `8/8/5R2/8/1k6/8/4K3/8 w - - 0 1`, first move `e2d3`, black reply `b4a5`, S1 `8/8/5R2/k7/8/3K4/8/8 w - - 2 2`, old second move `f6e6`. The S1 audit finds 22 legal candidates, 20 safe candidates, 10 audited candidates, and one true bridge-to-foundation candidate. The old `f6e6` is a narrow-envelope false positive: it solves 1/3 replies but fails deterministic worst reply `a5b4`. The good S1 candidate `d3c4` solves all replies under the audited reply set (1/1, worst reply `a5a4`) and had lost selection.
- Current TG29m result is a narrow repair pass, not broad KRK competence. The selected repair arm `second_move_full_reply_foundation_response_priority` materializes local second-move foundation-response evidence for the audited S1 row; it changes the second move from `f6e6` to `d3c4`, moves the S1 failure bucket from `second_move_bridge_candidate_exists_but_lost_selection` to `none`, and improves the two-start max2 check from TG29l's 1/2 to 2/2. Max3 diagnostic is also 2/2. Safety remains clean, foundation eval deltas are 0/0, Mate_In_1 and Mate_In_2 foundation sanity are 1.0/1.0, known trajectory microprobe remains clean, and minimal ablations are causal. No ActionRanker runtime, tablebase/DTM provider, direct override, or learner-visible stage/trajectory label is introduced.
- TG29n S1 full-reply handoff validation is implemented. `reports/autogrowth/krk_autogrowth_tg29n_s1_full_reply_handoff_validation.json` validates the TG29m second-move repair on a 4 train / 4 heldout / 4 near-miss post-trajectory S1 set from TG29m and TG29h. It audits 120 S1 second-move candidates with full legal-reply foundation envelopes, separates all-reply handoffs from partial and one-reply positives, and compares five local evidence arms. The selected arm is `strict_all_reply_priority`.
- Current TG29n result is a validation pass, not broad KRK competence. It finds only 2 all-reply positives, 15 partial positives, and 15 one-reply later-failed candidates, so the old narrow-envelope problem is real. Strict all-reply selection picks 1/4 heldout all-reply candidate where available, selects 0 one-reply later-failed candidates, keeps max2 and max3 known-start rechecks at 2/2, and stays safety-clean with 0 rook blunders, illegal moves, or stalemates. Foundation remains frozen during eval (M3/M4 deltas 0/0), Mate_In_1 and Mate_In_2 sanity remain 1.0/1.0, known trajectory microprobe remains clean, and causal masks collapse full-reply selection. Throughput caveat: the 12-S1 / 120-candidate full-reply audit took 1,858 seconds, so the next checkpoint should add persisted/resumable S1 full-reply evidence or a cheaper cached envelope before scaling.
- TG29o S1 full-reply evidence cache and online recheck is implemented. `reports/autogrowth/krk_autogrowth_tg29o_s1_full_reply_cache_online_recheck.json` persists TG29n's 120 full-reply S1 candidate rows into `reports/autogrowth/pools/tg29o_s1_full_reply_evidence_cache.jsonl` plus index. Cache identity is semantic and stable over S1 FEN, candidate move, foundation/cache hashes, reply-audit hash, reply cap, and audit version. First pass populates from the TG29n full-reply artifact, second pass reuses the cache with hit rate 1.0, live rollout count 0, and cache/live mismatch count 0.
- Current TG29o result is an infrastructure and validation pass, not broad KRK competence. It reuses the same 4 train / 4 heldout / 4 near-miss S1 pool, selects 2 all-reply candidates and 0 one-reply later-failed candidates, keeps known-start max2 and max3 at 2/2, and the small cached larger-online probe is 2/2. Safety remains clean, foundation eval deltas are 0/0, Mate_In_1 and Mate_In_2 sanity remain 1.0/1.0, known trajectory microprobe remains clean, and S1 full-reply cache/foundation-response/actuator/reply-envelope/Mate_In_2 quorum ablations are causal. Runtime dropped from TG29n's 1,932 seconds to TG29o's 54.7 seconds because TG29o does not rerun full legal-reply audits.
- TG29p cached online episode scale matrix is implemented. `reports/autogrowth/krk_autogrowth_tg29p_cached_online_episode_scale_matrix.json` uses TG29o cached S1 full-reply evidence with TG29i trajectory cache evidence in a controlled online matrix across separated start sets, max2/max3/max4 horizons, and deterministic-worst, mobility-maximizing, and fixed-seed-random black replies. The canonical artifact is bounded-scale: 2 known repaired starts plus 1 staged-pool, 1 frontier-near, 1 generic-edge, and 1 near-miss/decoy start, for 54 main episodes. The larger 2/4/4/4/4 quick-context attempt was stopped after the early staged/frontier cases proved too slow for a coherent checkpoint.
- Current TG29p result is a controlled online scale pass, not broad KRK competence. Overall success is 33/54 (0.611), with horizon sensitivity max2/max3/max4 = 0.50 / 0.611 / 0.722 and reply sensitivity worst/mobility/random = 0.556 / 0.444 / 0.833. By start set: known repaired 0.944, staged pool 0.889, frontier near 0.444, generic edge 0.444, near-miss/decoy 0.0. All failures are horizon-limit buckets (`max2`: 9, `max3`: 7, `max4`: 5); safety remains clean with 0 rook blunders, illegal moves, or stalemates. S1 selected all-reply handoffs 18 times and selected 0 one-reply later-failed candidates. Foundation remains frozen during eval (M3/M4 deltas 0/0), trajectory and S1 cache hit rates are 1.0, and no ActionRanker runtime, runtime tablebase/DTM, direct provider override, validator-driven runtime selection, or learner-visible stage/trajectory/S1 labels are introduced.
- TG29p next decision: do not call this broad KRK. The immediate next checkpoint should target horizon-limited failures and partial generalization on frontier/generic starts, while keeping near-miss rejection separate. If scaling again, add profiling/indexing or per-phase progress before increasing the 1-per-category bounded matrix; the TG29p run spent 3,504 seconds in episode evaluation and 994 seconds in diagnostic/regression work.
- TG29q horizon-limited continuation repair diagnostic is implemented. `reports/autogrowth/krk_autogrowth_tg29q_horizon_limited_continuation_repair.json` reuses TG29p's bounded matrix, separates solvable starts from near-miss/decoy starts, reruns the two targeted max4 frontier/generic failures at max4/max5/max6, and audits bounded legal continuations at the final horizon turns. No behavior-changing repair is applied; the repair arms are recorded as diagnostic-only because no materialized continuation-pressure terminal was added.
- Current TG29q result is a diagnostic pass, not a repair. Solvable-start success is 33/45 (0.733) after excluding near-miss/decoy; near-miss/decoy has 9/9 correct rejections and 0 false handoffs. The targeted frontier/generic failures remain 0/6 through max4/max5/max6: the frontier-near start `8/8/8/4R3/5K2/8/8/6k1 w - - 0 1` under deterministic-worst reply and the generic-edge start `8/2R5/8/8/8/8/3K1k2/8 w - - 0 1` under mobility-max reply do not reach foundation handoff by max6. Bounded continuation audit checks 4 late turns and 48 legal candidates, but all 4 audited turns hit candidate-cap/retrieval-blocked classification; no good lost all-reply/partial-reply continuation is proven. Safety remains clean, foundation remains frozen during eval (M3/M4 deltas 0/0), compact regressions pass, and no ActionRanker/runtime tablebase/direct provider/stage-label shortcut is introduced.
- TG29q next decision: do not simply increase horizon and do not claim a continuation repair. The next checkpoint should improve candidate retrieval/throughput and targeted continuation-positive pool coverage for the frontier/generic failure states, then rerun the same labeled max4/max5/max6 diagnostic. A repair is only justified after the audit can see enough legal safe candidates to distinguish `good continuation exists but lost` from `candidate cap/retrieval blocked`.
- TG29r continuation candidate retrieval repair diagnostic is implemented. `reports/autogrowth/krk_autogrowth_tg29r_continuation_candidate_retrieval_repair.json` widens the TG29q blocked-turn audit and persists layered candidate rows to `reports/autogrowth/pools/tg29r_continuation_candidate_retrieval_cache.jsonl` plus index. It audits the four TG29q late turns from the frontier/generic failures across legal, safe, edge, bridge, trajectory, S1 full-reply, foundation-response, and runtime-selectable layers. The continuation-positive label is trainer-side only; no continuation labels are learner-visible and no behavior-changing repair is applied.
- Current TG29r result is a diagnostic pass, not a repair. Across the 4 blocked turns it audits 81 legal candidates, 75 safe candidates, 81 edge candidates, 10 bridge-progressive candidates, 3 foundation-response candidates, and 48 runtime-selectable candidates under the current cap. The widened trainer-side audit marks 81 candidates continuation-positive, with 48 inside the current runtime cap and 33 dropped by cap. Retrieval-blocked is 0, while materialization-blocked is 47. Targeted episode success remains TG29q's 0/6 because no repair arm was applied. Safety remains clean, foundation remains frozen, decoy rejection remains inherited clean, and no ActionRanker/runtime tablebase/direct provider/stage-label shortcut is introduced.
- TG29s continuation evidence materialization and quality tiers is implemented. `reports/autogrowth/krk_autogrowth_tg29s_continuation_evidence_materialization.json` consumes the canonical TG29r retrieval cache and splits the over-broad continuation-positive label into trainer-side evidence tiers. Result is a diagnostic pass, not an applied repair: 81 continuation-positive legal rows reduce to 0 strong continuation candidates, 3 partial continuation candidates, 9 local-progress-only rows, 63 safe-low-progress rows, and 6 unsafe rows. Only the 3 partial rows are treated as materialized continuation evidence candidates; 78 rows remain materialization-blocked or veto/low-progress evidence. Targeted frontier/generic episode success remains TG29q/TG29r's 0/6 because TG29s does not change runtime behavior. Safety remains clean, foundation remains frozen, cache integrity is restored at 648 TG29r rows, and no ActionRanker/runtime tablebase/direct provider/stage-label/continuation-label shortcut is introduced.
- TG29s next decision: do not tune final scoring and do not broaden KRK. The useful signal is that continuation-positive evidence must be quality-tiered before graph materialization; most widened positives are merely safe local progress. The next checkpoint should turn the 3 partial continuation rows plus the local-progress/veto evidence into an actual graph-mediated materialization arm, ablate it, and rerun the same targeted frontier/generic max4/max5/max6 slice while preserving near-miss/decoy rejection.
- TG29t continuation candidate ecology materialization is implemented. `reports/autogrowth/krk_autogrowth_tg29t_continuation_candidate_ecology_materialization.json` over-spawns the TG29r/TG29s population into a bounded candidate ecology cache at `reports/autogrowth/pools/tg29t_continuation_candidate_ecology_cache.jsonl`. Result is a diagnostic pass, not an applied repair: 81 candidates are materialized, 75 are selectable safe candidates, 6 unsafe examples are pruned as hard negative/veto examples, 3 partial continuation candidates mature after repeated causal credit, and 72 low-value candidates enter decay. The 25-cycle ecology records 75 credit events, 1,446 debt events, and 72 decay events. A graph-evidence projection selects 4 continuation candidates, 2 of them mature/credited, but final runtime behavior is unchanged: targeted frontier/generic success remains 0/6 and `repair_applied=false`.
- TG29t interpretation: over-spawning is viable as an ecology diagnostic, but not yet a runtime repair. The useful outcome is lifecycle separation: weak candidates can remain materialized for exploration while debt/decay prevents them from dominating maturity. The current blocker remains downstream behavior: mature candidate evidence exists but has not been installed as a behavior-changing native graph path in final evaluation. Compact frontier/staged/generic regressions were not rerun because final runtime selection was unchanged; the artifact marks this explicitly with `compact_regression_not_rerun_runtime_unchanged=true`. Safety and decoy rejection remain clean, foundation remains frozen, and no ActionRanker/runtime tablebase/direct provider/Python final selector/stage-label/quality-tier learner leak is introduced.
- TG29u candidate ecology runtime path installation is implemented. `reports/autogrowth/krk_autogrowth_tg29u_candidate_ecology_runtime_path_installation.json` installs TG29t mature/credited/decay evidence as generic runtime ecology terminals over the same 81-row candidate population and writes `reports/autogrowth/pools/tg29u_candidate_ecology_runtime_path_cache.jsonl`. Result is a runtime-path installation pass, not a repair: the ecology runtime path is installed, all 3 mature candidates are present before and after, mature selected count rises from 1 before TG29u to 2 after TG29u, and decaying/pruned selected counts stay 0/0. Targeted frontier/generic success remains 0/6, so `repair_applied=false`. Decoy false handoff remains 0, safety remains clean, foundation remains frozen, and no ActionRanker/runtime tablebase/direct provider/Python final selector/stage-label/quality-tier learner leak is introduced.
- TG29u interpretation: runtime wiring is no longer the immediate blocker for the frontier-near mature candidates. Mature candidates can now win the bounded runtime ecology path, but selected mature evidence still does not improve the targeted max4/max5/max6 episode outcome. The next blocker is post-selection sufficiency: whether the selected ecology moves fail because the horizon remains too short, the foundation basin is still too narrow, or the mature candidates are only locally useful. Compact frontier/staged/generic regressions are still inherited/reused because this checkpoint did not run broad episode replay.
- TG29v mature candidate post-selection sufficiency audit is implemented. `reports/autogrowth/krk_autogrowth_tg29v_mature_candidate_post_selection_sufficiency_audit.json` follows the two mature candidates selected by TG29u and writes `reports/autogrowth/pools/tg29v_mature_candidate_followup_cache.jsonl`. Result is a diagnostic pass, not a repair: `g4g2` is classified as reply-policy-fragile under black reply `e1d1` but it creates a follow-up state with one follow-up candidate; `f4e3` is classified as horizon-insufficient/foundation-basin-missed under black reply `d1c1`. Follow-up exists/selected/succeeds for 1/1 audited follow-up, but targeted frontier/generic success remains 0/6 because no repair is applied. Safety and decoy rejection remain clean, foundation remains frozen, and no provider/selector/stage/continuation/quality/depth-label leak is introduced.
- TG29v interpretation: mature candidate selection is not pure false maturity. The first mature move can be useful but is reply-fragile and needs the next continuation; the second selected mature move reaches a state outside the current bounded foundation basin/horizon. Suggested next repair candidate is reply-robust maturity or follow-up ecology materialization, but the audit did not apply it. The next checkpoint should choose one narrow repair: either require stronger reply-envelope support before mature candidates dominate, or spawn/materialize follow-up continuation ecology from the after-mature-reply state and test whether the full chain improves max4/max5/max6 without decoy leakage.
- TG29w reply-robust mature follow-up repair diagnostic is implemented. `reports/autogrowth/krk_autogrowth_tg29w_reply_robust_mature_followup_repair.json` consumes the TG29v follow-up cache, materializes generic reply/follow-up runtime evidence, and writes `reports/autogrowth/pools/tg29w_reply_robust_followup_runtime_cache.jsonl`. Result is a diagnostic pass, not a behavior repair: the cache has 2 selected mature rows; `g4g2` remains reply-fragile but useful with one selected/successful follow-up, while `f4e3` remains a foundation-basin miss with no follow-up candidate. Follow-up ecology materializes 2 rows, selected follow-up/success is 1/1, targeted frontier/generic success remains 0/6, max7/max8 diagnostics remain 0.0/0.0 because the basin miss persists, and `repair_applied=false`.
- TG29w interpretation: reply/follow-up evidence is now represented as graph-consumable runtime evidence, but it is not sufficient to change the bounded online outcome. The immediate blocker is no longer mature-candidate selection or follow-up cache plumbing; it is live chain sufficiency and foundation-basin coverage after the selected mature/follow-up moves. Decoy rejection and safety remain clean, foundation remains frozen, and no ActionRanker/runtime tablebase/direct provider/Python final selector/stage/trajectory/S1/continuation/quality/depth/reply-policy learner leak is introduced.
- TG29x live chain-sufficiency and foundation-basin boundary audit is implemented. `reports/autogrowth/krk_autogrowth_tg29x_live_chain_sufficiency_basin_boundary_audit.json` reconstructs the two TG29w/TG29v mature-chain traces, verifies cached FEN transitions with live `python-chess` move application, audits frozen foundation/basin boundary states, and writes `reports/autogrowth/pools/tg29x_live_chain_sufficiency_cache.jsonl` plus `reports/autogrowth/pools/tg29x_foundation_basin_boundary_pool.jsonl`. Result is a diagnostic pass, not a repair: chain traces are 2, mature+follow-up chains are 1, chain cache/live mismatch is 0, foundation cache/live mismatch is 0, targeted frontier/generic success remains 0/6, and `repair_applied=false`.
- TG29x interpretation: the blocker is not cache validity or missing mature/follow-up cache wiring. The `g4g2 -> f4e3` chain has partial frozen foundation response, but the reply envelope still produces basin-miss states; the `f4e3` mature state has no cached follow-up candidate. TG29x records `followup_success_metric_too_weak_count=1`, `chain_misses_basin_count=2`, and a basin-boundary pool with 6 states. The next work should tighten what counts as follow-up success and/or run a frozen-foundation basin-coverage diagnostic over the TG29x boundary pool. Do not add another follow-up layer blindly, broaden KRK, or unfreeze TG27b foundation in the main arm.
- TG29y tight follow-up success and frozen foundation basin coverage is implemented. `reports/autogrowth/krk_autogrowth_tg29y_tight_followup_success_basin_coverage.json` consumes TG29x's chain/boundary caches, TG29w follow-up runtime evidence, TG29t/TG29u ecology caches, TG29o S1 full-reply cache, and TG29r retrieval rows. It writes `reports/autogrowth/pools/tg29y_frozen_foundation_basin_boundary_pool.jsonl`. Result is a diagnostic pass, not a runtime repair: the old follow-up success count drops from 1 to 0 under tightened criteria, false follow-up success is 1, boundary pool entries are 6, partial-support boundary states are 3, outside-frozen-basin states are 3, targeted frontier/generic success remains 0/6, and `repair_applied=false` with selected arm `boundary_pool_only`.
- TG29y interpretation: TG29w's old follow-up success was too weak. Partial one-reply foundation response is now treated as weak/reply-fragile support, not full success. The remaining issue is frozen foundation basin coverage: current boundary states have no all-reply foundation/quorum support, and the outside-basin states lack foundation response, quorum, S1 full-reply, bridge pressure, and shared-atom evidence. TG29y did not unfreeze TG27b, did not use a shadow child in main eval, and introduced no provider/selector/stage/continuation/quality/depth/reply-policy/basin-label leak.
- TG29z frozen-parent child foundation basin coverage diagnostic is implemented. `reports/autogrowth/krk_autogrowth_tg29z_child_foundation_basin_coverage_diagnostic.json` evaluates the frozen TG27b parent over TG29y's 6 boundary entries, then creates a separate diagnostic child branch over an explicit 3 train / 2 heldout / 1 regression split and writes `reports/autogrowth/pools/tg29z_child_foundation_boundary_coverage_pool.jsonl`. Result is a diagnostic pass, not child coverage success: parent recognizes 0/6 all-reply boundary states, has 3 partial-support states and 3 outside-basin states; the child branch recognizes 2/3 train entries, 0/2 heldout, and 0/1 regression. Child coverage rate is 0.333333, heldout coverage is 0.0, child false positives and decoy false handoffs are 0, TG27b parent M3/M4 deltas remain 0, and the child is not used in main runtime.
- TG29z interpretation: the child branch learns only train-specific partial-boundary signatures. This is not evidence for clean basin expansion. The boundary pool is too small/specific, and the blocking evidence families remain S1 full-reply, bridge pressure, foundation response, quorum, and shared atoms. Next work should generate a larger, lineage-preserved boundary pool or improve the missing evidence families before returning to online selection repair. Do not adopt the child branch into the main arm.
- TG30 boundary dataset expansion and child coverage ladder is implemented. `reports/autogrowth/krk_autogrowth_tg30_boundary_dataset_expansion_child_coverage_ladder.json` expands the TG29y/TG29z boundary evidence into 80 lineage-preserved entries and writes `reports/autogrowth/pools/tg30_expanded_foundation_basin_boundary_pool.jsonl` plus `reports/autogrowth/pools/tg30_child_foundation_boundary_coverage_pool.jsonl`. The split is group-disjoint with 32 train, 16 heldout, 16 regression, and 16 decoy entries; all 80 canonical FENs are unique. The expanded pool contains 32 partial-support boundary states, 32 outside-frozen-basin states, and 16 clean decoys. The frozen parent recognizes 0 all-reply boundary states and has 32 partial-support states.
- TG30 interpretation: the selected shadow child arm is `child_boundary_plus_shared_atoms`; it recognizes 8/16 heldout and 8/16 regression boundary entries with 0/16 decoy false handoffs. This is useful nonzero heldout boundary recognition, but it remains shadow-only partial boundary recognition, not all-reply foundation competence and not main-arm runtime improvement. TG27b remains frozen, the child is not used in main runtime, and no provider/selector/stage/trajectory/S1/continuation/quality/depth/reply-policy/basin-label leak is introduced. Next work should test whether the shared-atom child signal can be made robust through graph-mediated all-reply/quorum/S1/foundation-response coverage, or run a deeper shadow online sufficiency audit. Do not adopt the child branch into the main arm yet.
- TG31 long-run child boundary coverage scale and shadow stability is implemented. The canonical preferred-scale artifact `reports/autogrowth/krk_autogrowth_tg31_child_boundary_coverage_scale_shadow_stability.json` expands TG30's 80-entry pool to 512 unique, lineage-preserved entries and writes `reports/autogrowth/pools/tg31_scaled_foundation_basin_boundary_pool.jsonl`, `reports/autogrowth/pools/tg31_child_foundation_boundary_coverage_pool.jsonl`, `reports/autogrowth/pools/tg31_child_arm_results.jsonl`, and `reports/autogrowth/krk_autogrowth_tg31_shadow_child_online_stability_matrix.json`. Splits are group-disjoint: 192 train, 128 heldout, 96 regression, and 96 decoy. The frozen parent still recognizes 0 all-reply boundary states and has 208 partial-support states.
- TG31 interpretation: the five-seed, 14-arm ladder selects `child_boundary_plus_foundation_response`, not TG30's `child_boundary_plus_shared_atoms`, with 22/128 heldout recognition, 10/96 regression recognition, worst-seed heldout coverage 0.09375, and 0 decoy false handoffs. Shadow online is run only as a diagnostic and reports 5 targeted shadow successes; the child remains out of main runtime and TG27b remains frozen. This is stability evidence for a shadow boundary signal, but still not all-reply foundation competence and not a main-arm repair. The run finished quickly because the diagnostic arms completed from cached/persisted evidence; the artifact records `long_run_short_finish_reason=all_arms_completed_early`.
- TG32 child boundary active learning and shadow stress is implemented. The canonical long-mode artifact `reports/autogrowth/krk_autogrowth_tg32_child_boundary_active_learning_shadow_stress.json` expands through tier 3 to 4,096 unique, lineage-preserved entries and writes `reports/autogrowth/pools/tg32_active_foundation_basin_boundary_pool.jsonl`, `reports/autogrowth/pools/tg32_child_foundation_boundary_coverage_pool.jsonl`, `reports/autogrowth/pools/tg32_child_arm_results.jsonl`, `reports/autogrowth/pools/tg32_hard_decoy_pool.jsonl`, and `reports/autogrowth/krk_autogrowth_tg32_shadow_child_online_stress_matrix.json`. Splits are group-disjoint: 1,536 train, 1,024 heldout, 768 regression, and 768 decoy. TG32 mines 384 hard decoys and 384 child-confusable decoys, runs 10 seeds and cycle tiers up to 1,000, and keeps the frozen parent at 0 all-reply recognized boundary states.
- TG32 interpretation: the selected shadow arm is `child_boundary_combined_minimal`, with 141/1,024 heldout recognized, 87/768 regression recognized, 0 decoy false handoffs, and 0 hard-decoy false positives. Shadow stress reports 12 targeted successes and positive child-boundary/online-success correlation, but this is still shadow-only. TG27b remains frozen, the child is not used in main runtime, and no provider/selector/stage/trajectory/S1/continuation/quality/depth/reply-policy/basin-label leak is introduced. The run finished quickly despite tier-3 stress; the artifact records `long_run_short_finish_reason=high_tier_diagnostic_completed_fast_not_true_wall_clock_long_run`.
- TG33 controlled experimental child runtime A/B is implemented. The canonical long-mode artifact `reports/autogrowth/krk_autogrowth_tg33_experimental_child_runtime_ab.json` compares parent-only, child-shadow, and five experimental child runtime branches while keeping TG27b frozen and main runtime parent-only. It writes `reports/autogrowth/pools/tg33_branch_online_results.jsonl`, `reports/autogrowth/pools/tg33_child_intervention_log.jsonl`, `reports/autogrowth/pools/tg33_hard_decoy_stress_results.jsonl`, and `reports/autogrowth/pools/tg33_live_cache_equivalence_samples.jsonl`. The run completes 5,000 online A/B episodes, 347 child interventions, 1,869 decoy/hard-decoy stress rows, and 55 live/cache equivalence samples with 0 cache mismatches.
- TG33 interpretation: the selected experimental branch is `experimental_child_consensus`. Parent-main baseline gets 201 successes, the selected experimental child branch gets 276 successes, for a +0.105436 success-rate delta. Child interventions help 207 times, hurt 0 times, and produce 0 decoy or hard-decoy false handoffs. This confirms experimental-branch runtime value inside the TG33 harness, but it is still not main adoption: child use is explicitly limited to experimental branches, TG27b remains frozen, parent artifacts are untouched, and no provider/selector/stage/trajectory/S1/continuation/quality/depth/reply-policy/basin-label leak is introduced. The run finishes quickly and records `long_run_short_finish_reason=high_tier_online_stress_completed_fast_not_true_wall_clock_long_run`.
- TG34 paired child-consensus canary stress is implemented. The canonical artifact `reports/autogrowth/krk_autogrowth_tg34_paired_child_consensus_canary_stress.json` promotes TG33's `experimental_child_consensus` into named canary branches for adoption-readiness testing only, not main runtime adoption. The committed long-mode run uses tier-1 paired stress because higher tier produced oversized JSONL artifacts: 20,000 paired episode keys, 140,000 branch rows, 50 seeds, 1,000 live/cache samples, and the six TG34 JSONL logs. Parent TG27b stays frozen and main runtime stays parent-only.
- TG34 interpretation: selected canary is `experimental_child_consensus_canary_balanced`. Parent succeeds 5,678 times, TG33 experimental succeeds 7,581 times, and the selected canary succeeds 7,581 times, for `canary_success_delta_vs_parent=0.09515`. Paired help/hurt is 1,903/0, net help is 1,903, decoy and hard-decoy false handoffs are 0/0, and live/cache mismatches are 0. Classification is `canary_adoption_ready_next`, but only for a later named experimental adoption branch. It still finished quickly with `long_run_short_finish_reason=paired_canary_stress_completed_fast_below_long_wall_clock`.
- TG35 feature-flagged child-consensus runtime canary is implemented. `reports/autogrowth/krk_autogrowth_tg35_feature_flagged_child_consensus_runtime_canary.json` installs an explicit runtime policy surface with default `parent_only` and feature-flagged `child_consensus_canary_balanced` / `child_consensus_canary_failclosed` policies. Default/main runtime remains parent-only; the child can affect behavior only under the named experimental policy. TG35 also applies artifact hygiene: all new detailed logs are `.jsonl.gz`, the largest new TG35 artifact is 538,884 bytes, and the inherited TG34 65 MB log is reported as an oversized warning rather than rewritten.
- TG35 interpretation: classification is `feature_flagged_canary_ready`, still not main adoption. The canonical long-mode tier-1 run has 20,000 paired episode keys, 100,000 branch rows, 50 seeds, and 5,000 live/cache samples. Parent succeeds 5,678 times and canary succeeds 7,581 times, for `canary_success_delta_vs_parent=0.09515`. Paired help/hurt is 1,903/0, parity mismatches against TG34 harness are 0/0/0 for selected move/outcome/gate, decoy and hard-decoy false handoffs are 0/0, live/cache mismatches are 0, rollback/fail-closed tests pass, and parent TG27b remains frozen.
- TG36-TG38 runtime stage-gate campaign is implemented. `reports/autogrowth/krk_autogrowth_tg36_tg38_runtime_stage_gate_campaign.json` runs TG36 packaging validation, TG37 KRK stage-play ladder, and TG38 failure-driven classification. The campaign keeps default runtime `parent_only`, keeps the child out of main/default runtime, keeps TG27b frozen, and writes only compressed detailed stage-play logs. TG36 passes packaging/parity/rollback/fail-closed checks. TG37 runs 50,000 paired stage-play episodes across known repaired, staged, frontier, generic edge, boundary-derived, mixed controlled KRK, broad-probe, and decoy families with horizons max4-max16 and five reply policies. TG38 selects `default_off_canary_stage_play_package`.
- TG36-TG38 interpretation: campaign pass, not main/default adoption. Parent stage-play success rate is 0.31144, canary success rate is 0.38534, delta is +0.0739, paired help/hurt is 3,695/0, decoy and hard-decoy false handoffs are 0/0, live/cache samples are 10,000 with 0 mismatches, safety remains clean, and regressions pass. The run completes quickly with `overnight_budget_used_reason=tg36_tg37_tg38_completed_fast_with_clear_next_action`; it did not consume the 8-10 hour wall-clock budget because all requested phases reached a clear next action.
- `docs/autogrowth/SCAFFOLD_CORRECTION_NOTE.md` is the standing correction note. Treat TG26l/TG26m as scaffolded and cheating for autonomy claims because they depended on harness-orchestrated Mate_In_1 handoff, even though their terminal learning and diagnostics remain useful.
- Suggested next step: package the canary as a default-off experimental stage-play mode with user-facing docs and a runtime switch, while adding a stronger live execution audit beyond the current cache-backed harness. Do not merge child into default/main runtime.

## Current Architecture Guardrail

- Reuse `src/recon_lite_hector/nodes/stem_cell.py`, `src/recon_lite_hector/learning/m5_structure.py`, and `src/recon_lite_hector/nodes/pack_template.py` before adding new lifecycle concepts.
- Do not reduce topological growth to triplets only. Valid candidate nodes/subgraphs include stand-alone input terminals, local sensor-composition terminals, and small circuits built from generic primitives, as long as behavior-changing use is mediated through ReCoN graph structure.
- Shared sensor implementations are allowed as a training/runtime performance shortcut only when semantically equivalent to each consuming SCRIPT owning its own TERMINAL instance; saved topology and trace audit should make that equivalence explicit.
- LAG terminals are important future primitives because they add temporal resolution: they can compare a sensor with one or more previous ticks and let local circuits detect change, persistence, low-pass filtered state, or derivative-like movement.
- Keep relevance separate from valence. Relevance can keep a candidate alive; causal intervention credit is required for maturity/promotion.
- Disable or label as non-causal for autonomy claims: KRK box-method discovery, forced hoisting, perfect/survivor/extreme-failure/sample bypasses, random fallback credited as success, selector/arbiter special modes, and runtime tablebase/DTM.
- Before claiming KRK structural growth, verify the KRK online path runs evolved registry/topology causally rather than rebuilding a fresh demo graph that ignores candidates.

## Current No-Go List

- More readiness packets.
- More selector-runtime work.
- More Stage7/Stage8 patching.
- Report row IDs or stage labels as learner features.
- Runtime tablebase/DTM as a move provider.
- Direct provider override as the growth mechanism.

## Reporting Rule

One run should produce one machine-readable result artifact and, when useful, one short markdown summary. If a report does not summarize a run or decision that changes the next experiment, do not write it.
