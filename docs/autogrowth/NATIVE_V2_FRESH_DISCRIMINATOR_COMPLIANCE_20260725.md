# V2 fresh discriminator — pre-execution compliance matrix

| Binding requirement | New package owner | Fail-closed evidence before discovery |
|---|---|---|
| Protected V2.1 and atomic harness unchanged | source-boundary verifier | exact three SHA-256 values |
| New namespace/salt/seeds/paths | outer frozen manifest | no retired identifier or artifact path |
| Physically disjoint 80-row ecology | denylist + canonical enumerator | zero FEN, transition, outcome-tuple or physical-fingerprint overlap; no alternate search |
| Legal truthful chess transitions | import-stable opaque graph + environment manifest | one legal graph action; actual successor terminal equals frozen manifest |
| Opaque content-blind discovery | native request/error genome and StemCell lifecycle | no proposal enumeration/insertion API in runner |
| Prefix-only targets and fixed polarity | nomination escrow and deterministic selector | suffix unopened; missing targets retained as `ABSENT` |
| Exact A/B/C identities | persist-once semantic snapshot manifest | explicit common and lawfully differing identity maps; enum values only |
| No suffix topology growth | frozen structural contract | candidate/topology/escrow/frontier identities unchanged after every event |
| Registry-owned exposure | unchanged V2.1 registry | exact 32 wrappers per arm and complete registry result association |
| Original >=24/32 admission | experiment-local target gate | both targets >=4 distinct physical opportunities in every arm |
| Outcome capability isolation | durable mint capability | exposure/open/parity have no capability; one durable access per truthful receipt |
| Global all-arm preflight | unchanged atomic harness + exact outer binding | exact expected receipt digest and complete 96-row coverage before capability creation |
| Atomic tri-arm execution | unchanged executor + science adapter | seed-major row-major A/B/C barriers; no partial live state |
| Durable scientific evidence | experiment-local carrier bound into COMMITTED | every row/seed/final snapshot path, hash and semantic identity verified |
| Crash-only reconstruction | COMMITTED-record aggregator | byte-identical endpoints, paired tests and verdict without event replay |
| Frozen primary estimands | all-32 adjudicator | only `D_safe` and `D_signal`; exact sign tests; Holm family two |
| One serial bounded package | CLI phase gates | no multiprocessing, optimization, suffix reuse, fresh KRK, R1 or held-out loaders |
| Stop before fresh learner execution | pre-data closure command | no prefix artifacts, fresh receipts, snapshots or science journal exist |

Predeclared implementation:

- `src/recon_lite_chess/autogrowth/native_v2_fresh_discriminator.py`;
- `tests/autogrowth/test_native_v2_fresh_discriminator.py`.

Predeclared package root:

`reports/autogrowth/native_authority/v2_fresh_discriminator/`

The source freeze contains the retired actual-V2 canary. Only after that commit
is pushed may the package derive seeds and construct the denylist, ecology,
permutation, environment, source-runtime and outer manifests. Canonical prefix,
exposure, snapshots, journal, seed results and aggregate paths are declared but
must remain absent at this package's stop.
