# Native R0 Child-Availability Diagnostic Design

Date: 2026-07-16. Status: frozen retired-data engineering diagnostic. Commit
`5b6d2c1` and its negative result remain immutable evidence.

## Hypothesis and strongest null

Hypothesis: perfect local knowledge of whether R0's own formally selected action
actually confirms R0's declared completion goal is sufficient for the native
all-replies handover to select `d8c8` and convert the already-retired R1 row.

Strongest null: even this policy-success mask cannot make the fail-closed
all-replies/choice graph select and convert the row. If so, stop at graph choice,
all-reply composition, or R0 policy coverage; do not start competence learning.

## Information boundary

The deployed contract distinguishes POLICY_RESPONSE, AVAILABLE, VALUE, and
GROUNDING. Raw policy actuation never implies AVAILABLE. Exploitative action
options are `AND(legal actuator, ALL_REPLIES_AVAILABLE)`. A separate anonymous
graph exploration SCRIPT owns action choice when no exploit option confirms.

Perfect policy success, any-action success, shuffled success, and the old host
prototype gate are laboratory measurements only. They may not enter deployed
inference, weights, spawning, credit, maturity, or learning.

## Frozen masks

1. Any policy response exists.
2. R0 policy success after executing R0's own selected action on a diagnostic
   real clone and formally evaluating its declared completion terminal.
3. Any-action-success upper bound over legal R0 actions.
4. Shuffled policy-success mask preserving the 65-value multiset.
5. Disconnected child.

The old prototype gate is recorded separately as a laboratory upper bound and is
never connected to runtime.

## Predicted outcomes and gates

- Mask 1 remains nonselective.
- Mask 2 selects `d8c8` and converts.
- Mask 3 distinguishes child policy coverage from competence applicability.
- Masks 4 and 5 do not reproduce conversion.
- One unavailable reply formally eliminates its parent option.
- No qualified exploit option emits zero exploit actuators; graph-owned
  exploration is reported separately.
- Host fallback and dream mutation remain zero; R0 retention remains 32/32.
- R1 spawning and credit are untested unless learned R1 topology is connected to
  evaluation.

## Budget, kill rule, and transfer

Use exactly the already-retired 65 successors, 32 retained R0 rows, and existing
touched gate-decoy pools. Do not tune a threshold on the R1 row. Stop immediately
if policy-success cannot convert. If it converts, write—but do not execute—a
preregistered design for a graph-native learned competence envelope. No fresh
data, final pool, R2, or automatic competence training is authorized.
