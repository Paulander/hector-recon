# Contradiction-triggered one-level specialization preregistration

Date: 2026-07-19. Engineering/development data only. This package uses the 32
original corrected competence envelopes and the already-viewed 32-row validation
tape in its frozen order. It makes no fresh, regression, R1, or retired-65 claim.
The closed mature-cell falsification verdict and artifacts remain immutable.

## Question and objective

Can a contradicted mature competence abstraction recover its valid region by
growing one narrower descendant while preserving zero false AVAILABLE authority?
The objective is lexicographic: first require zero FP, then maximize deployable
TP. Per organism, `safe_tp = TP if FP == 0 else 0`; true positives from an
organism with any false AVAILABLE output do not count as safely deployable.
Safe-narrow and strict organism counts are reported separately.

## Engineering admission

The append-only `continuation_manifest.v2` preflight passed before viewed-data
access. It includes full evidence payloads, lifecycle/cell state, member specs,
continuation and specialization counters, audits, and graph snapshot. Mutation
negative tests and exact next-observation continuation equivalence pass. The
synthetic canary proves AVAILABLE and REFUTED symmetry, retained-evidence child
maturity, parent fail-closed behavior, impurity rejection, one request only,
virtual mutation rejection, and runner identity-authority closure.

The learner-invisible aggregate ceiling then found 918 safe support-qualified
one-level descendants across the cohort. Their oracle union was 313 TP, 0 FP,
31/32 safe-narrow, and 14/32 strict. No candidate identity left the ceiling.
This exceeds demotion-only 119 TP and 17 safe-narrow at zero FP, so learner
admission passed.

## Frozen mechanism

On the first unique graph-local contradiction that sends a depth-zero MATURE
cell to PROBATION, the same confirmed graph lineage receives exactly one
specialization opportunity. The parent retains identity, polarity, evidence,
members, and structural context, cannot remature, and has no direct authority.
The existing hash genome selects one graph-confirmed generic base terminal. The
child is exactly `AND(context:probation_parent, base_terminal)`, inherits
polarity, begins TRIAL, and uses the unchanged real ledger, support 4, Wilson
lower bound 0.55, zero-opposite contract, capacities, and genome seed. It has no
retry, grandchild, direct maturity, or virtual evidence.

Local contrast eligibility counts prior parent-matching evidence of the parent
polarity and excludes the current opposite-outcome terminal set. The blind
control uses the same supporting vocabulary without that exclusion. Implied
parent members plus policy, completion/outcome, experiment/row/identity, and
chess-semantic labels are excluded. Eligibility is an internal-terminal state;
the runner cannot supply a parent, child, or candidate identity.

## Arms and freeze

All arms clone the same source envelope per ordinal, consume the same rows in the
same order, and retain the source genome seed:

1. `local_contrast_specialization`: connected graph correction and local contrast.
2. `demotion_only`: exact mature-falsification correction with proposal edge disconnected.
3. `counterexample_blind_specialization`: identical request, genome, proposal
   count, capacity, and lifecycle dose using contradiction-blind vocabulary.

The manifest generated from the pushed preregistration commit freezes all 32
ordinals/seeds/source artifacts, source and runner hashes, reference-row order,
arm mapping, genome, thresholds, capacities, and lifecycle laws before replay.
There are no retries, seed exclusions, selected organisms, or post-result edits.

## Gates

Integrity requires: 32 organisms and 96 arms; exact demotion-only reproduction
of 119 TP, 0 FP, 17 safe-narrow, and one strict; A/C request-opportunity and
proposal-attempt parity; transitioned/requested parent IDs only from confirmed
graph legs; no direct PROBATION/TRIAL authority; only mature child authority;
all V2 restore manifests exact; source-cell preservation keyed by ordinal, cell
ID, and recursive pattern fingerprint; 32 paired rows per arm; depth exactly one;
and no forbidden data or virtual mutation.

Arm A passes scientifically only if integrity passes, FP=0, TP>119, safe-narrow
>=17/32, deployable TP exceeds both B and C, safe-narrow exceeds C, and at least
one child matures through ordinary real evidence. If A improves B but ties C,
specialization works but local contrast is unproven. Any A FP fails safety. If
the ceiling passes but no child matures, nomination/lifecycle fails at this
dose. No threshold, proposal count, member law, or seed may change after replay.

The package closes after adjudication regardless of outcome. No regression, new
pool, second refinement, voting, replay, competition, terminal-trace repair, R1,
or virtual grounding follows automatically.

