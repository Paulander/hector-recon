# Native mature-cell falsification preregistration

## Question and strongest null

Question: can graph-local contradictory real outcomes revoke an overbroad
mature competence cell's authority while preserving mature cells that remain
consistent?

Strongest null: any safety improvement is explained by removing cells without
regard to local responsibility. The frozen shuffled-responsibility arm tests
that null at identical transition count and evidence dose.

## Single changed factor

The only scientific factor is revocable maturity. Existing `PROBATION` is
reused. A MATURE cell whose own context and cell-local prediction-error terminal
both confirm on a unique REAL outcome becomes PROBATION immediately. PROBATION
retains identity, members, polarity, lineage, evidence, and statistics, remains
measurable for contradiction telemetry, but emits no top-level AVAILABLE or
REFUTED authority and cannot remature while contradictory evidence remains.

No child refinement, descendant growth, renewable proposal budget, capacity,
threshold, feature, topology-growth, proposal-counter reset, terminal-trace
repair, soft voting, confidence decay, host arbitration, R1, regression, retired
65 successors, or fresh data is permitted.

## Information and authority boundary

Every unique executed REAL outcome updates each matching cell's existing
evidence statistics. Inactive contexts receive nothing and duplicate evidence
keys are idempotent. AVAILABLE plus noncompletion and REFUTED plus completion
are polarity-symmetric contradictions.

The connected correction graph contains, for every MATURE or PROBATION cell:

```text
AND(cell context confirms, cell-local prediction error confirms)
```

Python may transition only cell identities emitted by confirmed graph legs.
The connected API accepts no responsible-cell identity, expected class, chess
predicate, validation label, or Boolean correction decision. Virtual frames and
dream sessions cannot call the real-observation mutation path. The shuffled arm
is an explicitly laboratory-only identity permutation and is unavailable to
organism inference.

Safe-pattern identities are never learner inputs. A post-run read-only check may
identify validation-safe mature member specifications solely to adjudicate the
preservation checksum.

## Source and tape

- source commit: `cebe92a5651ba293acd26bced22e39f7c19e4417`
- corrected source artifact SHA-256:
  `9325e5d076e328f85fe6b8dbb495d5f26ae3f503156e38463912972fa766b1a3`
- preserved V3C artifact SHA-256:
  `5ec16c0a775ec14ceb3d1daf3952a8944a4a298daa0648566ef06a8036f50bbb`
- frozen R0 SHA-256:
  `bb58b7d64bd3ab5b696713a7253555e051bd0e9fdef4637db7c27e7517495eaf`

Only the already-viewed 32-row V3C validation tape is used, in committed order.
Regression never opens. Before adaptation, actual R0 execution must reproduce
the committed action/signal/outcome rows exactly, and inference-only baseline
must reproduce 313 TP, 39 FP, and 6/32 safe-narrow organisms.

## Arms

All 32 corrected connected organisms are retained and independently cloned.

1. `local_responsibility`: confirmed graph-local contradictions transition the
   emitted MATURE identities to PROBATION.
2. `shuffled_responsibility`: the same graph emissions and evidence occur, but
   a cryptographically frozen within-organism permutation receives the same
   unique transition effects. This is a laboratory control only.
3. `immutable_maturity`: identical inference, actions, outcomes, evidence, and
   correction telemetry occur with lifecycle disconnected.

The complete permutation manifest, master seed digest, source hashes, and all
32 mappings must be committed and pushed before adaptation. No retry, seed or
organism exclusion, permutation tuning, cell selection, or ensemble is allowed.

## Prequential protocol

For each organism, arm, and row: classify before outcome access; execute the
frozen R0 graph-owned action; observe its real completion terminal; insert one
canonical evidence record; request graph-local correction; apply only the
arm-authorized effect; and persist state/provenance hashes. After all 32 rows,
serialize/restore the organism and replay the viewed tape inference-only.

## Frozen integrity checksums

The local arm must produce exactly:

- 37 unique MATURE to PROBATION transitions;
- 47 cell-local contradiction hits;
- 119 TP and 0 FP after adaptation;
- 17/32 safe-narrow organisms;
- 1/32 strict organisms;
- all 21 activated validation-safe mature specifications retained as MATURE.

No supporting or inactive cell may be demoted. The immutable arm must remain
exactly 313 TP, 39 FP, and 6/32 safe-narrow. Shuffled must have exact transition
and contradiction-hit parity. Any mismatch is an implementation/instrument
abort and closes the package without rescue.

## Scientific comparison and stop

Conditional on integrity, local responsibility must finish with fewer false
positives, more retained TP coverage, and more safe-narrow organisms than the
shuffled control. This is the responsibility-specific scientific gate.

Every result artifact reports `integrity_passed`, `mechanism_passed`, and
`scientific_passed` separately; no ambiguous top-level `passed` field is
allowed. The package stops after adjudication regardless of outcome. Refinement
is not implemented or automatically authorized.
