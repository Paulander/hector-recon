# V2 fresh discriminator — pre-execution preregistration

Date: 2026-07-25

## Status and immutable boundary

This document freezes the replacement scientific runner before its fresh
discovery prefix is constructed or any fresh grounded receipt is minted. The
package is a chess-backed synthetic mechanism test conditional on one frozen
ecology. It is not KRK generalization, R1 evidence, or evidence about arbitrary
environments.

The retired V2 experiment, its integrity abort, and the atomic-snapshot
reclosure remain immutable. The protected sources are:

- V2.1 authority:
  `25945864fd998caf22ae12cbcb9bcb4779447337c0079f705640c63d2356f029`;
- V2.1 registry:
  `f3aee5cccf761af1cb6a5de94b886d5e758c0a07cb0f6d77b8898f662ca73b58`;
- atomic harness:
  `8611853ca56c2dab3e2a44ebad18997f9d9d55578627acbaff6e727a578fd894`.

Any mismatch stops as `engineering_boundary_requires_reclosure`.

## Question, hypothesis, and strongest null

Question: does receipt-disjoint prospective certification reduce prequential
false authority from prefix-perfect adaptively discovered patterns while
preserving authority for a genuinely recurring pattern?

Hypothesis: relative to lawful same-ledger authority, prospective authority
reduces pre-outcome false deployment on the spurious suffix family; relative to
a truthfully permuted prospective control, it preserves coverage of the planted
family.

Strongest null: after adequate target opportunity and engagement, prospective
authority is no safer than lawful legacy authority and no better at recurring-
pattern coverage than the truthful permutation.

The sole scientific change from the retired contract is a new physically
disjoint ecology and experiment/seed/terminal namespace executed through the
reclosed transport and transaction boundary. Learner, genome, lifecycle,
polarity, nomination, authority, exposure, endpoints, doses, gates, and
inference do not change.

## Information and authority boundary

The learner sees opaque typed terminal signals, graph actuation, ordinary native
request/error dynamics, StemCell lifecycle, and signed grounded receipts. It
never sees private atom roles, planted/spurious labels, expected outcomes,
mate predicates, candidate targets, suffix families, or the C permutation.

Preflight, exposure, event opening, and commitment comparison receive an
outcome-blind row and no outcome capability. Only the experiment-local mint
capability may execute the graph-emitted legal move and observe checkmate. It
durably records exactly one access before returning a signed V2.1 receipt.
There is no fallback outcome path.

The host may enumerate legal chess states, construct the frozen environment,
execute one graph-emitted move, persist graph outputs, and aggregate committed
records. It may not nominate, insert, select, relabel, or repair a learner cell.

## Fresh physical ecology

Experiment namespace:
`native_v2_fresh_prospective_discriminator.v1`.

Opaque-terminal salt:
`native-v2-fresh-opaque-terminals.20260725.v1`.

Logical rows and doses remain exactly:

- 32 prefix-positive rows carrying both target pairs;
- 32 prefix-negative rows, eight for each target singleton, making those
  singletons impure;
- eight suffix-spurious rows;
- eight suffix-planted rows;
- exactly one graph-emitted legal action and truthful checkmate/noncheckmate
  successors.

Physical selection is one-shot. First freeze a denylist of every retired A/C
prefix and suffix predecessor, predecessor/action/successor/outcome tuple, and
available V2.1 physical fingerprint. Enumerate legal mate/nonmate latent pairs
in the predecessor's canonical move/WK/BK order; exclude the denylist and the
permanently retired actual-V2 canary interactions; take the first 80 remaining
pairs. No offset, reordered enumeration, alternate salt, replacement ecology,
or second search is allowed. Fewer than 80 pairs stops as
`fresh_ecology_capacity_failure`.

The committed manifest must prove zero FEN, physical-transition, and V2.1-
fingerprint overlap. Frame or experiment IDs alone never establish freshness.

## Seeds, discovery, and targets

After the exact source/canary commit is pushed, derive all 32 unique 63-bit
genome seeds as the first eight SHA-256 bytes of:

`experiment_id | canonical | source_freeze_commit | ordinal`.

Keep ordinals 0–31, including seeds with absent targets. No seed search,
replacement, rerun, ensemble, or qualified-only analysis is permitted.

Discovery uses only prefix receipts, the native graph request/error mechanism,
the frozen content-blind genome and ordinary StemCell lifecycle, with authority
disconnected. No host conjunction enumeration or candidate insertion is
allowed. Polarity is fixed at nomination. Nomination then closes permanently;
suffix births, nomination, specialization, and topology changes are forbidden,
while grounded contradiction and revocation remain active.

Target selection is prefix-only. A candidate must have the exact predeclared
pattern digest, AVAILABLE polarity, mature structure, at least four prefix
supports, and no contradiction. The planted candidate and the deterministic
lexicographically first eligible spurious-family candidate are recorded;
missing targets remain `ABSENT`.

## Arms and exact identity law

Each seed constructs A/B/C together exactly once and persists those objects:

- A: `V2Mode.PROSPECTIVE.value`;
- B: `V2Mode.LEGACY.value`;
- C: `V2Mode.PROSPECTIVE.value` under a truthful latent permutation.

All arms must exactly match on complete candidate hypotheses, members, fixed
polarity, lineage, escrow, structural state, birth/certification frontiers,
source organism/state, topology, executed topology, and nomination closure.
Expected derived differences are enumerated and limited to:

- mode: B is `legacy_same_ledger`; A/C are `prospective`;
- initial authority: B equals lawful mature same-ledger cells; A/C are empty;
- experimental-identity and authority-identity digests derived solely from
  those two fields.

No other derived difference is lawful. Exact per-seed/per-arm identities are
frozen in the snapshot manifest; generic parity alone is insufficient.

## Admission and execution

Exposure restores the saved 96 snapshots; it never reclones. Before outcomes,
global preflight must verify exact 32x3 coverage, transport, restoration,
semantic identity, experiment identity, registry package hash, and the exact
precommitted receipt digest.

For every suffix row, A/B/C must match exactly on ordered signals, typed signal
manifests, actuation and option identity, matching-cell commitment, complete
candidate activation map, target activation, and logical opportunity. C may
differ only in the precommitted physical transition and truthful outcome.

A seed qualifies only when both exact targets have at least four distinct
physical opportunities in every arm. Admission requires at least 24/32. All 32
remain in inference after admission.

Execution is serial, seed-major and row-major through the unchanged atomic
harness. A durable PREPARED record precedes any access. Every committed row
binds complete A/B/C scientific records. Before COMMITTED, the carrier persists
the complete seed result and final serialized A/B/C snapshots with transport
and semantic identities. Dangling PREPARED is permanently nonresumable;
COMMITTED resumes only at the next seed.

## Endpoints, engagement, and inference

Primary values are pre-outcome:

`D_safe = false_deployment_authority[B]
          - false_deployment_authority[A]`

`D_signal = planted_authority_coverage[A]
            - planted_authority_coverage[C]`

False deployment means production-formal AVAILABLE followed by truthful false.
Planted coverage means the exact planted cell appears in the graph-consumed
AVAILABLE emission on planted-family rows, without conditioning on the current
outcome. Selected-spurious-attributable false authority is secondary only.

Engagement remains: zero immediate A/C authority; lawful B authority; A planted
target gains at least four support, no contradiction and graph certification in
at least 24/32; A selected spurious receives at least one grounded contradiction
in at least 24/32; lawful revocation; and the frozen C association break.

Use all 32 paired values, one-sided exact sign tests, Holm correction over
exactly `D_safe` and `D_signal`, adjusted `p <= 0.05`, and at least 17/32
positive differences for each. The valid verdicts remain:

- `targeted_prospective_evidence_starvation` before outcomes;
- `mechanism_contrast_starvation` after adequate admission but insufficient
  engagement;
- `valid_negative_prospective_separation_not_supported`;
- `prospective_evidence_separation_supported_in_frozen_ecology`.

## Pre-data canary, budgets, and stop rules

Before canonical ecology construction, one permanently retired toy ecology
runs through the actual V2 wrapper and new adapter. It tests corruption of the
first/last of 96 snapshots; identity and receipt swaps/fabrication; no-outcome
parity failures; A/B/C stage and durable-commit failures; no partial state;
restart law; journal-only aggregate reconstruction; fresh-process graph loading;
and exposure immutability.

Engineering budget: one new experiment module, one test module, two frozen
design documents, one retired canary artifact, and the pre-data manifests.
Canonical compute in this package is limited to physical ecology enumeration,
graph truth/parity validation, and hashing. Fresh discovery requests and fresh
grounded receipts are forbidden.

Kill criteria are any protected hash change, inability to make outcomes
capability-only, incomplete exact identity binding, fewer than 80 fresh pairs,
any physical overlap, non-atomic row/seed evidence, a canary integrity failure,
or source drift after the source-freeze commit. Stop rather than repair a
scientific factor.

This package ends after the pre-data manifests are committed and pushed for an
external implementation audit. It never executes the canonical discovery
prefix.
