# Native V2 portable-admission bridge recovery stop

## Classification

The clean recovery execution is a **terminal admission instrument stop**. It is
not a portable-admission pass, a learning result, mechanism evidence, or KRK
evidence. No exposure, science, or outcome command ran.

## Why this recovery execution existed

The original foreground process was lost with its managed conversation session
after writing only its historical-journal checkpoint. That original attempt is
preserved unchanged on `codex/native-krk-resume-composition` at `06707de`.

To make execution independent of the conversation lifetime, the recovery used
a separate clean worktree and branch from the exact frozen package commit
`8479cbdd22ed06d09eea3bd051a2e0e8344063ec`. A 75-second detached canary passed.
The actual process then ran with its own session ID, unique stdout/stderr files,
and durable package-owned records.

An initial transport launch resolved the editable environment back to the
original worktree and exited before creating a recovery attempt. Its record is
preserved. The corrected transport fixed module resolution to the recovery
worktree before the sole recovery attempt began; no package code or frozen
factor changed.

## Terminal result

Attempt `portable-admission-01-e7dfd710b975` ran as PID `59965`. It wrote:

- `00_started.json`, SHA-256
  `d9de2f6653134a74bd3f56afbc0c4f911b500cdc183e1cd3f163551a12353048`;
- `01_historical_journal_verified.json`, SHA-256
  `5d3f34984649e3f455edd5bf2a3fbb8597ad40c2a26594819ed288bfbc55b827`;
- `failure.json`, SHA-256
  `8f66ffb264c8509e75dcdfb883081ebdff9cf2017d589fdb1ae077c5ee79fba1`.

Historical-journal validation passed. During the next restoration boundary,
the unchanged historical runtime raised:

`LauncherIntegrityError: frozen input binding mismatch`

The failure record digest is
`a25bcf3994bf53f5d4960817e855ec6e86994dd11897e01d2da77be9383871e2`.
The frozen package requires an immediate terminal stop for this condition, so
processes two and three and aggregate verification were not started.

## Preserved guarantees

- Outcome access is exactly zero, with no event identities.
- The 195 protected files remain exact at set digest
  `9082cf52f505d924590458c4dd2a7f365bbdec3494cdbbc3d974726e97cb4239`.
- The restored historical implementation and test retain SHA-256 values
  `ae663e67ffad3dbdf8a7e988b5638181fa7a2c4398d3d46b7668cb80ee3d76a6`
  and `fc3ea605cb8e49c0b3f20b359e3e181b2736d236b2f22f4b6c49946a9d4d2004`.
- No tracked learner, graph, package, manifest, cohort, or scientific artifact
  changed during execution.
- No retry, widening, repair, exposure, or science execution followed.

The detached execution method solved the conversation-lifetime interruption
problem. The stop itself is a genuine frozen-package gate and now requires a
separately reviewed decision; infrastructure persistence cannot lawfully bypass
it.
