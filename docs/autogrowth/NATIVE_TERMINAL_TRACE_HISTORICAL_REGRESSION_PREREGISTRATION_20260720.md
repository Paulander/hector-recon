# Native terminal-trace historical regression preregistration

Date: 2026-07-20
Stage-1 source commit: `50d57fa41088d7dd6c0e0c82d8b6b3c1dd21354d`
Stage-1 result SHA-256: `2cd0010559599b10b7ff3cd5246cae96a8587b274513417201d882d640bf8bef`

This is the single inference-only opening of the sealed historical regression split. It is not fresh evidence. Regression becomes viewed development data after execution.

## Frozen cohort and data

All 96 Stage-1 trace-native organisms are retained: 32 ordinals in local-contrast, demotion-only, and counterexample-blind arms. No selection, union, voting, averaging, retraining, correction, lifecycle update, topology update, or evidence insertion is permitted.

Commitments:

- R0 regression: `964c8d543e03cc6d756eb0f52218133e9af95fdb6c97dc9c0aff8b8e58858f69`
- regression decoys: `acdafa01d92b7ee77053de438168c828bbf94d5006cc6dfe5d0cf42299ee64e8`
- fixed row order: `24899b4a004cf68d5e4a4105ea479496d26fea884d447a105e339cf783bffee4`

The pre-data manifest freezes every compressed/raw artifact hash, V3 digest, source hash, runner hash, metrics, gates, and stop rule before any regression FEN is constructed.

## Authority and admission

For each row, frozen R0 emits one REAL graph action and selected-option trace. The environment executes exactly that action and observes checkmate for scoring only. Every serialized wrapper is queried on the same position through its VIRTUAL trace-native dream session.

Admission requires 32 graph-owned references, 16 actual completions, 16 actual noncompletions, 96 organisms, exact complete GraphActuation and ordered typed terminal-signal parity, complete sessions, and zero persistent mutation/effects/host fallback/authority violations. Failure is an instrument abort.

## Metrics and gates

Per organism report TP, FP, abstentions, REFUTED outcomes, strict count, safe-narrow count, matching cells, depth/lineage, traces, and V3 before/after hashes.

Deployable TP equals TP only for an organism with FP=0; otherwise it is zero.

Primary gates:

- zero local FP across 32 x 16 negative decisions;
- at least 24/32 local organisms safe-narrow;
- at least 24/32 local organisms have nonzero TP on both viewed development and regression;
- local deployable TP exceeds demotion-only and counterexample-blind;
- paired per-organism deployable-TP superiority over both controls passes one-sided exact sign tests with Holm correction at alpha 0.05;
- at least one held-out TP advantage over demotion is emitted by a mature depth-one child;
- all integrity, authority, trace, serialization, and no-mutation gates pass.

## Interpretation and stop

- all gates: trace-native historical generalization of safe contradiction-triggered specialization;
- zero FP, superiority over demotion but not blind: specialization transfers but local-contrast advantage does not replicate;
- zero FP without superiority over demotion: safety transfers but recovered coverage does not;
- any local FP: specialization still overgeneralizes;
- no depth-one advantage: learned refinement itself did not transfer;
- integrity failure: instrument abort.

No in-package repair, threshold change, new pool, selected organism, R1, retired-65 access, or fresh data. Stop after adjudication.
