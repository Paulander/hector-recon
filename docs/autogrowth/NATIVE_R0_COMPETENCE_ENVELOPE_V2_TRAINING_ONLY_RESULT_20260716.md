# Native R0 competence-envelope V2 training-only result

Date: 2026-07-16
Preregistration commit: 588ba7b
Status: canonical admission abort; learning never opened

## Immutable artifact

reports/autogrowth/native_authority/touched_r0_competence_envelope_v2_training_only.json

SHA-256:
dc0b5a7df130295b83075e5211f8237263fd1d59c271c39d47ee3996c4fcdb6b

Runtime: 417.14 seconds.

## Admission result

The purity-corrected real observation path produced exactly:

- 64 total events;
- 40 completion successes;
- 24 completion failures;
- 64 policy responses;
- 24 response-present failures;
- 64 unique evidence keys;
- zero fabricated reward;
- zero persistent R0 mutation;
- zero host-authority tripwire calls.

The package nevertheless failed its frozen complete-path parity gate. At least
one real-path GraphActuation field or active competence-signal identity differed
from the corresponding immutable addendum virtual-wrapper row. Consequently the
connected and outcome-shuffled envelopes were never instantiated or grown.

## Interpretation

This is an admission/instrument abort, not a negative competence-learning
result. It says that the real observation path and the addendum virtual-wrapper
path are not yet proven identical at the complete internal-input level, despite
agreeing on the aggregate 40/24 outcomes and leaving persistent state unchanged.

The abort artifact records only aggregate admission gates. It does not persist
per-frame mismatch rows, so the differing frame and field cannot be identified
from the canonical artifact alone. That is an instrumentation limitation to be
reviewed before any rerun; it is not permission to repair and selectively retry
this package.

No lifecycle ran. Therefore there are no connected/outcome-shuffled results,
round histograms, pure-pattern enumeration, nomination verdict, lifecycle
verdict, or representation verdict. The 0.625 control was preregistered but not
evaluated. Validation, regression, retired successors, R1, and fresh data were
untouched.

Preserve this abort and stop for external review.
